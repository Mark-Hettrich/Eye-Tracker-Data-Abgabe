---
title: "encap_mt2"
output: html_document
---
```{r setup, include=FALSE}
knitr::opts_chunk$set(
  echo = FALSE,
  warning = FALSE,
  message = FALSE,
  error = FALSE,
  results = "asis"
)
```
<style>
table {
  width: 100%;
  table-layout: fixed;
  border-collapse: collapse;
}

th, td {
  white-space: normal !important;
  word-wrap: break-word;
  overflow-wrap: break-word;
  vertical-align: top;
  padding: 4px;
}
</style>

```{r}
library(here)
library(dplyr)
library(tidyr)
library(purrr)
library(tibble)
library(ggplot2)
library(lme4)
library(mgcv)
library(knitr)
library(kableExtra)
```

```{r}
encap_mt2 <- readRDS(here("ValidationData", "encap_mt2.rds"))
encap_mt2 <- droplevels(encap_mt2)
```

*Missing Combinations*
```{r}
library(dplyr)
library(tidyr)

# AOI x Referent
check_aoi_referent <- encap_mt2 %>%
  count(AOI, Referent, name = "n") %>%
  complete(AOI, Referent, fill = list(n = 0)) %>%
  arrange(AOI, Referent)

missing_aoi_referent <- check_aoi_referent %>%
  filter(n == 0)

# AOI x Category
check_aoi_category <- encap_mt2 %>%
  count(AOI, Category, name = "n") %>%
  complete(AOI, Category, fill = list(n = 0)) %>%
  arrange(AOI, Category)

missing_aoi_category <- check_aoi_category %>%
  filter(n == 0)

#check_aoi_referent
#check_aoi_category
#missing_aoi_referent
#missing_aoi_category

knitr::kable(
  missing_aoi_referent,
  format = "html",
  table.attr = 'style="width:100%; table-layout: fixed;"'
)

knitr::kable(
  missing_aoi_category,
  format = "html",
  table.attr = 'style="width:100%; table-layout: fixed;"'
)
```


TRT: mod_trt <- glmmTMB(TRT ~ Referent*AOI + Category*AOI + nLetters + (1 | Topic) + (1 | Participant), data = encap_mt2, family = Gamma(link = "log"))

RRT: mod_rrt <- glmmTMB( RRT ~ Referent*AOI + Category*AOI + nLetters + (1 | Topic) + (1 | Participant), ziformula = ~ Referent*AOI + Category*AOI + nLetters, data = encap_mt2, family = ziGamma(link = "log") )

*TRT: Full Data Fit Problems*
```{r}
library(dplyr)
library(tidyr)
library(purrr)
library(tibble)
library(stringr)
library(glmmTMB)
library(lme4)

# =========================================================
# HILFSFUNKTIONEN
# =========================================================

safe_plain_df <- function(df) {
  out <- as.data.frame(df, stringsAsFactors = FALSE)
  rownames(out) <- NULL
  out
}

round_df_4 <- function(df) {
  df %>%
    mutate(across(where(is.numeric), ~ round(.x, 4)))
}

collapse_unique <- function(x) {
  x <- unique(stats::na.omit(x))
  if (length(x) == 0) return(NA_character_)
  paste(x, collapse = " | ")
}

calc_fit_metrics <- function(obs, pred) {
  keep <- is.finite(obs) & is.finite(pred)
  obs  <- obs[keep]
  pred <- pred[keep]

  if (length(obs) == 0) {
    return(tibble(
      n_obs_metric = 0L,
      rmse_in_sample = NA_real_,
      mae_in_sample = NA_real_,
      bias_in_sample = NA_real_,
      cor_obs_pred = NA_real_
    ))
  }

  err <- pred - obs

  tibble(
    n_obs_metric = length(obs),
    rmse_in_sample = sqrt(mean(err^2)),
    mae_in_sample = mean(abs(err)),
    bias_in_sample = mean(err),
    cor_obs_pred = suppressWarnings(stats::cor(obs, pred))
  )
}

# =========================================================
# DATEN
# Erwartet:
# TRT, nLetters, Participant, Topic, AOI, aggr_level,
# Referent, Category
# =========================================================

prep_encap_mt2_fullfit_data <- function(df) {
  needed <- c(
    "TRT", "nLetters", "Participant", "Topic", "AOI", "aggr_level",
    "Referent", "Category"
  )

  missing_cols <- setdiff(needed, names(df))
  if (length(missing_cols) > 0) {
    stop("Fehlende Variablen: ", paste(missing_cols, collapse = ", "))
  }

  out <- df %>%
    filter(!is.na(TRT), TRT > 0) %>%
    filter(!is.na(nLetters)) %>%
    filter(!is.na(Participant), !is.na(Topic), !is.na(AOI), !is.na(aggr_level)) %>%
    filter(!is.na(Referent), !is.na(Category)) %>%
    mutate(
      Participant = factor(Participant),
      Topic = factor(Topic),
      AOI = factor(AOI),
      aggr_level = factor(aggr_level),
      Referent = factor(Referent),
      Category = factor(Category)
    )

  cluster_sizes <- out %>%
    distinct(aggr_level, AOI) %>%
    count(aggr_level, name = "n_aoi_in_cluster")

  list(
    data = safe_plain_df(out),
    cluster_sizes = cluster_sizes
  )
}

# =========================================================
# FORMELN PRO aggr_level
# Mehrere AOIs:
#   TRT ~ Referent*AOI + Category*AOI + nLetters + ...
# Eine AOI:
#   TRT ~ Referent + Category + nLetters + ...
# =========================================================

make_formula_cluster_encap_mt2 <- function(response = "TRT",
                                           n_aoi_in_cluster = 1L) {
  rhs <- if (n_aoi_in_cluster > 1) {
    paste(
      "Referent*AOI + Category*AOI +",
      "nLetters + (1 | Topic) + (1 | Participant)"
    )
  } else {
    paste(
      "Referent + Category +",
      "nLetters + (1 | Topic) + (1 | Participant)"
    )
  }

  as.formula(paste(response, "~", rhs))
}

# =========================================================
# DESIGN-CHECKS
# =========================================================

check_cells <- function(df, var, aoi = "AOI") {
  aoi_levels <- levels(df[[aoi]])
  var_levels <- levels(df[[var]])

  obs <- df %>%
    dplyr::count(.data[[aoi]], .data[[var]], name = "n")

  full_grid <- tidyr::expand_grid(
    AOI__tmp = aoi_levels,
    VAR__tmp = var_levels
  ) %>%
    rename(!!aoi := AOI__tmp, !!var := VAR__tmp)

  full_grid %>%
    left_join(obs, by = c(aoi, var)) %>%
    mutate(
      n = tidyr::replace_na(n, 0L),
      present = n > 0
    ) %>%
    arrange(.data[[aoi]], .data[[var]])
}

summarise_cell_check <- function(cell_tbl, var_name) {
  tibble(
    factor_name = var_name,
    n_cells_total = nrow(cell_tbl),
    n_cells_present = sum(cell_tbl$present),
    n_cells_missing = sum(!cell_tbl$present),
    missing_cells = if (sum(!cell_tbl$present) == 0) {
      NA_character_
    } else {
      paste0(
        cell_tbl %>%
          dplyr::filter(!present) %>%
          transmute(txt = paste0(AOI, " x ", .data[[var_name]])) %>%
          pull(txt),
        collapse = "; "
      )
    }
  )
}

within_aoi_variation <- function(df, var, aoi = "AOI") {
  df %>%
    group_by(.data[[aoi]]) %>%
    summarise(
      n_levels = n_distinct(.data[[var]]),
      .groups = "drop"
    ) %>%
    mutate(
      factor_name = var,
      has_variation = n_levels >= 2
    ) %>%
    rename(AOI = .data[[aoi]])
}

summarise_within_aoi_variation <- function(var_tbl, var_name) {
  tibble(
    factor_name = var_name,
    n_aoi_total = nrow(var_tbl),
    n_aoi_without_variation = sum(!var_tbl$has_variation),
    aoi_without_variation = if (sum(!var_tbl$has_variation) == 0) {
      NA_character_
    } else {
      paste(var_tbl$AOI[!var_tbl$has_variation], collapse = "; ")
    }
  )
}

inspect_design_structure_encap_mt2 <- function(df) {
  cell_referent <- check_cells(df, "Referent")
  cell_category <- check_cells(df, "Category")

  cell_summary <- bind_rows(
    summarise_cell_check(cell_referent, "Referent"),
    summarise_cell_check(cell_category, "Category")
  )

  var_referent <- within_aoi_variation(df, "Referent")
  var_category <- within_aoi_variation(df, "Category")

  variation_summary <- bind_rows(
    summarise_within_aoi_variation(var_referent, "Referent"),
    summarise_within_aoi_variation(var_category, "Category")
  )

  missing_cells_any <- any(cell_summary$n_cells_missing > 0)
  no_variation_any  <- any(variation_summary$n_aoi_without_variation > 0)

  tibble(
    design_missing_cells_any = missing_cells_any,
    design_no_variation_any = no_variation_any,
    design_missing_Referent_cells = cell_summary$missing_cells[cell_summary$factor_name == "Referent"],
    design_missing_Category_cells = cell_summary$missing_cells[cell_summary$factor_name == "Category"],
    design_aoi_without_Referent_variation = variation_summary$aoi_without_variation[variation_summary$factor_name == "Referent"],
    design_aoi_without_Category_variation = variation_summary$aoi_without_variation[variation_summary$factor_name == "Category"]
  )
}

# =========================================================
# DESIGNMATRIX-CHECK
# =========================================================

inspect_design_matrix_identifiability_encap_mt2 <- function(df, formula) {
  fixed_formula <- lme4::nobars(formula)
  mf_fixed <- model.frame(fixed_formula, data = droplevels(df), na.action = na.omit)
  X <- model.matrix(fixed_formula, data = mf_fixed)

  qrX <- qr(X)
  rankX <- qrX$rank
  pX <- ncol(X)
  rankdef <- rankX < pX

  redundant_cols <- if (rankdef) {
    pivot <- qrX$pivot
    redundant_idx <- pivot[seq.int(rankX + 1, pX)]
    colnames(X)[redundant_idx]
  } else {
    character(0)
  }

  tibble(
    X_rank = rankX,
    X_ncol = pX,
    fit_rank_deficient = rankdef,
    redundant_cols = collapse_unique(redundant_cols),
    n_distinct_AOI = dplyr::n_distinct(df$AOI),
    n_distinct_Referent = dplyr::n_distinct(df$Referent),
    n_distinct_Category = dplyr::n_distinct(df$Category),
    n_distinct_nLetters = dplyr::n_distinct(df$nLetters),
    min_nLetters = min(df$nLetters),
    max_nLetters = max(df$nLetters)
  )
}

# =========================================================
# GLMMTMB FIT MIT STATUS-CHECK
# =========================================================

fit_glmmtmb_capture_encap_mt2 <- function(df, formula) {
  warn_texts <- character()
  msg_texts  <- character()

  fit <- withCallingHandlers(
    tryCatch(
      glmmTMB::glmmTMB(
        formula = formula,
        data = droplevels(df),
        family = Gamma(link = "log")
      ),
      error = function(e) e
    ),
    warning = function(w) {
      warn_texts <<- c(warn_texts, conditionMessage(w))
      invokeRestart("muffleWarning")
    },
    message = function(m) {
      msg_texts <<- c(msg_texts, conditionMessage(m))
      invokeRestart("muffleMessage")
    }
  )

  if (inherits(fit, "error")) {
    return(list(
      fit_tbl = tibble(
        fit_ok = FALSE,
        fit_error = conditionMessage(fit),
        fit_warning_text = collapse_unique(warn_texts),
        fit_message_text = collapse_unique(msg_texts),
        n_warning_messages = length(unique(stats::na.omit(warn_texts))),
        n_messages = length(unique(stats::na.omit(msg_texts))),
        fit_nonposdef_hessian = NA,
        fit_nonzero_convergence = NA,
        aic = NA_real_,
        bic = NA_real_,
        logLik = NA_real_
      ),
      fit_obj = NULL
    ))
  }

  fit_nonposdef_hessian <- tryCatch(
    !isTRUE(fit$sdr$pdHess),
    error = function(e) NA
  )

  fit_nonzero_convergence <- tryCatch(
    {
      conv <- fit$fit$convergence
      if (is.null(conv)) NA else conv != 0
    },
    error = function(e) NA
  )

  list(
    fit_tbl = tibble(
      fit_ok = TRUE,
      fit_error = NA_character_,
      fit_warning_text = collapse_unique(warn_texts),
      fit_message_text = collapse_unique(msg_texts),
      n_warning_messages = length(unique(stats::na.omit(warn_texts))),
      n_messages = length(unique(stats::na.omit(msg_texts))),
      fit_nonposdef_hessian = fit_nonposdef_hessian,
      fit_nonzero_convergence = fit_nonzero_convergence,
      aic = tryCatch(as.numeric(AIC(fit)), error = function(e) NA_real_),
      bic = tryCatch(as.numeric(BIC(fit)), error = function(e) NA_real_),
      logLik = tryCatch(as.numeric(logLik(fit)), error = function(e) NA_real_)
    ),
    fit_obj = fit
  )
}

# =========================================================
# KLASSIFIKATION PROBLEMART
# =========================================================

classify_problem_encap_mt2 <- function(fit_rank_deficient,
                                       fit_warning_text,
                                       fit_message_text,
                                       fit_error,
                                       fit_nonposdef_hessian,
                                       fit_nonzero_convergence,
                                       design_missing_cells_any,
                                       design_no_variation_any) {
  txt <- paste(
    c(fit_warning_text, fit_message_text, fit_error),
    collapse = " | "
  )

  case_when(
    !is.na(fit_error) ~ "fit_error",
    isTRUE(fit_nonposdef_hessian) ~ "nonposdef_hessian",
    isTRUE(fit_nonzero_convergence) ~ "nonzero_convergence",
    isTRUE(fit_rank_deficient) & (design_missing_cells_any | design_no_variation_any) &
      str_detect(txt, regex("rank-deficient|dropping columns|dropp.*column", ignore_case = TRUE)) ~
      "design_driven_rank_deficiency_with_fit_signal",
    isTRUE(fit_rank_deficient) & (design_missing_cells_any | design_no_variation_any) ~
      "design_driven_rank_deficiency",
    isTRUE(fit_rank_deficient) &
      str_detect(txt, regex("rank-deficient|dropping columns|dropp.*column", ignore_case = TRUE)) ~
      "rank_deficient_design_and_fit_signal",
    isTRUE(fit_rank_deficient) ~ "rank_deficient_design",
    str_detect(txt, regex("rank-deficient|dropping columns|dropp.*column", ignore_case = TRUE)) ~
      "rank_deficient_warning_without_matrix_flag",
    str_detect(txt, regex("hessian|singular|not positive definite|non-positive-definite", ignore_case = TRUE)) ~
      "hessian_or_singularity_signal",
    str_detect(txt, regex("convergence|did not converge|false convergence|iteration limit", ignore_case = TRUE)) ~
      "convergence_signal",
    (!is.na(fit_warning_text) || !is.na(fit_message_text)) ~ "warning_or_message_only",
    TRUE ~ "none"
  )
}

# =========================================================
# EIN FULL-DATA-FIT PRO aggr_level
# =========================================================

fit_one_full_case_encap_mt2 <- function(df_aggr,
                                        formula,
                                        exp_name = "encap_mt2",
                                        aggr_i,
                                        n_aoi_in_cluster) {
  df_aggr <- droplevels(df_aggr)

  n_levels_aoi <- dplyr::n_distinct(df_aggr$AOI)
  n_levels_referent <- dplyr::n_distinct(df_aggr$Referent)
  n_levels_category <- dplyr::n_distinct(df_aggr$Category)
  n_levels_topic <- dplyr::n_distinct(df_aggr$Topic)
  n_levels_participant <- dplyr::n_distinct(df_aggr$Participant)

  if (n_levels_referent < 2) {
    return(
      tibble(
        experiment = exp_name,
        aggr_level = aggr_i,
        n_aoi_in_cluster = n_aoi_in_cluster,
        n_obs = nrow(df_aggr),
        n_topics = n_levels_topic,
        n_participants = n_levels_participant,
        formula = paste(deparse(formula), collapse = " "),
        X_rank = NA_real_,
        X_ncol = NA_real_,
        fit_rank_deficient = NA,
        redundant_cols = NA_character_,
        n_distinct_AOI = n_levels_aoi,
        n_distinct_Referent = n_levels_referent,
        n_distinct_Category = n_levels_category,
        n_distinct_nLetters = dplyr::n_distinct(df_aggr$nLetters),
        min_nLetters = min(df_aggr$nLetters),
        max_nLetters = max(df_aggr$nLetters),
        design_missing_cells_any = NA,
        design_no_variation_any = NA,
        design_missing_Referent_cells = NA_character_,
        design_missing_Category_cells = NA_character_,
        design_aoi_without_Referent_variation = NA_character_,
        design_aoi_without_Category_variation = NA_character_,
        fit_ok = FALSE,
        fit_error = "Referent has fewer than 2 levels in full data for this aggr_level",
        fit_warning_text = NA_character_,
        fit_message_text = NA_character_,
        n_warning_messages = 0L,
        n_messages = 0L,
        fit_nonposdef_hessian = NA,
        fit_nonzero_convergence = NA,
        aic = NA_real_,
        bic = NA_real_,
        logLik = NA_real_,
        n_obs_metric = NA_real_,
        rmse_in_sample = NA_real_,
        mae_in_sample = NA_real_,
        bias_in_sample = NA_real_,
        cor_obs_pred = NA_real_,
        problem_type = "insufficient_levels_Referent"
      )
    )
  }

  if (n_levels_category < 2) {
    return(
      tibble(
        experiment = exp_name,
        aggr_level = aggr_i,
        n_aoi_in_cluster = n_aoi_in_cluster,
        n_obs = nrow(df_aggr),
        n_topics = n_levels_topic,
        n_participants = n_levels_participant,
        formula = paste(deparse(formula), collapse = " "),
        X_rank = NA_real_,
        X_ncol = NA_real_,
        fit_rank_deficient = NA,
        redundant_cols = NA_character_,
        n_distinct_AOI = n_levels_aoi,
        n_distinct_Referent = n_levels_referent,
        n_distinct_Category = n_levels_category,
        n_distinct_nLetters = dplyr::n_distinct(df_aggr$nLetters),
        min_nLetters = min(df_aggr$nLetters),
        max_nLetters = max(df_aggr$nLetters),
        design_missing_cells_any = NA,
        design_no_variation_any = NA,
        design_missing_Referent_cells = NA_character_,
        design_missing_Category_cells = NA_character_,
        design_aoi_without_Referent_variation = NA_character_,
        design_aoi_without_Category_variation = NA_character_,
        fit_ok = FALSE,
        fit_error = "Category has fewer than 2 levels in full data for this aggr_level",
        fit_warning_text = NA_character_,
        fit_message_text = NA_character_,
        n_warning_messages = 0L,
        n_messages = 0L,
        fit_nonposdef_hessian = NA,
        fit_nonzero_convergence = NA,
        aic = NA_real_,
        bic = NA_real_,
        logLik = NA_real_,
        n_obs_metric = NA_real_,
        rmse_in_sample = NA_real_,
        mae_in_sample = NA_real_,
        bias_in_sample = NA_real_,
        cor_obs_pred = NA_real_,
        problem_type = "insufficient_levels_Category"
      )
    )
  }

  if (n_levels_aoi < 2 && n_aoi_in_cluster > 1) {
    return(
      tibble(
        experiment = exp_name,
        aggr_level = aggr_i,
        n_aoi_in_cluster = n_aoi_in_cluster,
        n_obs = nrow(df_aggr),
        n_topics = n_levels_topic,
        n_participants = n_levels_participant,
        formula = paste(deparse(formula), collapse = " "),
        X_rank = NA_real_,
        X_ncol = NA_real_,
        fit_rank_deficient = NA,
        redundant_cols = NA_character_,
        n_distinct_AOI = n_levels_aoi,
        n_distinct_Referent = n_levels_referent,
        n_distinct_Category = n_levels_category,
        n_distinct_nLetters = dplyr::n_distinct(df_aggr$nLetters),
        min_nLetters = min(df_aggr$nLetters),
        max_nLetters = max(df_aggr$nLetters),
        design_missing_cells_any = NA,
        design_no_variation_any = NA,
        design_missing_Referent_cells = NA_character_,
        design_missing_Category_cells = NA_character_,
        design_aoi_without_Referent_variation = NA_character_,
        design_aoi_without_Category_variation = NA_character_,
        fit_ok = FALSE,
        fit_error = "AOI has fewer than 2 levels although cluster is expected to contain multiple AOIs",
        fit_warning_text = NA_character_,
        fit_message_text = NA_character_,
        n_warning_messages = 0L,
        n_messages = 0L,
        fit_nonposdef_hessian = NA,
        fit_nonzero_convergence = NA,
        aic = NA_real_,
        bic = NA_real_,
        logLik = NA_real_,
        n_obs_metric = NA_real_,
        rmse_in_sample = NA_real_,
        mae_in_sample = NA_real_,
        bias_in_sample = NA_real_,
        cor_obs_pred = NA_real_,
        problem_type = "insufficient_levels_AOI"
      )
    )
  }

  design_structure <- inspect_design_structure_encap_mt2(df_aggr)

  design_info <- inspect_design_matrix_identifiability_encap_mt2(
    df = df_aggr,
    formula = formula
  )

  fit_res <- fit_glmmtmb_capture_encap_mt2(
    df = df_aggr,
    formula = formula
  )

  fit_info <- fit_res$fit_tbl
  fit_obj  <- fit_res$fit_obj

  fit_metrics <- if (!is.null(fit_obj)) {
    fitted_vals <- tryCatch(
      predict(fit_obj, type = "response", re.form = NULL),
      error = function(e) rep(NA_real_, nrow(df_aggr))
    )

    calc_fit_metrics(
      obs = df_aggr$TRT,
      pred = fitted_vals
    )
  } else {
    tibble(
      n_obs_metric = NA_real_,
      rmse_in_sample = NA_real_,
      mae_in_sample = NA_real_,
      bias_in_sample = NA_real_,
      cor_obs_pred = NA_real_
    )
  }

  tibble(
    experiment = exp_name,
    aggr_level = aggr_i,
    n_aoi_in_cluster = n_aoi_in_cluster,
    n_obs = nrow(df_aggr),
    n_topics = n_levels_topic,
    n_participants = n_levels_participant,
    formula = paste(deparse(formula), collapse = " ")
  ) %>%
    bind_cols(design_structure) %>%
    bind_cols(design_info) %>%
    bind_cols(fit_info) %>%
    bind_cols(fit_metrics) %>%
    mutate(
      problem_type = classify_problem_encap_mt2(
        fit_rank_deficient = fit_rank_deficient,
        fit_warning_text = fit_warning_text,
        fit_message_text = fit_message_text,
        fit_error = fit_error,
        fit_nonposdef_hessian = fit_nonposdef_hessian,
        fit_nonzero_convergence = fit_nonzero_convergence,
        design_missing_cells_any = design_missing_cells_any,
        design_no_variation_any = design_no_variation_any
      )
    )
}

# =========================================================
# FULL-DATA-FITS ÜBER aggr_level
# =========================================================

run_fullfits_encap_mt2 <- function(df,
                                   exp_name = "encap_mt2") {
  prep <- prep_encap_mt2_fullfit_data(df)
  dat <- prep$data
  cluster_sizes <- prep$cluster_sizes

  aggr_levels <- unique(as.character(dat$aggr_level))

  fullfit_all <- purrr::map_dfr(aggr_levels, function(aggr_i) {
    df_aggr <- dat %>%
      filter(as.character(aggr_level) == aggr_i)

    n_aoi_cluster <- cluster_sizes$n_aoi_in_cluster[
      match(aggr_i, as.character(cluster_sizes$aggr_level))
    ]

    formula_i <- make_formula_cluster_encap_mt2(
      response = "TRT",
      n_aoi_in_cluster = n_aoi_cluster
    )

    fit_one_full_case_encap_mt2(
      df_aggr = df_aggr,
      formula = formula_i,
      exp_name = exp_name,
      aggr_i = aggr_i,
      n_aoi_in_cluster = n_aoi_cluster
    )
  })

  problem_counts <- fullfit_all %>%
    count(problem_type, sort = TRUE)

  problem_fits_only <- fullfit_all %>%
    filter(
      problem_type != "none" |
        fit_ok %in% FALSE |
        fit_nonposdef_hessian %in% TRUE |
        fit_nonzero_convergence %in% TRUE |
        fit_rank_deficient %in% TRUE |
        n_warning_messages > 0
    ) %>%
    arrange(aggr_level)

  list(
    fullfit_all = fullfit_all,
    problem_counts = problem_counts,
    problem_fits_only = problem_fits_only
  )
}

# =========================================================
# AUSFÜHREN
# =========================================================

res_encap_mt2_fullfit <- run_fullfits_encap_mt2(
  df = encap_mt2,
  exp_name = "encap_mt2"
)

# =========================================================
# OUTPUTS
# =========================================================

#round_df_4(res_encap_mt2_fullfit$fullfit_all)
#round_df_4(res_encap_mt2_fullfit$problem_counts)
#round_df_4(res_encap_mt2_fullfit$problem_fits_only)

knitr::kable(
  round_df_4(res_encap_mt2_fullfit$problem_counts),
  format = "html",
  table.attr = 'style="width:100%; table-layout: fixed;"'
)
```


*TRT: Topic LOO + Fit Problems*
```{r}
library(dplyr)
library(tidyr)
library(purrr)
library(tibble)
library(stringr)
library(glmmTMB)
library(lme4)

# =========================================================
# HILFSFUNKTIONEN
# =========================================================

safe_plain_df <- function(df) {
  out <- as.data.frame(df, stringsAsFactors = FALSE)
  rownames(out) <- NULL
  out
}

round_df_4 <- function(df) {
  df %>%
    mutate(across(where(is.numeric), ~ round(.x, 4)))
}

collapse_unique <- function(x) {
  x <- unique(stats::na.omit(x))
  if (length(x) == 0) return(NA_character_)
  paste(x, collapse = " | ")
}

calc_metrics <- function(obs, pred) {
  keep <- is.finite(obs) & is.finite(pred)
  obs  <- obs[keep]
  pred <- pred[keep]

  if (length(obs) == 0) {
    return(tibble(
      n_test = 0L,
      rmse   = NA_real_,
      mae    = NA_real_,
      bias   = NA_real_
    ))
  }

  err <- pred - obs

  tibble(
    n_test = length(obs),
    rmse   = sqrt(mean(err^2)),
    mae    = mean(abs(err)),
    bias   = mean(err)
  )
}

# =========================================================
# DATEN
# Erwartet:
# TRT, Referent, Category, AOI, nLetters,
# Participant, Topic, aggr_level
# =========================================================

prep_encap_mt2_topicloo_data <- function(df) {
  needed <- c(
    "TRT", "Referent", "Category", "AOI",
    "nLetters", "Participant", "Topic", "aggr_level"
  )

  missing_cols <- setdiff(needed, names(df))
  if (length(missing_cols) > 0) {
    stop("Fehlende Variablen: ", paste(missing_cols, collapse = ", "))
  }

  out <- df %>%
    filter(!is.na(TRT), TRT > 0) %>%
    filter(!is.na(Referent), !is.na(Category), !is.na(AOI), !is.na(nLetters)) %>%
    filter(!is.na(Participant), !is.na(Topic), !is.na(aggr_level)) %>%
    mutate(
      Referent = factor(Referent),
      Category = factor(Category),
      AOI = factor(AOI),
      Participant = factor(Participant),
      Topic = factor(Topic),
      aggr_level = factor(aggr_level)
    )

  cluster_sizes <- out %>%
    distinct(aggr_level, AOI) %>%
    count(aggr_level, name = "n_aoi_in_cluster")

  list(
    data = safe_plain_df(out),
    cluster_sizes = cluster_sizes
  )
}

# =========================================================
# FORMELN PRO CLUSTER
# - mehrere AOIs im Cluster:
#     TRT ~ Referent*AOI + Category*AOI + nLetters + ...
# - genau eine AOI im Cluster:
#     TRT ~ Referent + Category + nLetters + ...
# =========================================================

make_formula_cluster_encap_mt2 <- function(response = "TRT",
                                           n_aoi_in_cluster = 1L) {
  rhs <- if (n_aoi_in_cluster > 1) {
    "Referent*AOI + Category*AOI + nLetters + (1 | Topic) + (1 | Participant)"
  } else {
    "Referent + Category + nLetters + (1 | Topic) + (1 | Participant)"
  }

  as.formula(paste(response, "~", rhs))
}

# =========================================================
# DESIGNMATRIX-CHECK
# =========================================================

inspect_design_matrix_identifiability_encap_mt2 <- function(df,
                                                            formula,
                                                            n_aoi_in_cluster) {
  fixed_formula <- lme4::nobars(formula)
  mf_fixed <- model.frame(fixed_formula, data = droplevels(df), na.action = na.omit)
  X <- model.matrix(fixed_formula, data = mf_fixed)

  qrX <- qr(X)
  rankX <- qrX$rank
  pX <- ncol(X)
  rankdef <- rankX < pX

  redundant_cols <- if (rankdef) {
    pivot <- qrX$pivot
    redundant_idx <- pivot[seq.int(rankX + 1, pX)]
    colnames(X)[redundant_idx]
  } else {
    character(0)
  }

  if (n_aoi_in_cluster > 1) {
    cell_df <- df %>%
      group_by(AOI, Referent, Category, .drop = FALSE) %>%
      summarise(
        n_obs = dplyr::n(),
        n_distinct_nLetters = dplyr::n_distinct(nLetters),
        .groups = "drop"
      )
  } else {
    cell_df <- df %>%
      group_by(Referent, Category, .drop = FALSE) %>%
      summarise(
        n_obs = dplyr::n(),
        n_distinct_nLetters = dplyr::n_distinct(nLetters),
        .groups = "drop"
      )
  }

  tibble(
    X_rank = rankX,
    X_ncol = pX,
    fit_rank_deficient = rankdef,
    redundant_cols = collapse_unique(redundant_cols),
    n_distinct_nLetters = dplyr::n_distinct(df$nLetters),
    min_nLetters = min(df$nLetters),
    max_nLetters = max(df$nLetters),
    n_design_cells = nrow(cell_df),
    n_empty_design_cells = sum(cell_df$n_obs == 0),
    n_cells_nLetters_constant = sum(cell_df$n_distinct_nLetters <= 1)
  )
}

# =========================================================
# GLMMTMB FIT MIT STATUS-CHECK
# =========================================================

fit_glmmtmb_capture_encap_mt2 <- function(df,
                                          formula) {
  warn_texts <- character()
  msg_texts  <- character()

  fit <- withCallingHandlers(
    tryCatch(
      glmmTMB::glmmTMB(
        formula = formula,
        data = droplevels(df),
        family = Gamma(link = "log")
      ),
      error = function(e) e
    ),
    warning = function(w) {
      warn_texts <<- c(warn_texts, conditionMessage(w))
      invokeRestart("muffleWarning")
    },
    message = function(m) {
      msg_texts <<- c(msg_texts, conditionMessage(m))
      invokeRestart("muffleMessage")
    }
  )

  if (inherits(fit, "error")) {
    return(list(
      fit_tbl = tibble(
        fit_ok = FALSE,
        fit_error = conditionMessage(fit),
        fit_warning_text = collapse_unique(warn_texts),
        fit_message_text = collapse_unique(msg_texts),
        n_warning_messages = length(unique(stats::na.omit(warn_texts))),
        n_messages = length(unique(stats::na.omit(msg_texts))),
        fit_nonposdef_hessian = NA,
        fit_nonzero_convergence = NA,
        aic = NA_real_,
        bic = NA_real_,
        logLik = NA_real_
      ),
      fit_obj = NULL
    ))
  }

  fit_nonposdef_hessian <- tryCatch(
    !isTRUE(fit$sdr$pdHess),
    error = function(e) NA
  )

  fit_nonzero_convergence <- tryCatch(
    {
      conv <- fit$fit$convergence
      if (is.null(conv)) NA else conv != 0
    },
    error = function(e) NA
  )

  list(
    fit_tbl = tibble(
      fit_ok = TRUE,
      fit_error = NA_character_,
      fit_warning_text = collapse_unique(warn_texts),
      fit_message_text = collapse_unique(msg_texts),
      n_warning_messages = length(unique(stats::na.omit(warn_texts))),
      n_messages = length(unique(stats::na.omit(msg_texts))),
      fit_nonposdef_hessian = fit_nonposdef_hessian,
      fit_nonzero_convergence = fit_nonzero_convergence,
      aic = tryCatch(as.numeric(AIC(fit)), error = function(e) NA_real_),
      bic = tryCatch(as.numeric(BIC(fit)), error = function(e) NA_real_),
      logLik = tryCatch(as.numeric(logLik(fit)), error = function(e) NA_real_)
    ),
    fit_obj = fit
  )
}

# =========================================================
# KLASSIFIKATION PROBLEMART
# =========================================================

classify_problem_encap_mt2 <- function(fit_rank_deficient,
                                       fit_warning_text,
                                       fit_message_text,
                                       fit_error,
                                       fit_nonposdef_hessian,
                                       fit_nonzero_convergence,
                                       prediction_error) {
  txt <- paste(
    c(fit_warning_text, fit_message_text, fit_error, prediction_error),
    collapse = " | "
  )

  case_when(
    !is.na(fit_error) ~ "fit_error",
    !is.na(prediction_error) ~ "prediction_error",
    isTRUE(fit_nonposdef_hessian) ~ "nonposdef_hessian",
    isTRUE(fit_nonzero_convergence) ~ "nonzero_convergence",
    isTRUE(fit_rank_deficient) &
      str_detect(txt, regex("rank-deficient|dropping columns|dropp.*column", ignore_case = TRUE)) ~
      "rank_deficient_design_and_fit_signal",
    isTRUE(fit_rank_deficient) ~ "rank_deficient_design",
    str_detect(txt, regex("rank-deficient|dropping columns|dropp.*column", ignore_case = TRUE)) ~
      "rank_deficient_warning_without_matrix_flag",
    str_detect(txt, regex("hessian|singular|not positive definite|non-positive-definite", ignore_case = TRUE)) ~
      "hessian_or_singularity_signal",
    str_detect(txt, regex("convergence|did not converge|false convergence|iteration limit", ignore_case = TRUE)) ~
      "convergence_signal",
    (!is.na(fit_warning_text) || !is.na(fit_message_text)) ~ "warning_or_message_only",
    TRUE ~ "none"
  )
}

# =========================================================
# EIN TOPIC-LOO-FIT
# =========================================================

fit_one_topicloo_case_encap_mt2 <- function(train_df,
                                            test_df,
                                            formula,
                                            n_aoi_in_cluster,
                                            exp_name = "encap_mt2",
                                            aggr_i,
                                            topic_i) {
  train_df <- droplevels(train_df)
  test_df  <- droplevels(test_df)

  design_info <- inspect_design_matrix_identifiability_encap_mt2(
    df = train_df,
    formula = formula,
    n_aoi_in_cluster = n_aoi_in_cluster
  )

  fit_res <- fit_glmmtmb_capture_encap_mt2(
    df = train_df,
    formula = formula
  )

  fit_info <- fit_res$fit_tbl
  fit_obj  <- fit_res$fit_obj

  pred_error <- NA_character_
  pred <- rep(NA_real_, nrow(test_df))

  if (!is.null(fit_obj)) {
    pred <- tryCatch(
      predict(
        fit_obj,
        newdata = test_df,
        type = "response",
        re.form = NULL,
        allow.new.levels = TRUE
      ),
      error = function(e) {
        pred_error <<- conditionMessage(e)
        rep(NA_real_, nrow(test_df))
      }
    )
  }

  metric_info <- calc_metrics(
    obs = test_df$TRT,
    pred = pred
  )

  tibble(
    experiment = exp_name,
    aggr_level = aggr_i,
    heldout_topic = topic_i,
    n_aoi_in_cluster = n_aoi_in_cluster,
    cluster_formula_type = ifelse(n_aoi_in_cluster > 1, "Referent_Category_AOI", "Referent_Category_only"),
    n_train = nrow(train_df),
    n_test_rows = nrow(test_df),
    formula = paste(deparse(formula), collapse = " ")
  ) %>%
    bind_cols(design_info) %>%
    bind_cols(fit_info) %>%
    bind_cols(metric_info) %>%
    mutate(
      prediction_error = pred_error,
      problem_type = classify_problem_encap_mt2(
        fit_rank_deficient = fit_rank_deficient,
        fit_warning_text = fit_warning_text,
        fit_message_text = fit_message_text,
        fit_error = fit_error,
        fit_nonposdef_hessian = fit_nonposdef_hessian,
        fit_nonzero_convergence = fit_nonzero_convergence,
        prediction_error = prediction_error
      )
    )
}

# =========================================================
# TOPIC-LOO FÜR encap_mt2
# =========================================================

run_topicloo_encap_mt2 <- function(df,
                                   exp_name = "encap_mt2",
                                   verbose = TRUE) {
  prep <- prep_encap_mt2_topicloo_data(df)
  dat <- prep$data
  cluster_sizes <- prep$cluster_sizes

  aggr_levels <- unique(as.character(dat$aggr_level))

  loo_all <- purrr::map_dfr(aggr_levels, function(aggr_i) {
    dat_cluster <- dat %>%
      filter(as.character(aggr_level) == aggr_i)

    n_aoi_cluster <- cluster_sizes$n_aoi_in_cluster[
      match(aggr_i, cluster_sizes$aggr_level)
    ]

    formula_i <- make_formula_cluster_encap_mt2(
      response = "TRT",
      n_aoi_in_cluster = n_aoi_cluster
    )

    topics_cluster <- unique(as.character(dat_cluster$Topic))

    purrr::map_dfr(topics_cluster, function(topic_i) {
      train_df <- dat_cluster %>%
        filter(as.character(Topic) != topic_i)

      test_df <- dat_cluster %>%
        filter(as.character(Topic) == topic_i)

      if (isTRUE(verbose)) {
        message(
          "[Topic-LOO] exp=", exp_name,
          " aggr_level=", aggr_i,
          " holdout_topic=", topic_i,
          " n_train=", nrow(train_df)
        )
      }

      fit_one_topicloo_case_encap_mt2(
        train_df = train_df,
        test_df = test_df,
        formula = formula_i,
        n_aoi_in_cluster = n_aoi_cluster,
        exp_name = exp_name,
        aggr_i = aggr_i,
        topic_i = topic_i
      )
    })
  })

  loo_summary <- loo_all %>%
    mutate(has_warning = n_warning_messages > 0) %>%
    group_by(experiment, aggr_level) %>%
    summarise(
      n_holdout_fits = dplyr::n(),
      n_fit_failed = sum(fit_ok %in% FALSE, na.rm = TRUE),
      n_nonposdef_hessian = sum(fit_nonposdef_hessian %in% TRUE, na.rm = TRUE),
      n_nonzero_convergence = sum(fit_nonzero_convergence %in% TRUE, na.rm = TRUE),
      n_rank_deficient = sum(fit_rank_deficient %in% TRUE, na.rm = TRUE),
      n_fits_with_warning = sum(has_warning, na.rm = TRUE),
      total_warning_messages = sum(n_warning_messages, na.rm = TRUE),
      mean_rmse = mean(rmse, na.rm = TRUE),
      mean_mae = mean(mae, na.rm = TRUE),
      mean_bias = mean(bias, na.rm = TRUE),
      median_rmse = median(rmse, na.rm = TRUE),
      median_mae = median(mae, na.rm = TRUE),
      median_bias = median(bias, na.rm = TRUE),
      .groups = "drop"
    )

  problem_counts <- loo_all %>%
    count(aggr_level, problem_type, sort = TRUE)

  problem_fits_only <- loo_all %>%
    filter(
      problem_type != "none" |
        fit_ok %in% FALSE |
        fit_nonposdef_hessian %in% TRUE |
        fit_nonzero_convergence %in% TRUE |
        fit_rank_deficient %in% TRUE |
        n_warning_messages > 0 |
        !is.na(prediction_error)
    ) %>%
    arrange(aggr_level, heldout_topic)

  list(
    loo_all = loo_all,
    loo_summary = loo_summary,
    problem_counts = problem_counts,
    problem_fits_only = problem_fits_only
  )
}

# =========================================================
# AUSFÜHREN
# =========================================================

res_encap_mt2_topicloo <- run_topicloo_encap_mt2(
  df = encap_mt2,
  exp_name = "encap_mt2",
  verbose = TRUE
)

# =========================================================
# OUTPUTS
# =========================================================

#round_df_4(res_encap_mt2_topicloo$loo_all)
#round_df_4(res_encap_mt2_topicloo$loo_summary)
#round_df_4(res_encap_mt2_topicloo$problem_counts)
#round_df_4(res_encap_mt2_topicloo$problem_fits_only)

knitr::kable(
  round_df_4(res_encap_mt2_topicloo$loo_summary),
  format = "html",
  table.attr = 'style="width:100%; table-layout: fixed;"'
)


```


*TRT: Participant LOO +  Fit Problems*
```{r}
library(dplyr)
library(tidyr)
library(purrr)
library(tibble)
library(stringr)
library(glmmTMB)
library(lme4)

# =========================================================
# HILFSFUNKTIONEN
# =========================================================

safe_plain_df <- function(df) {
  out <- as.data.frame(df, stringsAsFactors = FALSE)
  rownames(out) <- NULL
  out
}

round_df_4 <- function(df) {
  df %>%
    mutate(across(where(is.numeric), ~ round(.x, 4)))
}

collapse_unique <- function(x) {
  x <- unique(stats::na.omit(x))
  if (length(x) == 0) return(NA_character_)
  paste(x, collapse = " | ")
}

calc_metrics <- function(obs, pred) {
  keep <- is.finite(obs) & is.finite(pred)
  obs  <- obs[keep]
  pred <- pred[keep]

  if (length(obs) == 0) {
    return(tibble(
      n_test = 0L,
      rmse   = NA_real_,
      mae    = NA_real_,
      bias   = NA_real_
    ))
  }

  err <- pred - obs

  tibble(
    n_test = length(obs),
    rmse   = sqrt(mean(err^2)),
    mae    = mean(abs(err)),
    bias   = mean(err)
  )
}

# =========================================================
# DATEN
# Erwartet:
# TRT, Referent, Category, AOI, nLetters,
# Participant, Topic, aggr_level
# =========================================================

prep_encap_mt2_participantloo_data <- function(df) {
  needed <- c(
    "TRT", "Referent", "Category", "AOI",
    "nLetters", "Participant", "Topic", "aggr_level"
  )

  missing_cols <- setdiff(needed, names(df))
  if (length(missing_cols) > 0) {
    stop("Fehlende Variablen: ", paste(missing_cols, collapse = ", "))
  }

  out <- df %>%
    filter(!is.na(TRT), TRT > 0) %>%
    filter(!is.na(Referent), !is.na(Category), !is.na(AOI), !is.na(nLetters)) %>%
    filter(!is.na(Participant), !is.na(Topic), !is.na(aggr_level)) %>%
    mutate(
      Referent = factor(Referent),
      Category = factor(Category),
      AOI = factor(AOI),
      Participant = factor(Participant),
      Topic = factor(Topic),
      aggr_level = factor(aggr_level)
    )

  cluster_sizes <- out %>%
    distinct(aggr_level, AOI) %>%
    count(aggr_level, name = "n_aoi_in_cluster")

  list(
    data = safe_plain_df(out),
    cluster_sizes = cluster_sizes
  )
}

# =========================================================
# FORMELN PRO CLUSTER
# - mehrere AOIs im Cluster:
#     TRT ~ Referent*AOI + Category*AOI + nLetters + ...
# - genau eine AOI im Cluster:
#     TRT ~ Referent + Category + nLetters + ...
# =========================================================

make_formula_cluster_encap_mt2 <- function(response = "TRT",
                                           n_aoi_in_cluster = 1L) {
  rhs <- if (n_aoi_in_cluster > 1) {
    "Referent*AOI + Category*AOI + nLetters + (1 | Topic) + (1 | Participant)"
  } else {
    "Referent + Category + nLetters + (1 | Topic) + (1 | Participant)"
  }

  as.formula(paste(response, "~", rhs))
}

# =========================================================
# DESIGNMATRIX-CHECK
# =========================================================

inspect_design_matrix_identifiability_encap_mt2 <- function(df,
                                                            formula,
                                                            n_aoi_in_cluster) {
  fixed_formula <- lme4::nobars(formula)
  mf_fixed <- model.frame(fixed_formula, data = droplevels(df), na.action = na.omit)
  X <- model.matrix(fixed_formula, data = mf_fixed)

  qrX <- qr(X)
  rankX <- qrX$rank
  pX <- ncol(X)
  rankdef <- rankX < pX

  redundant_cols <- if (rankdef) {
    pivot <- qrX$pivot
    redundant_idx <- pivot[seq.int(rankX + 1, pX)]
    colnames(X)[redundant_idx]
  } else {
    character(0)
  }

  if (n_aoi_in_cluster > 1) {
    cell_df <- df %>%
      group_by(AOI, Referent, Category, .drop = FALSE) %>%
      summarise(
        n_obs = dplyr::n(),
        n_distinct_nLetters = dplyr::n_distinct(nLetters),
        .groups = "drop"
      )
  } else {
    cell_df <- df %>%
      group_by(Referent, Category, .drop = FALSE) %>%
      summarise(
        n_obs = dplyr::n(),
        n_distinct_nLetters = dplyr::n_distinct(nLetters),
        .groups = "drop"
      )
  }

  tibble(
    X_rank = rankX,
    X_ncol = pX,
    fit_rank_deficient = rankdef,
    redundant_cols = collapse_unique(redundant_cols),
    n_distinct_nLetters = dplyr::n_distinct(df$nLetters),
    min_nLetters = min(df$nLetters),
    max_nLetters = max(df$nLetters),
    n_design_cells = nrow(cell_df),
    n_empty_design_cells = sum(cell_df$n_obs == 0),
    n_cells_nLetters_constant = sum(cell_df$n_distinct_nLetters <= 1)
  )
}

# =========================================================
# GLMMTMB FIT MIT STATUS-CHECK
# =========================================================

fit_glmmtmb_capture_encap_mt2 <- function(df,
                                          formula) {
  warn_texts <- character()
  msg_texts  <- character()

  fit <- withCallingHandlers(
    tryCatch(
      glmmTMB::glmmTMB(
        formula = formula,
        data = droplevels(df),
        family = Gamma(link = "log")
      ),
      error = function(e) e
    ),
    warning = function(w) {
      warn_texts <<- c(warn_texts, conditionMessage(w))
      invokeRestart("muffleWarning")
    },
    message = function(m) {
      msg_texts <<- c(msg_texts, conditionMessage(m))
      invokeRestart("muffleMessage")
    }
  )

  if (inherits(fit, "error")) {
    return(list(
      fit_tbl = tibble(
        fit_ok = FALSE,
        fit_error = conditionMessage(fit),
        fit_warning_text = collapse_unique(warn_texts),
        fit_message_text = collapse_unique(msg_texts),
        n_warning_messages = length(unique(stats::na.omit(warn_texts))),
        n_messages = length(unique(stats::na.omit(msg_texts))),
        fit_nonposdef_hessian = NA,
        fit_nonzero_convergence = NA,
        aic = NA_real_,
        bic = NA_real_,
        logLik = NA_real_
      ),
      fit_obj = NULL
    ))
  }

  fit_nonposdef_hessian <- tryCatch(
    !isTRUE(fit$sdr$pdHess),
    error = function(e) NA
  )

  fit_nonzero_convergence <- tryCatch(
    {
      conv <- fit$fit$convergence
      if (is.null(conv)) NA else conv != 0
    },
    error = function(e) NA
  )

  list(
    fit_tbl = tibble(
      fit_ok = TRUE,
      fit_error = NA_character_,
      fit_warning_text = collapse_unique(warn_texts),
      fit_message_text = collapse_unique(msg_texts),
      n_warning_messages = length(unique(stats::na.omit(warn_texts))),
      n_messages = length(unique(stats::na.omit(msg_texts))),
      fit_nonposdef_hessian = fit_nonposdef_hessian,
      fit_nonzero_convergence = fit_nonzero_convergence,
      aic = tryCatch(as.numeric(AIC(fit)), error = function(e) NA_real_),
      bic = tryCatch(as.numeric(BIC(fit)), error = function(e) NA_real_),
      logLik = tryCatch(as.numeric(logLik(fit)), error = function(e) NA_real_)
    ),
    fit_obj = fit
  )
}

# =========================================================
# KLASSIFIKATION PROBLEMART
# =========================================================

classify_problem_encap_mt2 <- function(fit_rank_deficient,
                                       fit_warning_text,
                                       fit_message_text,
                                       fit_error,
                                       fit_nonposdef_hessian,
                                       fit_nonzero_convergence,
                                       prediction_error) {
  txt <- paste(
    c(fit_warning_text, fit_message_text, fit_error, prediction_error),
    collapse = " | "
  )

  case_when(
    !is.na(fit_error) ~ "fit_error",
    !is.na(prediction_error) ~ "prediction_error",
    isTRUE(fit_nonposdef_hessian) ~ "nonposdef_hessian",
    isTRUE(fit_nonzero_convergence) ~ "nonzero_convergence",
    isTRUE(fit_rank_deficient) &
      str_detect(txt, regex("rank-deficient|dropping columns|dropp.*column", ignore_case = TRUE)) ~
      "rank_deficient_design_and_fit_signal",
    isTRUE(fit_rank_deficient) ~ "rank_deficient_design",
    str_detect(txt, regex("rank-deficient|dropping columns|dropp.*column", ignore_case = TRUE)) ~
      "rank_deficient_warning_without_matrix_flag",
    str_detect(txt, regex("hessian|singular|not positive definite|non-positive-definite", ignore_case = TRUE)) ~
      "hessian_or_singularity_signal",
    str_detect(txt, regex("convergence|did not converge|false convergence|iteration limit", ignore_case = TRUE)) ~
      "convergence_signal",
    (!is.na(fit_warning_text) || !is.na(fit_message_text)) ~ "warning_or_message_only",
    TRUE ~ "none"
  )
}

# =========================================================
# EIN PARTICIPANT-LOO-FIT
# =========================================================

fit_one_participantloo_case_encap_mt2 <- function(train_df,
                                                  test_df,
                                                  formula,
                                                  n_aoi_in_cluster,
                                                  exp_name = "encap_mt2",
                                                  aggr_i,
                                                  participant_i) {
  train_df <- droplevels(train_df)
  test_df  <- droplevels(test_df)

  design_info <- inspect_design_matrix_identifiability_encap_mt2(
    df = train_df,
    formula = formula,
    n_aoi_in_cluster = n_aoi_in_cluster
  )

  fit_res <- fit_glmmtmb_capture_encap_mt2(
    df = train_df,
    formula = formula
  )

  fit_info <- fit_res$fit_tbl
  fit_obj  <- fit_res$fit_obj

  pred_error <- NA_character_
  pred <- rep(NA_real_, nrow(test_df))

  if (!is.null(fit_obj)) {
    pred <- tryCatch(
      predict(
        fit_obj,
        newdata = test_df,
        type = "response",
        re.form = NULL,
        allow.new.levels = TRUE
      ),
      error = function(e) {
        pred_error <<- conditionMessage(e)
        rep(NA_real_, nrow(test_df))
      }
    )
  }

  metric_info <- calc_metrics(
    obs = test_df$TRT,
    pred = pred
  )

  tibble(
    experiment = exp_name,
    aggr_level = aggr_i,
    heldout_participant = participant_i,
    n_aoi_in_cluster = n_aoi_in_cluster,
    cluster_formula_type = ifelse(n_aoi_in_cluster > 1, "Referent_Category_AOI", "Referent_Category_only"),
    n_train = nrow(train_df),
    n_test_rows = nrow(test_df),
    formula = paste(deparse(formula), collapse = " ")
  ) %>%
    bind_cols(design_info) %>%
    bind_cols(fit_info) %>%
    bind_cols(metric_info) %>%
    mutate(
      prediction_error = pred_error,
      problem_type = classify_problem_encap_mt2(
        fit_rank_deficient = fit_rank_deficient,
        fit_warning_text = fit_warning_text,
        fit_message_text = fit_message_text,
        fit_error = fit_error,
        fit_nonposdef_hessian = fit_nonposdef_hessian,
        fit_nonzero_convergence = fit_nonzero_convergence,
        prediction_error = prediction_error
      )
    )
}

# =========================================================
# PARTICIPANT-LOO FÜR encap_mt2
# =========================================================

run_participantloo_encap_mt2 <- function(df,
                                         exp_name = "encap_mt2",
                                         verbose = TRUE) {
  prep <- prep_encap_mt2_participantloo_data(df)
  dat <- prep$data
  cluster_sizes <- prep$cluster_sizes

  aggr_levels <- unique(as.character(dat$aggr_level))

  loo_all <- purrr::map_dfr(aggr_levels, function(aggr_i) {
    dat_cluster <- dat %>%
      filter(as.character(aggr_level) == aggr_i)

    n_aoi_cluster <- cluster_sizes$n_aoi_in_cluster[
      match(aggr_i, cluster_sizes$aggr_level)
    ]

    formula_i <- make_formula_cluster_encap_mt2(
      response = "TRT",
      n_aoi_in_cluster = n_aoi_cluster
    )

    participants_cluster <- unique(as.character(dat_cluster$Participant))

    purrr::map_dfr(participants_cluster, function(participant_i) {
      train_df <- dat_cluster %>%
        filter(as.character(Participant) != participant_i)

      test_df <- dat_cluster %>%
        filter(as.character(Participant) == participant_i)

      if (isTRUE(verbose)) {
        message(
          "[Participant-LOO] exp=", exp_name,
          " aggr_level=", aggr_i,
          " holdout_participant=", participant_i,
          " n_train=", nrow(train_df)
        )
      }

      fit_one_participantloo_case_encap_mt2(
        train_df = train_df,
        test_df = test_df,
        formula = formula_i,
        n_aoi_in_cluster = n_aoi_cluster,
        exp_name = exp_name,
        aggr_i = aggr_i,
        participant_i = participant_i
      )
    })
  })

  loo_summary <- loo_all %>%
    mutate(has_warning = n_warning_messages > 0) %>%
    group_by(experiment, aggr_level) %>%
    summarise(
      n_holdout_fits = dplyr::n(),
      n_fit_failed = sum(fit_ok %in% FALSE, na.rm = TRUE),
      n_nonposdef_hessian = sum(fit_nonposdef_hessian %in% TRUE, na.rm = TRUE),
      n_nonzero_convergence = sum(fit_nonzero_convergence %in% TRUE, na.rm = TRUE),
      n_rank_deficient = sum(fit_rank_deficient %in% TRUE, na.rm = TRUE),
      n_fits_with_warning = sum(has_warning, na.rm = TRUE),
      total_warning_messages = sum(n_warning_messages, na.rm = TRUE),
      mean_rmse = mean(rmse, na.rm = TRUE),
      mean_mae = mean(mae, na.rm = TRUE),
      mean_bias = mean(bias, na.rm = TRUE),
      median_rmse = median(rmse, na.rm = TRUE),
      median_mae = median(mae, na.rm = TRUE),
      median_bias = median(bias, na.rm = TRUE),
      .groups = "drop"
    )

  problem_counts <- loo_all %>%
    count(aggr_level, problem_type, sort = TRUE)

  problem_fits_only <- loo_all %>%
    filter(
      problem_type != "none" |
        fit_ok %in% FALSE |
        fit_nonposdef_hessian %in% TRUE |
        fit_nonzero_convergence %in% TRUE |
        fit_rank_deficient %in% TRUE |
        n_warning_messages > 0 |
        !is.na(prediction_error)
    ) %>%
    arrange(aggr_level, heldout_participant)

  list(
    loo_all = loo_all,
    loo_summary = loo_summary,
    problem_counts = problem_counts,
    problem_fits_only = problem_fits_only
  )
}

# =========================================================
# AUSFÜHREN
# =========================================================

res_encap_mt2_participantloo <- run_participantloo_encap_mt2(
  df = encap_mt2,
  exp_name = "encap_mt2",
  verbose = TRUE
)

# =========================================================
# OUTPUTS
# =========================================================

#round_df_4(res_encap_mt2_participantloo$loo_all)
#round_df_4(res_encap_mt2_participantloo$loo_summary)
#round_df_4(res_encap_mt2_participantloo$problem_counts)
#round_df_4(res_encap_mt2_participantloo$problem_fits_only)
knitr::kable(
 round_df_4(res_encap_mt2_participantloo$loo_summary),
  format = "html",
  table.attr = 'style="width:100%; table-layout: fixed;"'
)
```



*FRT: Full Data Fit Problems*
```{r}
library(dplyr)
library(tidyr)
library(purrr)
library(tibble)
library(stringr)
library(glmmTMB)
library(lme4)

# =========================================================
# HILFSFUNKTIONEN
# =========================================================

safe_plain_df <- function(df) {
  out <- as.data.frame(df, stringsAsFactors = FALSE)
  rownames(out) <- NULL
  out
}

round_df_4 <- function(df) {
  df %>%
    mutate(across(where(is.numeric), ~ round(.x, 4)))
}

collapse_unique <- function(x) {
  x <- unique(stats::na.omit(x))
  if (length(x) == 0) return(NA_character_)
  paste(x, collapse = " | ")
}

calc_fit_metrics <- function(obs, pred) {
  keep <- is.finite(obs) & is.finite(pred)
  obs  <- obs[keep]
  pred <- pred[keep]

  if (length(obs) == 0) {
    return(tibble(
      n_obs_metric = 0L,
      rmse_in_sample = NA_real_,
      mae_in_sample = NA_real_,
      bias_in_sample = NA_real_,
      cor_obs_pred = NA_real_
    ))
  }

  err <- pred - obs

  tibble(
    n_obs_metric = length(obs),
    rmse_in_sample = sqrt(mean(err^2)),
    mae_in_sample = mean(abs(err)),
    bias_in_sample = mean(err),
    cor_obs_pred = suppressWarnings(stats::cor(obs, pred))
  )
}

# =========================================================
# DATEN
# Erwartet:
# FRT, nLetters, Participant, Topic, AOI, aggr_level,
# Referent, Category
# =========================================================

prep_encap_mt2_fullfit_data <- function(df) {
  needed <- c(
    "FRT", "nLetters", "Participant", "Topic", "AOI", "aggr_level",
    "Referent", "Category"
  )

  missing_cols <- setdiff(needed, names(df))
  if (length(missing_cols) > 0) {
    stop("Fehlende Variablen: ", paste(missing_cols, collapse = ", "))
  }

  out <- df %>%
    filter(!is.na(FRT), FRT > 0) %>%
    filter(!is.na(nLetters)) %>%
    filter(!is.na(Participant), !is.na(Topic), !is.na(AOI), !is.na(aggr_level)) %>%
    filter(!is.na(Referent), !is.na(Category)) %>%
    mutate(
      Participant = factor(Participant),
      Topic = factor(Topic),
      AOI = factor(AOI),
      aggr_level = factor(aggr_level),
      Referent = factor(Referent),
      Category = factor(Category)
    )

  cluster_sizes <- out %>%
    distinct(aggr_level, AOI) %>%
    count(aggr_level, name = "n_aoi_in_cluster")

  list(
    data = safe_plain_df(out),
    cluster_sizes = cluster_sizes
  )
}

# =========================================================
# FORMELN PRO aggr_level
# Mehrere AOIs:
#   FRT ~ Referent*AOI + Category*AOI + nLetters + ...
# Eine AOI:
#   FRT ~ Referent + Category + nLetters + ...
# =========================================================

make_formula_cluster_encap_mt2 <- function(response = "FRT",
                                           n_aoi_in_cluster = 1L) {
  rhs <- if (n_aoi_in_cluster > 1) {
    paste(
      "Referent*AOI + Category*AOI +",
      "nLetters + (1 | Topic) + (1 | Participant)"
    )
  } else {
    paste(
      "Referent + Category +",
      "nLetters + (1 | Topic) + (1 | Participant)"
    )
  }

  as.formula(paste(response, "~", rhs))
}

# =========================================================
# DESIGN-CHECKS
# =========================================================

check_cells <- function(df, var, aoi = "AOI") {
  aoi_levels <- levels(df[[aoi]])
  var_levels <- levels(df[[var]])

  obs <- df %>%
    dplyr::count(.data[[aoi]], .data[[var]], name = "n")

  full_grid <- tidyr::expand_grid(
    AOI__tmp = aoi_levels,
    VAR__tmp = var_levels
  ) %>%
    rename(!!aoi := AOI__tmp, !!var := VAR__tmp)

  full_grid %>%
    left_join(obs, by = c(aoi, var)) %>%
    mutate(
      n = tidyr::replace_na(n, 0L),
      present = n > 0
    ) %>%
    arrange(.data[[aoi]], .data[[var]])
}

summarise_cell_check <- function(cell_tbl, var_name) {
  tibble(
    factor_name = var_name,
    n_cells_total = nrow(cell_tbl),
    n_cells_present = sum(cell_tbl$present),
    n_cells_missing = sum(!cell_tbl$present),
    missing_cells = if (sum(!cell_tbl$present) == 0) {
      NA_character_
    } else {
      paste0(
        cell_tbl %>%
          dplyr::filter(!present) %>%
          transmute(txt = paste0(AOI, " x ", .data[[var_name]])) %>%
          pull(txt),
        collapse = "; "
      )
    }
  )
}

within_aoi_variation <- function(df, var, aoi = "AOI") {
  df %>%
    group_by(.data[[aoi]]) %>%
    summarise(
      n_levels = n_distinct(.data[[var]]),
      .groups = "drop"
    ) %>%
    mutate(
      factor_name = var,
      has_variation = n_levels >= 2
    ) %>%
    rename(AOI = .data[[aoi]])
}

summarise_within_aoi_variation <- function(var_tbl, var_name) {
  tibble(
    factor_name = var_name,
    n_aoi_total = nrow(var_tbl),
    n_aoi_without_variation = sum(!var_tbl$has_variation),
    aoi_without_variation = if (sum(!var_tbl$has_variation) == 0) {
      NA_character_
    } else {
      paste(var_tbl$AOI[!var_tbl$has_variation], collapse = "; ")
    }
  )
}

inspect_design_structure_encap_mt2 <- function(df) {
  cell_referent <- check_cells(df, "Referent")
  cell_category <- check_cells(df, "Category")

  cell_summary <- bind_rows(
    summarise_cell_check(cell_referent, "Referent"),
    summarise_cell_check(cell_category, "Category")
  )

  var_referent <- within_aoi_variation(df, "Referent")
  var_category <- within_aoi_variation(df, "Category")

  variation_summary <- bind_rows(
    summarise_within_aoi_variation(var_referent, "Referent"),
    summarise_within_aoi_variation(var_category, "Category")
  )

  missing_cells_any <- any(cell_summary$n_cells_missing > 0)
  no_variation_any  <- any(variation_summary$n_aoi_without_variation > 0)

  tibble(
    design_missing_cells_any = missing_cells_any,
    design_no_variation_any = no_variation_any,
    design_missing_Referent_cells = cell_summary$missing_cells[cell_summary$factor_name == "Referent"],
    design_missing_Category_cells = cell_summary$missing_cells[cell_summary$factor_name == "Category"],
    design_aoi_without_Referent_variation = variation_summary$aoi_without_variation[variation_summary$factor_name == "Referent"],
    design_aoi_without_Category_variation = variation_summary$aoi_without_variation[variation_summary$factor_name == "Category"]
  )
}

# =========================================================
# DESIGNMATRIX-CHECK
# =========================================================

inspect_design_matrix_identifiability_encap_mt2 <- function(df, formula) {
  fixed_formula <- lme4::nobars(formula)
  mf_fixed <- model.frame(fixed_formula, data = droplevels(df), na.action = na.omit)
  X <- model.matrix(fixed_formula, data = mf_fixed)

  qrX <- qr(X)
  rankX <- qrX$rank
  pX <- ncol(X)
  rankdef <- rankX < pX

  redundant_cols <- if (rankdef) {
    pivot <- qrX$pivot
    redundant_idx <- pivot[seq.int(rankX + 1, pX)]
    colnames(X)[redundant_idx]
  } else {
    character(0)
  }

  tibble(
    X_rank = rankX,
    X_ncol = pX,
    fit_rank_deficient = rankdef,
    redundant_cols = collapse_unique(redundant_cols),
    n_distinct_AOI = dplyr::n_distinct(df$AOI),
    n_distinct_Referent = dplyr::n_distinct(df$Referent),
    n_distinct_Category = dplyr::n_distinct(df$Category),
    n_distinct_nLetters = dplyr::n_distinct(df$nLetters),
    min_nLetters = min(df$nLetters),
    max_nLetters = max(df$nLetters)
  )
}

# =========================================================
# GLMMTMB FIT MIT STATUS-CHECK
# =========================================================

fit_glmmtmb_capture_encap_mt2 <- function(df, formula) {
  warn_texts <- character()
  msg_texts  <- character()

  fit <- withCallingHandlers(
    tryCatch(
      glmmTMB::glmmTMB(
        formula = formula,
        data = droplevels(df),
        family = Gamma(link = "log")
      ),
      error = function(e) e
    ),
    warning = function(w) {
      warn_texts <<- c(warn_texts, conditionMessage(w))
      invokeRestart("muffleWarning")
    },
    message = function(m) {
      msg_texts <<- c(msg_texts, conditionMessage(m))
      invokeRestart("muffleMessage")
    }
  )

  if (inherits(fit, "error")) {
    return(list(
      fit_tbl = tibble(
        fit_ok = FALSE,
        fit_error = conditionMessage(fit),
        fit_warning_text = collapse_unique(warn_texts),
        fit_message_text = collapse_unique(msg_texts),
        n_warning_messages = length(unique(stats::na.omit(warn_texts))),
        n_messages = length(unique(stats::na.omit(msg_texts))),
        fit_nonposdef_hessian = NA,
        fit_nonzero_convergence = NA,
        aic = NA_real_,
        bic = NA_real_,
        logLik = NA_real_
      ),
      fit_obj = NULL
    ))
  }

  fit_nonposdef_hessian <- tryCatch(
    !isTRUE(fit$sdr$pdHess),
    error = function(e) NA
  )

  fit_nonzero_convergence <- tryCatch(
    {
      conv <- fit$fit$convergence
      if (is.null(conv)) NA else conv != 0
    },
    error = function(e) NA
  )

  list(
    fit_tbl = tibble(
      fit_ok = TRUE,
      fit_error = NA_character_,
      fit_warning_text = collapse_unique(warn_texts),
      fit_message_text = collapse_unique(msg_texts),
      n_warning_messages = length(unique(stats::na.omit(warn_texts))),
      n_messages = length(unique(stats::na.omit(msg_texts))),
      fit_nonposdef_hessian = fit_nonposdef_hessian,
      fit_nonzero_convergence = fit_nonzero_convergence,
      aic = tryCatch(as.numeric(AIC(fit)), error = function(e) NA_real_),
      bic = tryCatch(as.numeric(BIC(fit)), error = function(e) NA_real_),
      logLik = tryCatch(as.numeric(logLik(fit)), error = function(e) NA_real_)
    ),
    fit_obj = fit
  )
}

# =========================================================
# KLASSIFIKATION PROBLEMART
# =========================================================

classify_problem_encap_mt2 <- function(fit_rank_deficient,
                                       fit_warning_text,
                                       fit_message_text,
                                       fit_error,
                                       fit_nonposdef_hessian,
                                       fit_nonzero_convergence,
                                       design_missing_cells_any,
                                       design_no_variation_any) {
  txt <- paste(
    c(fit_warning_text, fit_message_text, fit_error),
    collapse = " | "
  )

  case_when(
    !is.na(fit_error) ~ "fit_error",
    isTRUE(fit_nonposdef_hessian) ~ "nonposdef_hessian",
    isTRUE(fit_nonzero_convergence) ~ "nonzero_convergence",
    isTRUE(fit_rank_deficient) & (design_missing_cells_any | design_no_variation_any) &
      str_detect(txt, regex("rank-deficient|dropping columns|dropp.*column", ignore_case = TRUE)) ~
      "design_driven_rank_deficiency_with_fit_signal",
    isTRUE(fit_rank_deficient) & (design_missing_cells_any | design_no_variation_any) ~
      "design_driven_rank_deficiency",
    isTRUE(fit_rank_deficient) &
      str_detect(txt, regex("rank-deficient|dropping columns|dropp.*column", ignore_case = TRUE)) ~
      "rank_deficient_design_and_fit_signal",
    isTRUE(fit_rank_deficient) ~ "rank_deficient_design",
    str_detect(txt, regex("rank-deficient|dropping columns|dropp.*column", ignore_case = TRUE)) ~
      "rank_deficient_warning_without_matrix_flag",
    str_detect(txt, regex("hessian|singular|not positive definite|non-positive-definite", ignore_case = TRUE)) ~
      "hessian_or_singularity_signal",
    str_detect(txt, regex("convergence|did not converge|false convergence|iteration limit", ignore_case = TRUE)) ~
      "convergence_signal",
    (!is.na(fit_warning_text) || !is.na(fit_message_text)) ~ "warning_or_message_only",
    TRUE ~ "none"
  )
}

# =========================================================
# EIN FULL-DATA-FIT PRO aggr_level
# =========================================================

fit_one_full_case_encap_mt2 <- function(df_aggr,
                                        formula,
                                        exp_name = "encap_mt2",
                                        aggr_i,
                                        n_aoi_in_cluster) {
  df_aggr <- droplevels(df_aggr)

  n_levels_aoi <- dplyr::n_distinct(df_aggr$AOI)
  n_levels_referent <- dplyr::n_distinct(df_aggr$Referent)
  n_levels_category <- dplyr::n_distinct(df_aggr$Category)
  n_levels_topic <- dplyr::n_distinct(df_aggr$Topic)
  n_levels_participant <- dplyr::n_distinct(df_aggr$Participant)

  if (n_levels_referent < 2) {
    return(
      tibble(
        experiment = exp_name,
        aggr_level = aggr_i,
        n_aoi_in_cluster = n_aoi_in_cluster,
        n_obs = nrow(df_aggr),
        n_topics = n_levels_topic,
        n_participants = n_levels_participant,
        formula = paste(deparse(formula), collapse = " "),
        X_rank = NA_real_,
        X_ncol = NA_real_,
        fit_rank_deficient = NA,
        redundant_cols = NA_character_,
        n_distinct_AOI = n_levels_aoi,
        n_distinct_Referent = n_levels_referent,
        n_distinct_Category = n_levels_category,
        n_distinct_nLetters = dplyr::n_distinct(df_aggr$nLetters),
        min_nLetters = min(df_aggr$nLetters),
        max_nLetters = max(df_aggr$nLetters),
        design_missing_cells_any = NA,
        design_no_variation_any = NA,
        design_missing_Referent_cells = NA_character_,
        design_missing_Category_cells = NA_character_,
        design_aoi_without_Referent_variation = NA_character_,
        design_aoi_without_Category_variation = NA_character_,
        fit_ok = FALSE,
        fit_error = "Referent has fewer than 2 levels in full data for this aggr_level",
        fit_warning_text = NA_character_,
        fit_message_text = NA_character_,
        n_warning_messages = 0L,
        n_messages = 0L,
        fit_nonposdef_hessian = NA,
        fit_nonzero_convergence = NA,
        aic = NA_real_,
        bic = NA_real_,
        logLik = NA_real_,
        n_obs_metric = NA_real_,
        rmse_in_sample = NA_real_,
        mae_in_sample = NA_real_,
        bias_in_sample = NA_real_,
        cor_obs_pred = NA_real_,
        problem_type = "insufficient_levels_Referent"
      )
    )
  }

  if (n_levels_category < 2) {
    return(
      tibble(
        experiment = exp_name,
        aggr_level = aggr_i,
        n_aoi_in_cluster = n_aoi_in_cluster,
        n_obs = nrow(df_aggr),
        n_topics = n_levels_topic,
        n_participants = n_levels_participant,
        formula = paste(deparse(formula), collapse = " "),
        X_rank = NA_real_,
        X_ncol = NA_real_,
        fit_rank_deficient = NA,
        redundant_cols = NA_character_,
        n_distinct_AOI = n_levels_aoi,
        n_distinct_Referent = n_levels_referent,
        n_distinct_Category = n_levels_category,
        n_distinct_nLetters = dplyr::n_distinct(df_aggr$nLetters),
        min_nLetters = min(df_aggr$nLetters),
        max_nLetters = max(df_aggr$nLetters),
        design_missing_cells_any = NA,
        design_no_variation_any = NA,
        design_missing_Referent_cells = NA_character_,
        design_missing_Category_cells = NA_character_,
        design_aoi_without_Referent_variation = NA_character_,
        design_aoi_without_Category_variation = NA_character_,
        fit_ok = FALSE,
        fit_error = "Category has fewer than 2 levels in full data for this aggr_level",
        fit_warning_text = NA_character_,
        fit_message_text = NA_character_,
        n_warning_messages = 0L,
        n_messages = 0L,
        fit_nonposdef_hessian = NA,
        fit_nonzero_convergence = NA,
        aic = NA_real_,
        bic = NA_real_,
        logLik = NA_real_,
        n_obs_metric = NA_real_,
        rmse_in_sample = NA_real_,
        mae_in_sample = NA_real_,
        bias_in_sample = NA_real_,
        cor_obs_pred = NA_real_,
        problem_type = "insufficient_levels_Category"
      )
    )
  }

  if (n_levels_aoi < 2 && n_aoi_in_cluster > 1) {
    return(
      tibble(
        experiment = exp_name,
        aggr_level = aggr_i,
        n_aoi_in_cluster = n_aoi_in_cluster,
        n_obs = nrow(df_aggr),
        n_topics = n_levels_topic,
        n_participants = n_levels_participant,
        formula = paste(deparse(formula), collapse = " "),
        X_rank = NA_real_,
        X_ncol = NA_real_,
        fit_rank_deficient = NA,
        redundant_cols = NA_character_,
        n_distinct_AOI = n_levels_aoi,
        n_distinct_Referent = n_levels_referent,
        n_distinct_Category = n_levels_category,
        n_distinct_nLetters = dplyr::n_distinct(df_aggr$nLetters),
        min_nLetters = min(df_aggr$nLetters),
        max_nLetters = max(df_aggr$nLetters),
        design_missing_cells_any = NA,
        design_no_variation_any = NA,
        design_missing_Referent_cells = NA_character_,
        design_missing_Category_cells = NA_character_,
        design_aoi_without_Referent_variation = NA_character_,
        design_aoi_without_Category_variation = NA_character_,
        fit_ok = FALSE,
        fit_error = "AOI has fewer than 2 levels although cluster is expected to contain multiple AOIs",
        fit_warning_text = NA_character_,
        fit_message_text = NA_character_,
        n_warning_messages = 0L,
        n_messages = 0L,
        fit_nonposdef_hessian = NA,
        fit_nonzero_convergence = NA,
        aic = NA_real_,
        bic = NA_real_,
        logLik = NA_real_,
        n_obs_metric = NA_real_,
        rmse_in_sample = NA_real_,
        mae_in_sample = NA_real_,
        bias_in_sample = NA_real_,
        cor_obs_pred = NA_real_,
        problem_type = "insufficient_levels_AOI"
      )
    )
  }

  design_structure <- inspect_design_structure_encap_mt2(df_aggr)

  design_info <- inspect_design_matrix_identifiability_encap_mt2(
    df = df_aggr,
    formula = formula
  )

  fit_res <- fit_glmmtmb_capture_encap_mt2(
    df = df_aggr,
    formula = formula
  )

  fit_info <- fit_res$fit_tbl
  fit_obj  <- fit_res$fit_obj

  fit_metrics <- if (!is.null(fit_obj)) {
    fitted_vals <- tryCatch(
      predict(fit_obj, type = "response", re.form = NULL),
      error = function(e) rep(NA_real_, nrow(df_aggr))
    )

    calc_fit_metrics(
      obs = df_aggr$FRT,
      pred = fitted_vals
    )
  } else {
    tibble(
      n_obs_metric = NA_real_,
      rmse_in_sample = NA_real_,
      mae_in_sample = NA_real_,
      bias_in_sample = NA_real_,
      cor_obs_pred = NA_real_
    )
  }

  tibble(
    experiment = exp_name,
    aggr_level = aggr_i,
    n_aoi_in_cluster = n_aoi_in_cluster,
    n_obs = nrow(df_aggr),
    n_topics = n_levels_topic,
    n_participants = n_levels_participant,
    formula = paste(deparse(formula), collapse = " ")
  ) %>%
    bind_cols(design_structure) %>%
    bind_cols(design_info) %>%
    bind_cols(fit_info) %>%
    bind_cols(fit_metrics) %>%
    mutate(
      problem_type = classify_problem_encap_mt2(
        fit_rank_deficient = fit_rank_deficient,
        fit_warning_text = fit_warning_text,
        fit_message_text = fit_message_text,
        fit_error = fit_error,
        fit_nonposdef_hessian = fit_nonposdef_hessian,
        fit_nonzero_convergence = fit_nonzero_convergence,
        design_missing_cells_any = design_missing_cells_any,
        design_no_variation_any = design_no_variation_any
      )
    )
}

# =========================================================
# FULL-DATA-FITS ÜBER aggr_level
# =========================================================

run_fullfits_encap_mt2 <- function(df,
                                   exp_name = "encap_mt2") {
  prep <- prep_encap_mt2_fullfit_data(df)
  dat <- prep$data
  cluster_sizes <- prep$cluster_sizes

  aggr_levels <- unique(as.character(dat$aggr_level))

  fullfit_all <- purrr::map_dfr(aggr_levels, function(aggr_i) {
    df_aggr <- dat %>%
      filter(as.character(aggr_level) == aggr_i)

    n_aoi_cluster <- cluster_sizes$n_aoi_in_cluster[
      match(aggr_i, as.character(cluster_sizes$aggr_level))
    ]

    formula_i <- make_formula_cluster_encap_mt2(
      response = "FRT",
      n_aoi_in_cluster = n_aoi_cluster
    )

    fit_one_full_case_encap_mt2(
      df_aggr = df_aggr,
      formula = formula_i,
      exp_name = exp_name,
      aggr_i = aggr_i,
      n_aoi_in_cluster = n_aoi_cluster
    )
  })

  problem_counts <- fullfit_all %>%
    count(problem_type, sort = TRUE)

  problem_fits_only <- fullfit_all %>%
    filter(
      problem_type != "none" |
        fit_ok %in% FALSE |
        fit_nonposdef_hessian %in% TRUE |
        fit_nonzero_convergence %in% TRUE |
        fit_rank_deficient %in% TRUE |
        n_warning_messages > 0
    ) %>%
    arrange(aggr_level)

  list(
    fullfit_all = fullfit_all,
    problem_counts = problem_counts,
    problem_fits_only = problem_fits_only
  )
}

# =========================================================
# AUSFÜHREN
# =========================================================

res_encap_mt2_fullfit <- run_fullfits_encap_mt2(
  df = encap_mt2,
  exp_name = "encap_mt2"
)

# =========================================================
# OUTPUTS
# =========================================================

round_df_4(res_encap_mt2_fullfit$fullfit_all)
round_df_4(res_encap_mt2_fullfit$problem_counts)
round_df_4(res_encap_mt2_fullfit$problem_fits_only)

knitr::kable(
 round_df_4(res_encap_mt2_fullfit$problem_counts),
  format = "html",
  table.attr = 'style="width:100%; table-layout: fixed;"'
)


```


*FRT: Topic LOO + Fit Problems*
```{r}
library(dplyr)
library(tidyr)
library(purrr)
library(tibble)
library(stringr)
library(glmmTMB)
library(lme4)

# =========================================================
# HILFSFUNKTIONEN
# =========================================================

safe_plain_df <- function(df) {
  out <- as.data.frame(df, stringsAsFactors = FALSE)
  rownames(out) <- NULL
  out
}

round_df_4 <- function(df) {
  df %>%
    mutate(across(where(is.numeric), ~ round(.x, 4)))
}

collapse_unique <- function(x) {
  x <- unique(stats::na.omit(x))
  if (length(x) == 0) return(NA_character_)
  paste(x, collapse = " | ")
}

calc_metrics <- function(obs, pred) {
  keep <- is.finite(obs) & is.finite(pred)
  obs  <- obs[keep]
  pred <- pred[keep]

  if (length(obs) == 0) {
    return(tibble(
      n_test = 0L,
      rmse   = NA_real_,
      mae    = NA_real_,
      bias   = NA_real_
    ))
  }

  err <- pred - obs

  tibble(
    n_test = length(obs),
    rmse   = sqrt(mean(err^2)),
    mae    = mean(abs(err)),
    bias   = mean(err)
  )
}

# =========================================================
# DATEN
# Erwartet:
# FRT, Referent, Category, AOI, nLetters,
# Participant, Topic, aggr_level
# =========================================================

prep_encap_mt2_topicloo_data <- function(df) {
  needed <- c(
    "FRT", "Referent", "Category", "AOI",
    "nLetters", "Participant", "Topic", "aggr_level"
  )

  missing_cols <- setdiff(needed, names(df))
  if (length(missing_cols) > 0) {
    stop("Fehlende Variablen: ", paste(missing_cols, collapse = ", "))
  }

  out <- df %>%
    filter(!is.na(FRT), FRT > 0) %>%
    filter(!is.na(Referent), !is.na(Category), !is.na(AOI), !is.na(nLetters)) %>%
    filter(!is.na(Participant), !is.na(Topic), !is.na(aggr_level)) %>%
    mutate(
      Referent = factor(Referent),
      Category = factor(Category),
      AOI = factor(AOI),
      Participant = factor(Participant),
      Topic = factor(Topic),
      aggr_level = factor(aggr_level)
    )

  cluster_sizes <- out %>%
    distinct(aggr_level, AOI) %>%
    count(aggr_level, name = "n_aoi_in_cluster")

  list(
    data = safe_plain_df(out),
    cluster_sizes = cluster_sizes
  )
}

# =========================================================
# FORMELN PRO CLUSTER
# - mehrere AOIs im Cluster:
#     FRT ~ Referent*AOI + Category*AOI + nLetters + ...
# - genau eine AOI im Cluster:
#     FRT ~ Referent + Category + nLetters + ...
# =========================================================

make_formula_cluster_encap_mt2 <- function(response = "FRT",
                                           n_aoi_in_cluster = 1L) {
  rhs <- if (n_aoi_in_cluster > 1) {
    "Referent*AOI + Category*AOI + nLetters + (1 | Topic) + (1 | Participant)"
  } else {
    "Referent + Category + nLetters + (1 | Topic) + (1 | Participant)"
  }

  as.formula(paste(response, "~", rhs))
}

# =========================================================
# DESIGNMATRIX-CHECK
# =========================================================

inspect_design_matrix_identifiability_encap_mt2 <- function(df,
                                                            formula,
                                                            n_aoi_in_cluster) {
  fixed_formula <- lme4::nobars(formula)
  mf_fixed <- model.frame(fixed_formula, data = droplevels(df), na.action = na.omit)
  X <- model.matrix(fixed_formula, data = mf_fixed)

  qrX <- qr(X)
  rankX <- qrX$rank
  pX <- ncol(X)
  rankdef <- rankX < pX

  redundant_cols <- if (rankdef) {
    pivot <- qrX$pivot
    redundant_idx <- pivot[seq.int(rankX + 1, pX)]
    colnames(X)[redundant_idx]
  } else {
    character(0)
  }

  if (n_aoi_in_cluster > 1) {
    cell_df <- df %>%
      group_by(AOI, Referent, Category, .drop = FALSE) %>%
      summarise(
        n_obs = dplyr::n(),
        n_distinct_nLetters = dplyr::n_distinct(nLetters),
        .groups = "drop"
      )
  } else {
    cell_df <- df %>%
      group_by(Referent, Category, .drop = FALSE) %>%
      summarise(
        n_obs = dplyr::n(),
        n_distinct_nLetters = dplyr::n_distinct(nLetters),
        .groups = "drop"
      )
  }

  tibble(
    X_rank = rankX,
    X_ncol = pX,
    fit_rank_deficient = rankdef,
    redundant_cols = collapse_unique(redundant_cols),
    n_distinct_nLetters = dplyr::n_distinct(df$nLetters),
    min_nLetters = min(df$nLetters),
    max_nLetters = max(df$nLetters),
    n_design_cells = nrow(cell_df),
    n_empty_design_cells = sum(cell_df$n_obs == 0),
    n_cells_nLetters_constant = sum(cell_df$n_distinct_nLetters <= 1)
  )
}

# =========================================================
# GLMMTMB FIT MIT STATUS-CHECK
# =========================================================

fit_glmmtmb_capture_encap_mt2 <- function(df,
                                          formula) {
  warn_texts <- character()
  msg_texts  <- character()

  fit <- withCallingHandlers(
    tryCatch(
      glmmTMB::glmmTMB(
        formula = formula,
        data = droplevels(df),
        family = Gamma(link = "log")
      ),
      error = function(e) e
    ),
    warning = function(w) {
      warn_texts <<- c(warn_texts, conditionMessage(w))
      invokeRestart("muffleWarning")
    },
    message = function(m) {
      msg_texts <<- c(msg_texts, conditionMessage(m))
      invokeRestart("muffleMessage")
    }
  )

  if (inherits(fit, "error")) {
    return(list(
      fit_tbl = tibble(
        fit_ok = FALSE,
        fit_error = conditionMessage(fit),
        fit_warning_text = collapse_unique(warn_texts),
        fit_message_text = collapse_unique(msg_texts),
        n_warning_messages = length(unique(stats::na.omit(warn_texts))),
        n_messages = length(unique(stats::na.omit(msg_texts))),
        fit_nonposdef_hessian = NA,
        fit_nonzero_convergence = NA,
        aic = NA_real_,
        bic = NA_real_,
        logLik = NA_real_
      ),
      fit_obj = NULL
    ))
  }

  fit_nonposdef_hessian <- tryCatch(
    !isTRUE(fit$sdr$pdHess),
    error = function(e) NA
  )

  fit_nonzero_convergence <- tryCatch(
    {
      conv <- fit$fit$convergence
      if (is.null(conv)) NA else conv != 0
    },
    error = function(e) NA
  )

  list(
    fit_tbl = tibble(
      fit_ok = TRUE,
      fit_error = NA_character_,
      fit_warning_text = collapse_unique(warn_texts),
      fit_message_text = collapse_unique(msg_texts),
      n_warning_messages = length(unique(stats::na.omit(warn_texts))),
      n_messages = length(unique(stats::na.omit(msg_texts))),
      fit_nonposdef_hessian = fit_nonposdef_hessian,
      fit_nonzero_convergence = fit_nonzero_convergence,
      aic = tryCatch(as.numeric(AIC(fit)), error = function(e) NA_real_),
      bic = tryCatch(as.numeric(BIC(fit)), error = function(e) NA_real_),
      logLik = tryCatch(as.numeric(logLik(fit)), error = function(e) NA_real_)
    ),
    fit_obj = fit
  )
}

# =========================================================
# KLASSIFIKATION PROBLEMART
# =========================================================

classify_problem_encap_mt2 <- function(fit_rank_deficient,
                                       fit_warning_text,
                                       fit_message_text,
                                       fit_error,
                                       fit_nonposdef_hessian,
                                       fit_nonzero_convergence,
                                       prediction_error) {
  txt <- paste(
    c(fit_warning_text, fit_message_text, fit_error, prediction_error),
    collapse = " | "
  )

  case_when(
    !is.na(fit_error) ~ "fit_error",
    !is.na(prediction_error) ~ "prediction_error",
    isTRUE(fit_nonposdef_hessian) ~ "nonposdef_hessian",
    isTRUE(fit_nonzero_convergence) ~ "nonzero_convergence",
    isTRUE(fit_rank_deficient) &
      str_detect(txt, regex("rank-deficient|dropping columns|dropp.*column", ignore_case = TRUE)) ~
      "rank_deficient_design_and_fit_signal",
    isTRUE(fit_rank_deficient) ~ "rank_deficient_design",
    str_detect(txt, regex("rank-deficient|dropping columns|dropp.*column", ignore_case = TRUE)) ~
      "rank_deficient_warning_without_matrix_flag",
    str_detect(txt, regex("hessian|singular|not positive definite|non-positive-definite", ignore_case = TRUE)) ~
      "hessian_or_singularity_signal",
    str_detect(txt, regex("convergence|did not converge|false convergence|iteration limit", ignore_case = TRUE)) ~
      "convergence_signal",
    (!is.na(fit_warning_text) || !is.na(fit_message_text)) ~ "warning_or_message_only",
    TRUE ~ "none"
  )
}

# =========================================================
# EIN TOPIC-LOO-FIT
# =========================================================

fit_one_topicloo_case_encap_mt2 <- function(train_df,
                                            test_df,
                                            formula,
                                            n_aoi_in_cluster,
                                            exp_name = "encap_mt2",
                                            aggr_i,
                                            topic_i) {
  train_df <- droplevels(train_df)
  test_df  <- droplevels(test_df)

  design_info <- inspect_design_matrix_identifiability_encap_mt2(
    df = train_df,
    formula = formula,
    n_aoi_in_cluster = n_aoi_in_cluster
  )

  fit_res <- fit_glmmtmb_capture_encap_mt2(
    df = train_df,
    formula = formula
  )

  fit_info <- fit_res$fit_tbl
  fit_obj  <- fit_res$fit_obj

  pred_error <- NA_character_
  pred <- rep(NA_real_, nrow(test_df))

  if (!is.null(fit_obj)) {
    pred <- tryCatch(
      predict(
        fit_obj,
        newdata = test_df,
        type = "response",
        re.form = NULL,
        allow.new.levels = TRUE
      ),
      error = function(e) {
        pred_error <<- conditionMessage(e)
        rep(NA_real_, nrow(test_df))
      }
    )
  }

  metric_info <- calc_metrics(
    obs = test_df$FRT,
    pred = pred
  )

  tibble(
    experiment = exp_name,
    aggr_level = aggr_i,
    heldout_topic = topic_i,
    n_aoi_in_cluster = n_aoi_in_cluster,
    cluster_formula_type = ifelse(n_aoi_in_cluster > 1, "Referent_Category_AOI", "Referent_Category_only"),
    n_train = nrow(train_df),
    n_test_rows = nrow(test_df),
    formula = paste(deparse(formula), collapse = " ")
  ) %>%
    bind_cols(design_info) %>%
    bind_cols(fit_info) %>%
    bind_cols(metric_info) %>%
    mutate(
      prediction_error = pred_error,
      problem_type = classify_problem_encap_mt2(
        fit_rank_deficient = fit_rank_deficient,
        fit_warning_text = fit_warning_text,
        fit_message_text = fit_message_text,
        fit_error = fit_error,
        fit_nonposdef_hessian = fit_nonposdef_hessian,
        fit_nonzero_convergence = fit_nonzero_convergence,
        prediction_error = prediction_error
      )
    )
}

# =========================================================
# TOPIC-LOO FÜR encap_mt2
# =========================================================

run_topicloo_encap_mt2 <- function(df,
                                   exp_name = "encap_mt2",
                                   verbose = TRUE) {
  prep <- prep_encap_mt2_topicloo_data(df)
  dat <- prep$data
  cluster_sizes <- prep$cluster_sizes

  aggr_levels <- unique(as.character(dat$aggr_level))

  loo_all <- purrr::map_dfr(aggr_levels, function(aggr_i) {
    dat_cluster <- dat %>%
      filter(as.character(aggr_level) == aggr_i)

    n_aoi_cluster <- cluster_sizes$n_aoi_in_cluster[
      match(aggr_i, cluster_sizes$aggr_level)
    ]

    formula_i <- make_formula_cluster_encap_mt2(
      response = "FRT",
      n_aoi_in_cluster = n_aoi_cluster
    )

    topics_cluster <- unique(as.character(dat_cluster$Topic))

    purrr::map_dfr(topics_cluster, function(topic_i) {
      train_df <- dat_cluster %>%
        filter(as.character(Topic) != topic_i)

      test_df <- dat_cluster %>%
        filter(as.character(Topic) == topic_i)

      if (isTRUE(verbose)) {
        message(
          "[Topic-LOO] exp=", exp_name,
          " aggr_level=", aggr_i,
          " holdout_topic=", topic_i,
          " n_train=", nrow(train_df)
        )
      }

      fit_one_topicloo_case_encap_mt2(
        train_df = train_df,
        test_df = test_df,
        formula = formula_i,
        n_aoi_in_cluster = n_aoi_cluster,
        exp_name = exp_name,
        aggr_i = aggr_i,
        topic_i = topic_i
      )
    })
  })

  loo_summary <- loo_all %>%
    mutate(has_warning = n_warning_messages > 0) %>%
    group_by(experiment, aggr_level) %>%
    summarise(
      n_holdout_fits = dplyr::n(),
      n_fit_failed = sum(fit_ok %in% FALSE, na.rm = TRUE),
      n_nonposdef_hessian = sum(fit_nonposdef_hessian %in% TRUE, na.rm = TRUE),
      n_nonzero_convergence = sum(fit_nonzero_convergence %in% TRUE, na.rm = TRUE),
      n_rank_deficient = sum(fit_rank_deficient %in% TRUE, na.rm = TRUE),
      n_fits_with_warning = sum(has_warning, na.rm = TRUE),
      total_warning_messages = sum(n_warning_messages, na.rm = TRUE),
      mean_rmse = mean(rmse, na.rm = TRUE),
      mean_mae = mean(mae, na.rm = TRUE),
      mean_bias = mean(bias, na.rm = TRUE),
      median_rmse = median(rmse, na.rm = TRUE),
      median_mae = median(mae, na.rm = TRUE),
      median_bias = median(bias, na.rm = TRUE),
      .groups = "drop"
    )

  problem_counts <- loo_all %>%
    count(aggr_level, problem_type, sort = TRUE)

  problem_fits_only <- loo_all %>%
    filter(
      problem_type != "none" |
        fit_ok %in% FALSE |
        fit_nonposdef_hessian %in% TRUE |
        fit_nonzero_convergence %in% TRUE |
        fit_rank_deficient %in% TRUE |
        n_warning_messages > 0 |
        !is.na(prediction_error)
    ) %>%
    arrange(aggr_level, heldout_topic)

  list(
    loo_all = loo_all,
    loo_summary = loo_summary,
    problem_counts = problem_counts,
    problem_fits_only = problem_fits_only
  )
}

# =========================================================
# AUSFÜHREN
# =========================================================

res_encap_mt2_topicloo <- run_topicloo_encap_mt2(
  df = encap_mt2,
  exp_name = "encap_mt2",
  verbose = TRUE
)

# =========================================================
# OUTPUTS
# =========================================================

#round_df_4(res_encap_mt2_topicloo$loo_all)
#round_df_4(res_encap_mt2_topicloo$loo_summary)
#round_df_4(res_encap_mt2_topicloo$problem_counts)
#round_df_4(res_encap_mt2_topicloo$problem_fits_only)

knitr::kable(
 round_df_4(res_encap_mt2_topicloo$loo_summary),
  format = "html",
  table.attr = 'style="width:100%; table-layout: fixed;"'
)
```



*FRT: Participant LOO +  Fit Problems*
```{r}
library(dplyr)
library(tidyr)
library(purrr)
library(tibble)
library(stringr)
library(glmmTMB)
library(lme4)

# =========================================================
# HILFSFUNKTIONEN
# =========================================================

safe_plain_df <- function(df) {
  out <- as.data.frame(df, stringsAsFactors = FALSE)
  rownames(out) <- NULL
  out
}

round_df_4 <- function(df) {
  df %>%
    mutate(across(where(is.numeric), ~ round(.x, 4)))
}

collapse_unique <- function(x) {
  x <- unique(stats::na.omit(x))
  if (length(x) == 0) return(NA_character_)
  paste(x, collapse = " | ")
}

calc_metrics <- function(obs, pred) {
  keep <- is.finite(obs) & is.finite(pred)
  obs  <- obs[keep]
  pred <- pred[keep]

  if (length(obs) == 0) {
    return(tibble(
      n_test = 0L,
      rmse   = NA_real_,
      mae    = NA_real_,
      bias   = NA_real_
    ))
  }

  err <- pred - obs

  tibble(
    n_test = length(obs),
    rmse   = sqrt(mean(err^2)),
    mae    = mean(abs(err)),
    bias   = mean(err)
  )
}

# =========================================================
# DATEN
# Erwartet:
# FRT, Referent, Category, AOI, nLetters,
# Participant, Topic, aggr_level
# =========================================================

prep_encap_mt2_participantloo_data <- function(df) {
  needed <- c(
    "FRT", "Referent", "Category", "AOI",
    "nLetters", "Participant", "Topic", "aggr_level"
  )

  missing_cols <- setdiff(needed, names(df))
  if (length(missing_cols) > 0) {
    stop("Fehlende Variablen: ", paste(missing_cols, collapse = ", "))
  }

  out <- df %>%
    filter(!is.na(FRT), FRT > 0) %>%
    filter(!is.na(Referent), !is.na(Category), !is.na(AOI), !is.na(nLetters)) %>%
    filter(!is.na(Participant), !is.na(Topic), !is.na(aggr_level)) %>%
    mutate(
      Referent = factor(Referent),
      Category = factor(Category),
      AOI = factor(AOI),
      Participant = factor(Participant),
      Topic = factor(Topic),
      aggr_level = factor(aggr_level)
    )

  cluster_sizes <- out %>%
    distinct(aggr_level, AOI) %>%
    count(aggr_level, name = "n_aoi_in_cluster")

  list(
    data = safe_plain_df(out),
    cluster_sizes = cluster_sizes
  )
}

# =========================================================
# FORMELN PRO CLUSTER
# - mehrere AOIs im Cluster:
#     FRT ~ Referent*AOI + Category*AOI + nLetters + ...
# - genau eine AOI im Cluster:
#     FRT ~ Referent + Category + nLetters + ...
# =========================================================

make_formula_cluster_encap_mt2 <- function(response = "FRT",
                                           n_aoi_in_cluster = 1L) {
  rhs <- if (n_aoi_in_cluster > 1) {
    "Referent*AOI + Category*AOI + nLetters + (1 | Topic) + (1 | Participant)"
  } else {
    "Referent + Category + nLetters + (1 | Topic) + (1 | Participant)"
  }

  as.formula(paste(response, "~", rhs))
}

# =========================================================
# DESIGNMATRIX-CHECK
# =========================================================

inspect_design_matrix_identifiability_encap_mt2 <- function(df,
                                                            formula,
                                                            n_aoi_in_cluster) {
  fixed_formula <- lme4::nobars(formula)
  mf_fixed <- model.frame(fixed_formula, data = droplevels(df), na.action = na.omit)
  X <- model.matrix(fixed_formula, data = mf_fixed)

  qrX <- qr(X)
  rankX <- qrX$rank
  pX <- ncol(X)
  rankdef <- rankX < pX

  redundant_cols <- if (rankdef) {
    pivot <- qrX$pivot
    redundant_idx <- pivot[seq.int(rankX + 1, pX)]
    colnames(X)[redundant_idx]
  } else {
    character(0)
  }

  if (n_aoi_in_cluster > 1) {
    cell_df <- df %>%
      group_by(AOI, Referent, Category, .drop = FALSE) %>%
      summarise(
        n_obs = dplyr::n(),
        n_distinct_nLetters = dplyr::n_distinct(nLetters),
        .groups = "drop"
      )
  } else {
    cell_df <- df %>%
      group_by(Referent, Category, .drop = FALSE) %>%
      summarise(
        n_obs = dplyr::n(),
        n_distinct_nLetters = dplyr::n_distinct(nLetters),
        .groups = "drop"
      )
  }

  tibble(
    X_rank = rankX,
    X_ncol = pX,
    fit_rank_deficient = rankdef,
    redundant_cols = collapse_unique(redundant_cols),
    n_distinct_nLetters = dplyr::n_distinct(df$nLetters),
    min_nLetters = min(df$nLetters),
    max_nLetters = max(df$nLetters),
    n_design_cells = nrow(cell_df),
    n_empty_design_cells = sum(cell_df$n_obs == 0),
    n_cells_nLetters_constant = sum(cell_df$n_distinct_nLetters <= 1)
  )
}

# =========================================================
# GLMMTMB FIT MIT STATUS-CHECK
# =========================================================

fit_glmmtmb_capture_encap_mt2 <- function(df,
                                          formula) {
  warn_texts <- character()
  msg_texts  <- character()

  fit <- withCallingHandlers(
    tryCatch(
      glmmTMB::glmmTMB(
        formula = formula,
        data = droplevels(df),
        family = Gamma(link = "log")
      ),
      error = function(e) e
    ),
    warning = function(w) {
      warn_texts <<- c(warn_texts, conditionMessage(w))
      invokeRestart("muffleWarning")
    },
    message = function(m) {
      msg_texts <<- c(msg_texts, conditionMessage(m))
      invokeRestart("muffleMessage")
    }
  )

  if (inherits(fit, "error")) {
    return(list(
      fit_tbl = tibble(
        fit_ok = FALSE,
        fit_error = conditionMessage(fit),
        fit_warning_text = collapse_unique(warn_texts),
        fit_message_text = collapse_unique(msg_texts),
        n_warning_messages = length(unique(stats::na.omit(warn_texts))),
        n_messages = length(unique(stats::na.omit(msg_texts))),
        fit_nonposdef_hessian = NA,
        fit_nonzero_convergence = NA,
        aic = NA_real_,
        bic = NA_real_,
        logLik = NA_real_
      ),
      fit_obj = NULL
    ))
  }

  fit_nonposdef_hessian <- tryCatch(
    !isTRUE(fit$sdr$pdHess),
    error = function(e) NA
  )

  fit_nonzero_convergence <- tryCatch(
    {
      conv <- fit$fit$convergence
      if (is.null(conv)) NA else conv != 0
    },
    error = function(e) NA
  )

  list(
    fit_tbl = tibble(
      fit_ok = TRUE,
      fit_error = NA_character_,
      fit_warning_text = collapse_unique(warn_texts),
      fit_message_text = collapse_unique(msg_texts),
      n_warning_messages = length(unique(stats::na.omit(warn_texts))),
      n_messages = length(unique(stats::na.omit(msg_texts))),
      fit_nonposdef_hessian = fit_nonposdef_hessian,
      fit_nonzero_convergence = fit_nonzero_convergence,
      aic = tryCatch(as.numeric(AIC(fit)), error = function(e) NA_real_),
      bic = tryCatch(as.numeric(BIC(fit)), error = function(e) NA_real_),
      logLik = tryCatch(as.numeric(logLik(fit)), error = function(e) NA_real_)
    ),
    fit_obj = fit
  )
}

# =========================================================
# KLASSIFIKATION PROBLEMART
# =========================================================

classify_problem_encap_mt2 <- function(fit_rank_deficient,
                                       fit_warning_text,
                                       fit_message_text,
                                       fit_error,
                                       fit_nonposdef_hessian,
                                       fit_nonzero_convergence,
                                       prediction_error) {
  txt <- paste(
    c(fit_warning_text, fit_message_text, fit_error, prediction_error),
    collapse = " | "
  )

  case_when(
    !is.na(fit_error) ~ "fit_error",
    !is.na(prediction_error) ~ "prediction_error",
    isTRUE(fit_nonposdef_hessian) ~ "nonposdef_hessian",
    isTRUE(fit_nonzero_convergence) ~ "nonzero_convergence",
    isTRUE(fit_rank_deficient) &
      str_detect(txt, regex("rank-deficient|dropping columns|dropp.*column", ignore_case = TRUE)) ~
      "rank_deficient_design_and_fit_signal",
    isTRUE(fit_rank_deficient) ~ "rank_deficient_design",
    str_detect(txt, regex("rank-deficient|dropping columns|dropp.*column", ignore_case = TRUE)) ~
      "rank_deficient_warning_without_matrix_flag",
    str_detect(txt, regex("hessian|singular|not positive definite|non-positive-definite", ignore_case = TRUE)) ~
      "hessian_or_singularity_signal",
    str_detect(txt, regex("convergence|did not converge|false convergence|iteration limit", ignore_case = TRUE)) ~
      "convergence_signal",
    (!is.na(fit_warning_text) || !is.na(fit_message_text)) ~ "warning_or_message_only",
    TRUE ~ "none"
  )
}

# =========================================================
# EIN PARTICIPANT-LOO-FIT
# =========================================================

fit_one_participantloo_case_encap_mt2 <- function(train_df,
                                                  test_df,
                                                  formula,
                                                  n_aoi_in_cluster,
                                                  exp_name = "encap_mt2",
                                                  aggr_i,
                                                  participant_i) {
  train_df <- droplevels(train_df)
  test_df  <- droplevels(test_df)

  design_info <- inspect_design_matrix_identifiability_encap_mt2(
    df = train_df,
    formula = formula,
    n_aoi_in_cluster = n_aoi_in_cluster
  )

  fit_res <- fit_glmmtmb_capture_encap_mt2(
    df = train_df,
    formula = formula
  )

  fit_info <- fit_res$fit_tbl
  fit_obj  <- fit_res$fit_obj

  pred_error <- NA_character_
  pred <- rep(NA_real_, nrow(test_df))

  if (!is.null(fit_obj)) {
    pred <- tryCatch(
      predict(
        fit_obj,
        newdata = test_df,
        type = "response",
        re.form = NULL,
        allow.new.levels = TRUE
      ),
      error = function(e) {
        pred_error <<- conditionMessage(e)
        rep(NA_real_, nrow(test_df))
      }
    )
  }

  metric_info <- calc_metrics(
    obs = test_df$FRT,
    pred = pred
  )

  tibble(
    experiment = exp_name,
    aggr_level = aggr_i,
    heldout_participant = participant_i,
    n_aoi_in_cluster = n_aoi_in_cluster,
    cluster_formula_type = ifelse(n_aoi_in_cluster > 1, "Referent_Category_AOI", "Referent_Category_only"),
    n_train = nrow(train_df),
    n_test_rows = nrow(test_df),
    formula = paste(deparse(formula), collapse = " ")
  ) %>%
    bind_cols(design_info) %>%
    bind_cols(fit_info) %>%
    bind_cols(metric_info) %>%
    mutate(
      prediction_error = pred_error,
      problem_type = classify_problem_encap_mt2(
        fit_rank_deficient = fit_rank_deficient,
        fit_warning_text = fit_warning_text,
        fit_message_text = fit_message_text,
        fit_error = fit_error,
        fit_nonposdef_hessian = fit_nonposdef_hessian,
        fit_nonzero_convergence = fit_nonzero_convergence,
        prediction_error = prediction_error
      )
    )
}

# =========================================================
# PARTICIPANT-LOO FÜR encap_mt2
# =========================================================

run_participantloo_encap_mt2 <- function(df,
                                         exp_name = "encap_mt2",
                                         verbose = TRUE) {
  prep <- prep_encap_mt2_participantloo_data(df)
  dat <- prep$data
  cluster_sizes <- prep$cluster_sizes

  aggr_levels <- unique(as.character(dat$aggr_level))

  loo_all <- purrr::map_dfr(aggr_levels, function(aggr_i) {
    dat_cluster <- dat %>%
      filter(as.character(aggr_level) == aggr_i)

    n_aoi_cluster <- cluster_sizes$n_aoi_in_cluster[
      match(aggr_i, cluster_sizes$aggr_level)
    ]

    formula_i <- make_formula_cluster_encap_mt2(
      response = "FRT",
      n_aoi_in_cluster = n_aoi_cluster
    )

    participants_cluster <- unique(as.character(dat_cluster$Participant))

    purrr::map_dfr(participants_cluster, function(participant_i) {
      train_df <- dat_cluster %>%
        filter(as.character(Participant) != participant_i)

      test_df <- dat_cluster %>%
        filter(as.character(Participant) == participant_i)

      if (isTRUE(verbose)) {
        message(
          "[Participant-LOO] exp=", exp_name,
          " aggr_level=", aggr_i,
          " holdout_participant=", participant_i,
          " n_train=", nrow(train_df)
        )
      }

      fit_one_participantloo_case_encap_mt2(
        train_df = train_df,
        test_df = test_df,
        formula = formula_i,
        n_aoi_in_cluster = n_aoi_cluster,
        exp_name = exp_name,
        aggr_i = aggr_i,
        participant_i = participant_i
      )
    })
  })

  loo_summary <- loo_all %>%
    mutate(has_warning = n_warning_messages > 0) %>%
    group_by(experiment, aggr_level) %>%
    summarise(
      n_holdout_fits = dplyr::n(),
      n_fit_failed = sum(fit_ok %in% FALSE, na.rm = TRUE),
      n_nonposdef_hessian = sum(fit_nonposdef_hessian %in% TRUE, na.rm = TRUE),
      n_nonzero_convergence = sum(fit_nonzero_convergence %in% TRUE, na.rm = TRUE),
      n_rank_deficient = sum(fit_rank_deficient %in% TRUE, na.rm = TRUE),
      n_fits_with_warning = sum(has_warning, na.rm = TRUE),
      total_warning_messages = sum(n_warning_messages, na.rm = TRUE),
      mean_rmse = mean(rmse, na.rm = TRUE),
      mean_mae = mean(mae, na.rm = TRUE),
      mean_bias = mean(bias, na.rm = TRUE),
      median_rmse = median(rmse, na.rm = TRUE),
      median_mae = median(mae, na.rm = TRUE),
      median_bias = median(bias, na.rm = TRUE),
      .groups = "drop"
    )

  problem_counts <- loo_all %>%
    count(aggr_level, problem_type, sort = TRUE)

  problem_fits_only <- loo_all %>%
    filter(
      problem_type != "none" |
        fit_ok %in% FALSE |
        fit_nonposdef_hessian %in% TRUE |
        fit_nonzero_convergence %in% TRUE |
        fit_rank_deficient %in% TRUE |
        n_warning_messages > 0 |
        !is.na(prediction_error)
    ) %>%
    arrange(aggr_level, heldout_participant)

  list(
    loo_all = loo_all,
    loo_summary = loo_summary,
    problem_counts = problem_counts,
    problem_fits_only = problem_fits_only
  )
}

# =========================================================
# AUSFÜHREN
# =========================================================

res_encap_mt2_participantloo <- run_participantloo_encap_mt2(
  df = encap_mt2,
  exp_name = "encap_mt2",
  verbose = TRUE
)

# =========================================================
# OUTPUTS
# =========================================================

#round_df_4(res_encap_mt2_participantloo$loo_all)
#round_df_4(res_encap_mt2_participantloo$loo_summary)
#round_df_4(res_encap_mt2_participantloo$problem_counts)
#round_df_4(res_encap_mt2_participantloo$problem_fits_only)

knitr::kable(
 round_df_4(res_encap_mt2_participantloo$loo_summary),
  format = "html",
  table.attr = 'style="width:100%; table-layout: fixed;"'
)
```




*RRT: Full Data Fit Problems*
```{r}
library(dplyr)
library(tidyr)
library(purrr)
library(tibble)
library(stringr)
library(glmmTMB)
library(lme4)

# =========================================================
# HILFSFUNKTIONEN
# =========================================================

safe_plain_df <- function(df) {
  out <- as.data.frame(df, stringsAsFactors = FALSE)
  rownames(out) <- NULL
  out
}

round_df_4 <- function(df) {
  df %>%
    mutate(across(where(is.numeric), ~ round(.x, 4)))
}

collapse_unique <- function(x) {
  x <- unique(stats::na.omit(x))
  if (length(x) == 0) return(NA_character_)
  paste(x, collapse = " | ")
}

calc_fit_metrics <- function(obs, pred) {
  keep <- is.finite(obs) & is.finite(pred)
  obs  <- obs[keep]
  pred <- pred[keep]

  if (length(obs) == 0) {
    return(tibble(
      n_obs_metric = 0L,
      rmse_in_sample = NA_real_,
      mae_in_sample = NA_real_,
      bias_in_sample = NA_real_,
      cor_obs_pred = NA_real_
    ))
  }

  err <- pred - obs

  tibble(
    n_obs_metric = length(obs),
    rmse_in_sample = sqrt(mean(err^2)),
    mae_in_sample = mean(abs(err)),
    bias_in_sample = mean(err),
    cor_obs_pred = suppressWarnings(stats::cor(obs, pred))
  )
}

# =========================================================
# HEURISTIK FÜR SINGULAR RANDOM EFFECTS IN glmmTMB
# =========================================================

extract_re_variances_glmmtmb <- function(fit_obj) {
  vc <- tryCatch(VarCorr(fit_obj), error = function(e) NULL)
  if (is.null(vc) || is.null(vc$cond)) return(numeric(0))

  vars <- c()

  for (grp in names(vc$cond)) {
    mat <- vc$cond[[grp]]
    sd_attr <- attr(mat, "stddev")

    if (!is.null(sd_attr) && length(sd_attr) > 0) {
      vars <- c(vars, as.numeric(sd_attr)^2)
    } else if (is.matrix(mat) && nrow(mat) > 0) {
      vars <- c(vars, diag(as.matrix(mat)))
    }
  }

  vars
}

check_singular_re_glmmtmb <- function(fit_obj, tol = 1e-8) {
  re_vars <- extract_re_variances_glmmtmb(fit_obj)

  if (length(re_vars) == 0) {
    return(tibble(
      fit_singular_re = NA,
      n_random_effect_vars = 0L,
      n_near_zero_random_effect_vars = NA_integer_,
      min_random_effect_var = NA_real_
    ))
  }

  tibble(
    fit_singular_re = any(re_vars < tol),
    n_random_effect_vars = length(re_vars),
    n_near_zero_random_effect_vars = sum(re_vars < tol),
    min_random_effect_var = min(re_vars)
  )
}

# =========================================================
# DATEN
# Erwartet:
# RRT, nLetters, Participant, Topic, AOI, aggr_level,
# Referent, Category
# =========================================================

prep_encap_mt2_fullfit_data_rrt <- function(df) {
  needed <- c(
    "RRT", "nLetters", "Participant", "Topic", "AOI", "aggr_level",
    "Referent", "Category"
  )

  missing_cols <- setdiff(needed, names(df))
  if (length(missing_cols) > 0) {
    stop("Fehlende Variablen: ", paste(missing_cols, collapse = ", "))
  }

  out <- df %>%
    filter(!is.na(RRT), RRT >= 0) %>%
    filter(!is.na(nLetters)) %>%
    filter(!is.na(Participant), !is.na(Topic), !is.na(AOI), !is.na(aggr_level)) %>%
    filter(!is.na(Referent), !is.na(Category)) %>%
    mutate(
      Participant = factor(Participant),
      Topic = factor(Topic),
      AOI = factor(AOI),
      aggr_level = factor(aggr_level),
      Referent = factor(Referent),
      Category = factor(Category)
    )

  cluster_sizes <- out %>%
    distinct(aggr_level, AOI) %>%
    count(aggr_level, name = "n_aoi_in_cluster")

  list(
    data = safe_plain_df(out),
    cluster_sizes = cluster_sizes
  )
}

# =========================================================
# FORMELN PRO aggr_level
# Mehrere AOIs:
#   RRT ~ Referent*AOI + Category*AOI + nLetters + ...
#   ziformula = ~ Referent*AOI + Category*AOI + nLetters
# Eine AOI:
#   RRT ~ Referent + Category + nLetters + ...
#   ziformula = ~ Referent + Category + nLetters
# =========================================================

make_formula_cluster_encap_mt2_rrt <- function(response = "RRT",
                                               n_aoi_in_cluster = 1L) {
  rhs <- if (n_aoi_in_cluster > 1) {
    paste(
      "Referent*AOI + Category*AOI +",
      "nLetters + (1 | Topic) + (1 | Participant)"
    )
  } else {
    paste(
      "Referent + Category +",
      "nLetters + (1 | Topic) + (1 | Participant)"
    )
  }

  as.formula(paste(response, "~", rhs))
}

make_ziformula_cluster_encap_mt2_rrt <- function(n_aoi_in_cluster = 1L) {
  rhs <- if (n_aoi_in_cluster > 1) {
    "Referent*AOI + Category*AOI + nLetters"
  } else {
    "Referent + Category + nLetters"
  }

  as.formula(paste("~", rhs))
}

# =========================================================
# DESIGN-CHECKS
# =========================================================

check_cells <- function(df, var, aoi = "AOI") {
  aoi_levels <- levels(df[[aoi]])
  var_levels <- levels(df[[var]])

  obs <- df %>%
    dplyr::count(.data[[aoi]], .data[[var]], name = "n")

  full_grid <- tidyr::expand_grid(
    AOI__tmp = aoi_levels,
    VAR__tmp = var_levels
  ) %>%
    rename(!!aoi := AOI__tmp, !!var := VAR__tmp)

  full_grid %>%
    left_join(obs, by = c(aoi, var)) %>%
    mutate(
      n = tidyr::replace_na(n, 0L),
      present = n > 0
    ) %>%
    arrange(.data[[aoi]], .data[[var]])
}

summarise_cell_check <- function(cell_tbl, var_name) {
  tibble(
    factor_name = var_name,
    n_cells_total = nrow(cell_tbl),
    n_cells_present = sum(cell_tbl$present),
    n_cells_missing = sum(!cell_tbl$present),
    missing_cells = if (sum(!cell_tbl$present) == 0) {
      NA_character_
    } else {
      paste0(
        cell_tbl %>%
          dplyr::filter(!present) %>%
          transmute(txt = paste0(AOI, " x ", .data[[var_name]])) %>%
          pull(txt),
        collapse = "; "
      )
    }
  )
}

within_aoi_variation <- function(df, var, aoi = "AOI") {
  df %>%
    group_by(.data[[aoi]]) %>%
    summarise(
      n_levels = n_distinct(.data[[var]]),
      .groups = "drop"
    ) %>%
    mutate(
      factor_name = var,
      has_variation = n_levels >= 2
    ) %>%
    rename(AOI = .data[[aoi]])
}

summarise_within_aoi_variation <- function(var_tbl, var_name) {
  tibble(
    factor_name = var_name,
    n_aoi_total = nrow(var_tbl),
    n_aoi_without_variation = sum(!var_tbl$has_variation),
    aoi_without_variation = if (sum(!var_tbl$has_variation) == 0) {
      NA_character_
    } else {
      paste(var_tbl$AOI[!var_tbl$has_variation], collapse = "; ")
    }
  )
}

inspect_design_structure_encap_mt2 <- function(df) {
  cell_referent <- check_cells(df, "Referent")
  cell_category <- check_cells(df, "Category")

  cell_summary <- bind_rows(
    summarise_cell_check(cell_referent, "Referent"),
    summarise_cell_check(cell_category, "Category")
  )

  var_referent <- within_aoi_variation(df, "Referent")
  var_category <- within_aoi_variation(df, "Category")

  variation_summary <- bind_rows(
    summarise_within_aoi_variation(var_referent, "Referent"),
    summarise_within_aoi_variation(var_category, "Category")
  )

  missing_cells_any <- any(cell_summary$n_cells_missing > 0)
  no_variation_any  <- any(variation_summary$n_aoi_without_variation > 0)

  tibble(
    design_missing_cells_any = missing_cells_any,
    design_no_variation_any = no_variation_any,
    design_missing_Referent_cells = cell_summary$missing_cells[cell_summary$factor_name == "Referent"],
    design_missing_Category_cells = cell_summary$missing_cells[cell_summary$factor_name == "Category"],
    design_aoi_without_Referent_variation = variation_summary$aoi_without_variation[variation_summary$factor_name == "Referent"],
    design_aoi_without_Category_variation = variation_summary$aoi_without_variation[variation_summary$factor_name == "Category"]
  )
}

# =========================================================
# DESIGNMATRIX-CHECK
# getrennt für cond und zi
# =========================================================

inspect_one_matrix <- function(formula, data) {
  mf <- model.frame(formula, data = droplevels(data), na.action = na.omit)
  X <- model.matrix(formula, data = mf)

  qrX <- qr(X)
  rankX <- qrX$rank
  pX <- ncol(X)
  rankdef <- rankX < pX

  redundant_cols <- if (rankdef) {
    pivot <- qrX$pivot
    redundant_idx <- pivot[seq.int(rankX + 1, pX)]
    colnames(X)[redundant_idx]
  } else {
    character(0)
  }

  tibble(
    X_rank = rankX,
    X_ncol = pX,
    fit_rank_deficient = rankdef,
    redundant_cols = collapse_unique(redundant_cols)
  )
}

inspect_design_matrices_encap_mt2_rrt <- function(df, formula_cond, formula_zi) {
  fixed_cond <- lme4::nobars(formula_cond)

  cond_info <- inspect_one_matrix(fixed_cond, df) %>%
    rename(
      cond_X_rank = X_rank,
      cond_X_ncol = X_ncol,
      cond_rank_deficient = fit_rank_deficient,
      cond_redundant_cols = redundant_cols
    )

  zi_info <- inspect_one_matrix(formula_zi, df) %>%
    rename(
      zi_X_rank = X_rank,
      zi_X_ncol = X_ncol,
      zi_rank_deficient = fit_rank_deficient,
      zi_redundant_cols = redundant_cols
    )

  bind_cols(cond_info, zi_info) %>%
    mutate(
      n_distinct_AOI = dplyr::n_distinct(df$AOI),
      n_distinct_Referent = dplyr::n_distinct(df$Referent),
      n_distinct_Category = dplyr::n_distinct(df$Category),
      n_distinct_nLetters = dplyr::n_distinct(df$nLetters),
      min_nLetters = min(df$nLetters),
      max_nLetters = max(df$nLetters),
      prop_zero = mean(df$RRT == 0),
      n_zero = sum(df$RRT == 0),
      n_positive = sum(df$RRT > 0)
    )
}

# =========================================================
# FIT
# =========================================================

fit_glmmtmb_capture_encap_mt2_rrt <- function(df, formula_cond, formula_zi) {
  warn_texts <- character()
  msg_texts  <- character()

  fit <- withCallingHandlers(
    tryCatch(
      glmmTMB::glmmTMB(
        formula = formula_cond,
        ziformula = formula_zi,
        data = droplevels(df),
        family = ziGamma(link = "log")
      ),
      error = function(e) e
    ),
    warning = function(w) {
      warn_texts <<- c(warn_texts, conditionMessage(w))
      invokeRestart("muffleWarning")
    },
    message = function(m) {
      msg_texts <<- c(msg_texts, conditionMessage(m))
      invokeRestart("muffleMessage")
    }
  )

  if (inherits(fit, "error")) {
    return(list(
      fit_tbl = tibble(
        fit_ok = FALSE,
        fit_error = conditionMessage(fit),
        fit_warning_text = collapse_unique(warn_texts),
        fit_message_text = collapse_unique(msg_texts),
        n_warning_messages = length(unique(stats::na.omit(warn_texts))),
        n_messages = length(unique(stats::na.omit(msg_texts))),
        fit_nonposdef_hessian = NA,
        fit_nonzero_convergence = NA,
        fit_singular_re = NA,
        n_random_effect_vars = NA_integer_,
        n_near_zero_random_effect_vars = NA_integer_,
        min_random_effect_var = NA_real_,
        aic = NA_real_,
        bic = NA_real_,
        logLik = NA_real_
      ),
      fit_obj = NULL
    ))
  }

  fit_nonposdef_hessian <- tryCatch(
    !isTRUE(fit$sdr$pdHess),
    error = function(e) NA
  )

  fit_nonzero_convergence <- tryCatch(
    {
      conv <- fit$fit$convergence
      if (is.null(conv)) NA else conv != 0
    },
    error = function(e) NA
  )

  singular_info <- check_singular_re_glmmtmb(fit)

  list(
    fit_tbl = tibble(
      fit_ok = TRUE,
      fit_error = NA_character_,
      fit_warning_text = collapse_unique(warn_texts),
      fit_message_text = collapse_unique(msg_texts),
      n_warning_messages = length(unique(stats::na.omit(warn_texts))),
      n_messages = length(unique(stats::na.omit(msg_texts))),
      fit_nonposdef_hessian = fit_nonposdef_hessian,
      fit_nonzero_convergence = fit_nonzero_convergence
    ) %>%
      bind_cols(singular_info) %>%
      mutate(
        aic = tryCatch(as.numeric(AIC(fit)), error = function(e) NA_real_),
        bic = tryCatch(as.numeric(BIC(fit)), error = function(e) NA_real_),
        logLik = tryCatch(as.numeric(logLik(fit)), error = function(e) NA_real_)
      ),
    fit_obj = fit
  )
}

# =========================================================
# KLASSIFIKATION PROBLEMART
# =========================================================

classify_problem_encap_mt2_rrt <- function(cond_rank_deficient,
                                           zi_rank_deficient,
                                           fit_warning_text,
                                           fit_message_text,
                                           fit_error,
                                           fit_nonposdef_hessian,
                                           fit_nonzero_convergence,
                                           fit_singular_re,
                                           design_missing_cells_any,
                                           design_no_variation_any) {
  txt <- paste(
    c(fit_warning_text, fit_message_text, fit_error),
    collapse = " | "
  )

  any_rankdef <- isTRUE(cond_rank_deficient) || isTRUE(zi_rank_deficient)

  case_when(
    !is.na(fit_error) ~ "fit_error",
    isTRUE(fit_nonposdef_hessian) ~ "nonposdef_hessian",
    isTRUE(fit_singular_re) ~ "singular_random_effects",
    isTRUE(fit_nonzero_convergence) ~ "nonzero_convergence",
    any_rankdef & (design_missing_cells_any | design_no_variation_any) &
      str_detect(txt, regex("rank-deficient|dropping columns|dropp.*column", ignore_case = TRUE)) ~
      "design_driven_rank_deficiency_with_fit_signal",
    any_rankdef & (design_missing_cells_any | design_no_variation_any) ~
      "design_driven_rank_deficiency",
    any_rankdef &
      str_detect(txt, regex("rank-deficient|dropping columns|dropp.*column", ignore_case = TRUE)) ~
      "rank_deficient_design_and_fit_signal",
    any_rankdef ~ "rank_deficient_design",
    str_detect(txt, regex("rank-deficient|dropping columns|dropp.*column", ignore_case = TRUE)) ~
      "rank_deficient_warning_without_matrix_flag",
    str_detect(txt, regex("hessian|not positive definite|non-positive-definite", ignore_case = TRUE)) ~
      "hessian_signal_only",
    str_detect(txt, regex("convergence|did not converge|false convergence|iteration limit", ignore_case = TRUE)) ~
      "convergence_signal",
    (!is.na(fit_warning_text) || !is.na(fit_message_text)) ~ "warning_or_message_only",
    TRUE ~ "none"
  )
}

# =========================================================
# EIN FULL-DATA-FIT PRO aggr_level
# =========================================================

fit_one_full_case_encap_mt2_rrt <- function(df_aggr,
                                            formula_cond,
                                            formula_zi,
                                            exp_name = "encap_mt2",
                                            aggr_i,
                                            n_aoi_in_cluster) {
  df_aggr <- droplevels(df_aggr)

  n_levels_aoi <- dplyr::n_distinct(df_aggr$AOI)
  n_levels_referent <- dplyr::n_distinct(df_aggr$Referent)
  n_levels_category <- dplyr::n_distinct(df_aggr$Category)
  n_levels_topic <- dplyr::n_distinct(df_aggr$Topic)
  n_levels_participant <- dplyr::n_distinct(df_aggr$Participant)

  if (n_levels_referent < 2) {
    return(
      tibble(
        experiment = exp_name,
        aggr_level = aggr_i,
        n_aoi_in_cluster = n_aoi_in_cluster,
        n_obs = nrow(df_aggr),
        n_topics = n_levels_topic,
        n_participants = n_levels_participant,
        cond_formula = paste(deparse(formula_cond), collapse = " "),
        zi_formula = paste(deparse(formula_zi), collapse = " "),
        design_missing_cells_any = NA,
        design_no_variation_any = NA,
        design_missing_Referent_cells = NA_character_,
        design_missing_Category_cells = NA_character_,
        design_aoi_without_Referent_variation = NA_character_,
        design_aoi_without_Category_variation = NA_character_,
        cond_X_rank = NA_real_,
        cond_X_ncol = NA_real_,
        cond_rank_deficient = NA,
        cond_redundant_cols = NA_character_,
        zi_X_rank = NA_real_,
        zi_X_ncol = NA_real_,
        zi_rank_deficient = NA,
        zi_redundant_cols = NA_character_,
        n_distinct_AOI = n_levels_aoi,
        n_distinct_Referent = n_levels_referent,
        n_distinct_Category = n_levels_category,
        n_distinct_nLetters = dplyr::n_distinct(df_aggr$nLetters),
        min_nLetters = min(df_aggr$nLetters),
        max_nLetters = max(df_aggr$nLetters),
        prop_zero = mean(df_aggr$RRT == 0),
        n_zero = sum(df_aggr$RRT == 0),
        n_positive = sum(df_aggr$RRT > 0),
        fit_ok = FALSE,
        fit_error = "Referent has fewer than 2 levels in full data for this aggr_level",
        fit_warning_text = NA_character_,
        fit_message_text = NA_character_,
        n_warning_messages = 0L,
        n_messages = 0L,
        fit_nonposdef_hessian = NA,
        fit_nonzero_convergence = NA,
        fit_singular_re = NA,
        n_random_effect_vars = NA_integer_,
        n_near_zero_random_effect_vars = NA_integer_,
        min_random_effect_var = NA_real_,
        aic = NA_real_,
        bic = NA_real_,
        logLik = NA_real_,
        n_obs_metric = NA_real_,
        rmse_in_sample = NA_real_,
        mae_in_sample = NA_real_,
        bias_in_sample = NA_real_,
        cor_obs_pred = NA_real_,
        problem_type = "insufficient_levels_Referent"
      )
    )
  }

  if (n_levels_category < 2) {
    return(
      tibble(
        experiment = exp_name,
        aggr_level = aggr_i,
        n_aoi_in_cluster = n_aoi_in_cluster,
        n_obs = nrow(df_aggr),
        n_topics = n_levels_topic,
        n_participants = n_levels_participant,
        cond_formula = paste(deparse(formula_cond), collapse = " "),
        zi_formula = paste(deparse(formula_zi), collapse = " "),
        design_missing_cells_any = NA,
        design_no_variation_any = NA,
        design_missing_Referent_cells = NA_character_,
        design_missing_Category_cells = NA_character_,
        design_aoi_without_Referent_variation = NA_character_,
        design_aoi_without_Category_variation = NA_character_,
        cond_X_rank = NA_real_,
        cond_X_ncol = NA_real_,
        cond_rank_deficient = NA,
        cond_redundant_cols = NA_character_,
        zi_X_rank = NA_real_,
        zi_X_ncol = NA_real_,
        zi_rank_deficient = NA,
        zi_redundant_cols = NA_character_,
        n_distinct_AOI = n_levels_aoi,
        n_distinct_Referent = n_levels_referent,
        n_distinct_Category = n_levels_category,
        n_distinct_nLetters = dplyr::n_distinct(df_aggr$nLetters),
        min_nLetters = min(df_aggr$nLetters),
        max_nLetters = max(df_aggr$nLetters),
        prop_zero = mean(df_aggr$RRT == 0),
        n_zero = sum(df_aggr$RRT == 0),
        n_positive = sum(df_aggr$RRT > 0),
        fit_ok = FALSE,
        fit_error = "Category has fewer than 2 levels in full data for this aggr_level",
        fit_warning_text = NA_character_,
        fit_message_text = NA_character_,
        n_warning_messages = 0L,
        n_messages = 0L,
        fit_nonposdef_hessian = NA,
        fit_nonzero_convergence = NA,
        fit_singular_re = NA,
        n_random_effect_vars = NA_integer_,
        n_near_zero_random_effect_vars = NA_integer_,
        min_random_effect_var = NA_real_,
        aic = NA_real_,
        bic = NA_real_,
        logLik = NA_real_,
        n_obs_metric = NA_real_,
        rmse_in_sample = NA_real_,
        mae_in_sample = NA_real_,
        bias_in_sample = NA_real_,
        cor_obs_pred = NA_real_,
        problem_type = "insufficient_levels_Category"
      )
    )
  }

  if (n_levels_aoi < 2 && n_aoi_in_cluster > 1) {
    return(
      tibble(
        experiment = exp_name,
        aggr_level = aggr_i,
        n_aoi_in_cluster = n_aoi_in_cluster,
        n_obs = nrow(df_aggr),
        n_topics = n_levels_topic,
        n_participants = n_levels_participant,
        cond_formula = paste(deparse(formula_cond), collapse = " "),
        zi_formula = paste(deparse(formula_zi), collapse = " "),
        design_missing_cells_any = NA,
        design_no_variation_any = NA,
        design_missing_Referent_cells = NA_character_,
        design_missing_Category_cells = NA_character_,
        design_aoi_without_Referent_variation = NA_character_,
        design_aoi_without_Category_variation = NA_character_,
        cond_X_rank = NA_real_,
        cond_X_ncol = NA_real_,
        cond_rank_deficient = NA,
        cond_redundant_cols = NA_character_,
        zi_X_rank = NA_real_,
        zi_X_ncol = NA_real_,
        zi_rank_deficient = NA,
        zi_redundant_cols = NA_character_,
        n_distinct_AOI = n_levels_aoi,
        n_distinct_Referent = n_levels_referent,
        n_distinct_Category = n_levels_category,
        n_distinct_nLetters = dplyr::n_distinct(df_aggr$nLetters),
        min_nLetters = min(df_aggr$nLetters),
        max_nLetters = max(df_aggr$nLetters),
        prop_zero = mean(df_aggr$RRT == 0),
        n_zero = sum(df_aggr$RRT == 0),
        n_positive = sum(df_aggr$RRT > 0),
        fit_ok = FALSE,
        fit_error = "AOI has fewer than 2 levels although cluster is expected to contain multiple AOIs",
        fit_warning_text = NA_character_,
        fit_message_text = NA_character_,
        n_warning_messages = 0L,
        n_messages = 0L,
        fit_nonposdef_hessian = NA,
        fit_nonzero_convergence = NA,
        fit_singular_re = NA,
        n_random_effect_vars = NA_integer_,
        n_near_zero_random_effect_vars = NA_integer_,
        min_random_effect_var = NA_real_,
        aic = NA_real_,
        bic = NA_real_,
        logLik = NA_real_,
        n_obs_metric = NA_real_,
        rmse_in_sample = NA_real_,
        mae_in_sample = NA_real_,
        bias_in_sample = NA_real_,
        cor_obs_pred = NA_real_,
        problem_type = "insufficient_levels_AOI"
      )
    )
  }

  design_structure <- inspect_design_structure_encap_mt2(df_aggr)

  design_info <- inspect_design_matrices_encap_mt2_rrt(
    df = df_aggr,
    formula_cond = formula_cond,
    formula_zi = formula_zi
  )

  fit_res <- fit_glmmtmb_capture_encap_mt2_rrt(
    df = df_aggr,
    formula_cond = formula_cond,
    formula_zi = formula_zi
  )

  fit_info <- fit_res$fit_tbl
  fit_obj  <- fit_res$fit_obj

  fit_metrics <- if (!is.null(fit_obj)) {
    fitted_vals <- tryCatch(
      predict(fit_obj, type = "response", re.form = NULL),
      error = function(e) rep(NA_real_, nrow(df_aggr))
    )

    calc_fit_metrics(
      obs = df_aggr$RRT,
      pred = fitted_vals
    )
  } else {
    tibble(
      n_obs_metric = NA_real_,
      rmse_in_sample = NA_real_,
      mae_in_sample = NA_real_,
      bias_in_sample = NA_real_,
      cor_obs_pred = NA_real_
    )
  }

  tibble(
    experiment = exp_name,
    aggr_level = aggr_i,
    n_aoi_in_cluster = n_aoi_in_cluster,
    n_obs = nrow(df_aggr),
    n_topics = n_levels_topic,
    n_participants = n_levels_participant,
    cond_formula = paste(deparse(formula_cond), collapse = " "),
    zi_formula = paste(deparse(formula_zi), collapse = " ")
  ) %>%
    bind_cols(design_structure) %>%
    bind_cols(design_info) %>%
    bind_cols(fit_info) %>%
    bind_cols(fit_metrics) %>%
    mutate(
      problem_type = classify_problem_encap_mt2_rrt(
        cond_rank_deficient = cond_rank_deficient,
        zi_rank_deficient = zi_rank_deficient,
        fit_warning_text = fit_warning_text,
        fit_message_text = fit_message_text,
        fit_error = fit_error,
        fit_nonposdef_hessian = fit_nonposdef_hessian,
        fit_nonzero_convergence = fit_nonzero_convergence,
        fit_singular_re = fit_singular_re,
        design_missing_cells_any = design_missing_cells_any,
        design_no_variation_any = design_no_variation_any
      )
    )
}

# =========================================================
# FULL-DATA-FITS ÜBER aggr_level
# =========================================================

run_fullfits_encap_mt2_rrt <- function(df,
                                       exp_name = "encap_mt2") {
  prep <- prep_encap_mt2_fullfit_data_rrt(df)
  dat <- prep$data
  cluster_sizes <- prep$cluster_sizes

  aggr_levels <- unique(as.character(dat$aggr_level))

  fullfit_all <- purrr::map_dfr(aggr_levels, function(aggr_i) {
    df_aggr <- dat %>%
      filter(as.character(aggr_level) == aggr_i)

    n_aoi_cluster <- cluster_sizes$n_aoi_in_cluster[
      match(aggr_i, as.character(cluster_sizes$aggr_level))
    ]

    formula_cond <- make_formula_cluster_encap_mt2_rrt(
      response = "RRT",
      n_aoi_in_cluster = n_aoi_cluster
    )

    formula_zi <- make_ziformula_cluster_encap_mt2_rrt(
      n_aoi_in_cluster = n_aoi_cluster
    )

    fit_one_full_case_encap_mt2_rrt(
      df_aggr = df_aggr,
      formula_cond = formula_cond,
      formula_zi = formula_zi,
      exp_name = exp_name,
      aggr_i = aggr_i,
      n_aoi_in_cluster = n_aoi_cluster
    )
  })

  problem_counts <- fullfit_all %>%
    count(problem_type, sort = TRUE)

  problem_fits_only <- fullfit_all %>%
    filter(
      problem_type != "none" |
        fit_ok %in% FALSE |
        fit_nonposdef_hessian %in% TRUE |
        fit_singular_re %in% TRUE |
        fit_nonzero_convergence %in% TRUE |
        cond_rank_deficient %in% TRUE |
        zi_rank_deficient %in% TRUE |
        n_warning_messages > 0
    ) %>%
    arrange(aggr_level)

  list(
    fullfit_all = fullfit_all,
    problem_counts = problem_counts,
    problem_fits_only = problem_fits_only
  )
}

# =========================================================
# AUSFÜHREN
# =========================================================

res_encap_mt2_fullfit_rrt <- run_fullfits_encap_mt2_rrt(
  df = encap_mt2,
  exp_name = "encap_mt2"
)

# =========================================================
# OUTPUTS
# =========================================================

#round_df_4(res_encap_mt2_fullfit_rrt$fullfit_all)
#round_df_4(res_encap_mt2_fullfit_rrt$problem_counts)
#round_df_4(res_encap_mt2_fullfit_rrt$problem_fits_only)

knitr::kable(
 round_df_4(res_encap_mt2_fullfit_rrt$problem_counts),
  format = "html",
  table.attr = 'style="width:100%; table-layout: fixed;"'
)
```


*RRT: Topic LOO + Fit Problems*
```{r}
library(dplyr)
library(tidyr)
library(purrr)
library(tibble)
library(stringr)
library(glmmTMB)
library(lme4)

# =========================================================
# HILFSFUNKTIONEN
# =========================================================

safe_plain_df <- function(df) {
  out <- as.data.frame(df, stringsAsFactors = FALSE)
  rownames(out) <- NULL
  out
}

round_df_4 <- function(df) {
  df %>%
    mutate(across(where(is.numeric), ~ round(.x, 4)))
}

collapse_unique <- function(x) {
  x <- unique(stats::na.omit(x))
  if (length(x) == 0) return(NA_character_)
  paste(x, collapse = " | ")
}

calc_metrics <- function(obs, pred) {
  keep <- is.finite(obs) & is.finite(pred)
  obs  <- obs[keep]
  pred <- pred[keep]

  if (length(obs) == 0) {
    return(tibble(
      n_test = 0L,
      rmse   = NA_real_,
      mae    = NA_real_,
      bias   = NA_real_
    ))
  }

  err <- pred - obs

  tibble(
    n_test = length(obs),
    rmse   = sqrt(mean(err^2)),
    mae    = mean(abs(err)),
    bias   = mean(err)
  )
}

# =========================================================
# HEURISTIK FÜR SINGULAR RANDOM EFFECTS IN glmmTMB
# =========================================================

extract_re_variances_glmmtmb <- function(fit_obj) {
  vc <- tryCatch(VarCorr(fit_obj), error = function(e) NULL)
  if (is.null(vc) || is.null(vc$cond)) return(numeric(0))

  vars <- c()

  for (grp in names(vc$cond)) {
    mat <- vc$cond[[grp]]
    sd_attr <- attr(mat, "stddev")

    if (!is.null(sd_attr) && length(sd_attr) > 0) {
      vars <- c(vars, as.numeric(sd_attr)^2)
    } else if (is.matrix(mat) && nrow(mat) > 0) {
      vars <- c(vars, diag(as.matrix(mat)))
    }
  }

  vars
}

check_singular_re_glmmtmb <- function(fit_obj, tol = 1e-8) {
  re_vars <- extract_re_variances_glmmtmb(fit_obj)

  if (length(re_vars) == 0) {
    return(tibble(
      fit_singular_re = NA,
      n_random_effect_vars = 0L,
      n_near_zero_random_effect_vars = NA_integer_,
      min_random_effect_var = NA_real_
    ))
  }

  tibble(
    fit_singular_re = any(re_vars < tol),
    n_random_effect_vars = length(re_vars),
    n_near_zero_random_effect_vars = sum(re_vars < tol),
    min_random_effect_var = min(re_vars)
  )
}

# =========================================================
# DATEN
# Fit und Fehlermetriken auf allen Werten
# Erwartet:
# RRT, Referent, Category, AOI, nLetters,
# Participant, Topic, aggr_level
# =========================================================

prep_encap_mt2_topicloo_data_rrt <- function(df) {
  needed <- c(
    "RRT", "Referent", "Category", "AOI",
    "nLetters", "Participant", "Topic", "aggr_level"
  )

  missing_cols <- setdiff(needed, names(df))
  if (length(missing_cols) > 0) {
    stop("Fehlende Variablen: ", paste(missing_cols, collapse = ", "))
  }

  out <- df %>%
    filter(!is.na(RRT), RRT >= 0) %>%
    filter(!is.na(Referent), !is.na(Category), !is.na(AOI), !is.na(nLetters)) %>%
    filter(!is.na(Participant), !is.na(Topic), !is.na(aggr_level)) %>%
    mutate(
      Referent = factor(Referent),
      Category = factor(Category),
      AOI = factor(AOI),
      Participant = factor(Participant),
      Topic = factor(Topic),
      aggr_level = factor(aggr_level)
    )

  cluster_sizes <- out %>%
    distinct(aggr_level, AOI) %>%
    count(aggr_level, name = "n_aoi_in_cluster")

  list(
    data = safe_plain_df(out),
    cluster_sizes = cluster_sizes
  )
}

# =========================================================
# FORMELN PRO CLUSTER
# =========================================================

make_formula_cluster_encap_mt2_rrt <- function(response = "RRT",
                                               n_aoi_in_cluster = 1L) {
  rhs <- if (n_aoi_in_cluster > 1) {
    "Referent*AOI + Category*AOI + nLetters + (1 | Topic) + (1 | Participant)"
  } else {
    "Referent + Category + nLetters + (1 | Topic) + (1 | Participant)"
  }

  as.formula(paste(response, "~", rhs))
}

make_ziformula_cluster_encap_mt2_rrt <- function(n_aoi_in_cluster = 1L) {
  rhs <- if (n_aoi_in_cluster > 1) {
    "Referent*AOI + Category*AOI + nLetters"
  } else {
    "Referent + Category + nLetters"
  }

  as.formula(paste("~", rhs))
}

# =========================================================
# DESIGNMATRIX-CHECK
# getrennt für cond und zi
# =========================================================

inspect_one_matrix <- function(formula, data) {
  mf <- model.frame(formula, data = droplevels(data), na.action = na.omit)
  X <- model.matrix(formula, data = mf)

  qrX <- qr(X)
  rankX <- qrX$rank
  pX <- ncol(X)
  rankdef <- rankX < pX

  redundant_cols <- if (rankdef) {
    pivot <- qrX$pivot
    redundant_idx <- pivot[seq.int(rankX + 1, pX)]
    colnames(X)[redundant_idx]
  } else {
    character(0)
  }

  tibble(
    X_rank = rankX,
    X_ncol = pX,
    fit_rank_deficient = rankdef,
    redundant_cols = collapse_unique(redundant_cols)
  )
}

inspect_design_matrices_encap_mt2_rrt <- function(df,
                                                  formula_cond,
                                                  formula_zi) {
  fixed_cond <- lme4::nobars(formula_cond)

  cond_info <- inspect_one_matrix(fixed_cond, df) %>%
    rename(
      cond_X_rank = X_rank,
      cond_X_ncol = X_ncol,
      cond_rank_deficient = fit_rank_deficient,
      cond_redundant_cols = redundant_cols
    )

  zi_info <- inspect_one_matrix(formula_zi, df) %>%
    rename(
      zi_X_rank = X_rank,
      zi_X_ncol = X_ncol,
      zi_rank_deficient = fit_rank_deficient,
      zi_redundant_cols = redundant_cols
    )

  bind_cols(cond_info, zi_info) %>%
    mutate(
      n_distinct_nLetters = dplyr::n_distinct(df$nLetters),
      min_nLetters = min(df$nLetters),
      max_nLetters = max(df$nLetters),
      prop_zero = mean(df$RRT == 0),
      n_zero = sum(df$RRT == 0),
      n_positive = sum(df$RRT > 0)
    )
}

# =========================================================
# FIT
# =========================================================

fit_glmmtmb_capture_encap_mt2_rrt <- function(df,
                                              formula_cond,
                                              formula_zi) {
  warn_texts <- character()
  msg_texts  <- character()

  fit <- withCallingHandlers(
    tryCatch(
      glmmTMB::glmmTMB(
        formula = formula_cond,
        ziformula = formula_zi,
        data = droplevels(df),
        family = ziGamma(link = "log")
      ),
      error = function(e) e
    ),
    warning = function(w) {
      warn_texts <<- c(warn_texts, conditionMessage(w))
      invokeRestart("muffleWarning")
    },
    message = function(m) {
      msg_texts <<- c(msg_texts, conditionMessage(m))
      invokeRestart("muffleMessage")
    }
  )

  if (inherits(fit, "error")) {
    return(list(
      fit_tbl = tibble(
        fit_ok = FALSE,
        fit_error = conditionMessage(fit),
        fit_warning_text = collapse_unique(warn_texts),
        fit_message_text = collapse_unique(msg_texts),
        n_warning_messages = length(unique(stats::na.omit(warn_texts))),
        n_messages = length(unique(stats::na.omit(msg_texts))),
        fit_nonposdef_hessian = NA,
        fit_nonzero_convergence = NA,
        fit_singular_re = NA,
        n_random_effect_vars = NA_integer_,
        n_near_zero_random_effect_vars = NA_integer_,
        min_random_effect_var = NA_real_,
        aic = NA_real_,
        bic = NA_real_,
        logLik = NA_real_
      ),
      fit_obj = NULL
    ))
  }

  fit_nonposdef_hessian <- tryCatch(
    !isTRUE(fit$sdr$pdHess),
    error = function(e) NA
  )

  fit_nonzero_convergence <- tryCatch(
    {
      conv <- fit$fit$convergence
      if (is.null(conv)) NA else conv != 0
    },
    error = function(e) NA
  )

  singular_info <- check_singular_re_glmmtmb(fit)

  list(
    fit_tbl = tibble(
      fit_ok = TRUE,
      fit_error = NA_character_,
      fit_warning_text = collapse_unique(warn_texts),
      fit_message_text = collapse_unique(msg_texts),
      n_warning_messages = length(unique(stats::na.omit(warn_texts))),
      n_messages = length(unique(stats::na.omit(msg_texts))),
      fit_nonposdef_hessian = fit_nonposdef_hessian,
      fit_nonzero_convergence = fit_nonzero_convergence
    ) %>%
      bind_cols(singular_info) %>%
      mutate(
        aic = tryCatch(as.numeric(AIC(fit)), error = function(e) NA_real_),
        bic = tryCatch(as.numeric(BIC(fit)), error = function(e) NA_real_),
        logLik = tryCatch(as.numeric(logLik(fit)), error = function(e) NA_real_)
      ),
    fit_obj = fit
  )
}

# =========================================================
# KLASSIFIKATION PROBLEMART
# Hessian und Singularität getrennt
# =========================================================

classify_problem_encap_mt2_rrt <- function(cond_rank_deficient,
                                           zi_rank_deficient,
                                           fit_warning_text,
                                           fit_message_text,
                                           fit_error,
                                           fit_nonposdef_hessian,
                                           fit_nonzero_convergence,
                                           fit_singular_re,
                                           prediction_error) {
  txt <- paste(
    c(fit_warning_text, fit_message_text, fit_error, prediction_error),
    collapse = " | "
  )

  any_rankdef <- isTRUE(cond_rank_deficient) || isTRUE(zi_rank_deficient)

  case_when(
    !is.na(fit_error) ~ "fit_error",
    !is.na(prediction_error) ~ "prediction_error",
    isTRUE(fit_nonposdef_hessian) ~ "nonposdef_hessian",
    isTRUE(fit_singular_re) ~ "singular_random_effects",
    isTRUE(fit_nonzero_convergence) ~ "nonzero_convergence",
    any_rankdef &
      str_detect(txt, regex("rank-deficient|dropping columns|dropp.*column", ignore_case = TRUE)) ~
      "rank_deficient_design_and_fit_signal",
    any_rankdef ~ "rank_deficient_design",
    str_detect(txt, regex("rank-deficient|dropping columns|dropp.*column", ignore_case = TRUE)) ~
      "rank_deficient_warning_without_matrix_flag",
    str_detect(txt, regex("hessian|not positive definite|non-positive-definite", ignore_case = TRUE)) ~
      "hessian_signal_only",
    str_detect(txt, regex("convergence|did not converge|false convergence|iteration limit", ignore_case = TRUE)) ~
      "convergence_signal",
    (!is.na(fit_warning_text) || !is.na(fit_message_text)) ~ "warning_or_message_only",
    TRUE ~ "none"
  )
}

# =========================================================
# EIN TOPIC-LOO-FIT
# Vorhersage und Fehlermetriken auf allen Testwerten
# =========================================================

fit_one_topicloo_case_encap_mt2_rrt <- function(train_df,
                                                test_df,
                                                formula_cond,
                                                formula_zi,
                                                n_aoi_in_cluster,
                                                exp_name = "encap_mt2",
                                                aggr_i,
                                                topic_i) {
  train_df <- droplevels(train_df)
  test_df  <- droplevels(test_df)

  design_info <- inspect_design_matrices_encap_mt2_rrt(
    df = train_df,
    formula_cond = formula_cond,
    formula_zi = formula_zi
  )

  fit_res <- fit_glmmtmb_capture_encap_mt2_rrt(
    df = train_df,
    formula_cond = formula_cond,
    formula_zi = formula_zi
  )

  fit_info <- fit_res$fit_tbl
  fit_obj  <- fit_res$fit_obj

  pred_error <- NA_character_
  pred <- rep(NA_real_, nrow(test_df))

  if (!is.null(fit_obj)) {
    pred <- tryCatch(
      predict(
        fit_obj,
        newdata = test_df,
        type = "response",
        re.form = NULL,
        allow.new.levels = TRUE
      ),
      error = function(e) {
        pred_error <<- conditionMessage(e)
        rep(NA_real_, nrow(test_df))
      }
    )
  }

  metric_info <- calc_metrics(
    obs = test_df$RRT,
    pred = pred
  )

  tibble(
    experiment = exp_name,
    aggr_level = aggr_i,
    heldout_topic = topic_i,
    n_aoi_in_cluster = n_aoi_in_cluster,
    cluster_formula_type = ifelse(n_aoi_in_cluster > 1, "Referent_Category_AOI", "Referent_Category_only"),
    n_train = nrow(train_df),
    n_test_rows = nrow(test_df),
    n_test_zero = sum(test_df$RRT == 0),
    n_test_positive = sum(test_df$RRT > 0),
    formula_cond = paste(deparse(formula_cond), collapse = " "),
    formula_zi = paste(deparse(formula_zi), collapse = " ")
  ) %>%
    bind_cols(design_info) %>%
    bind_cols(fit_info) %>%
    bind_cols(metric_info) %>%
    mutate(
      prediction_error = pred_error,
      problem_type = classify_problem_encap_mt2_rrt(
        cond_rank_deficient = cond_rank_deficient,
        zi_rank_deficient = zi_rank_deficient,
        fit_warning_text = fit_warning_text,
        fit_message_text = fit_message_text,
        fit_error = fit_error,
        fit_nonposdef_hessian = fit_nonposdef_hessian,
        fit_nonzero_convergence = fit_nonzero_convergence,
        fit_singular_re = fit_singular_re,
        prediction_error = prediction_error
      )
    )
}

# =========================================================
# TOPIC-LOO FÜR encap_mt2, RRT
# Gesamtbereich inklusive 0ern
# =========================================================

run_topicloo_encap_mt2_rrt <- function(df,
                                       exp_name = "encap_mt2",
                                       verbose = TRUE) {
  prep <- prep_encap_mt2_topicloo_data_rrt(df)
  dat <- prep$data
  cluster_sizes <- prep$cluster_sizes

  aggr_levels <- unique(as.character(dat$aggr_level))

  loo_all <- purrr::map_dfr(aggr_levels, function(aggr_i) {
    dat_cluster <- dat %>%
      filter(as.character(aggr_level) == aggr_i)

    n_aoi_cluster <- cluster_sizes$n_aoi_in_cluster[
      match(aggr_i, cluster_sizes$aggr_level)
    ]

    formula_cond <- make_formula_cluster_encap_mt2_rrt(
      response = "RRT",
      n_aoi_in_cluster = n_aoi_cluster
    )

    formula_zi <- make_ziformula_cluster_encap_mt2_rrt(
      n_aoi_in_cluster = n_aoi_cluster
    )

    topics_cluster <- unique(as.character(dat_cluster$Topic))

    purrr::map_dfr(topics_cluster, function(topic_i) {
      train_df <- dat_cluster %>%
        filter(as.character(Topic) != topic_i)

      test_df <- dat_cluster %>%
        filter(as.character(Topic) == topic_i)

      if (isTRUE(verbose)) {
        message(
          "[RRT Topic-LOO] exp=", exp_name,
          " aggr_level=", aggr_i,
          " holdout_topic=", topic_i,
          " n_train=", nrow(train_df)
        )
      }

      fit_one_topicloo_case_encap_mt2_rrt(
        train_df = train_df,
        test_df = test_df,
        formula_cond = formula_cond,
        formula_zi = formula_zi,
        n_aoi_in_cluster = n_aoi_cluster,
        exp_name = exp_name,
        aggr_i = aggr_i,
        topic_i = topic_i
      )
    })
  })

  loo_summary <- loo_all %>%
    mutate(has_warning = n_warning_messages > 0) %>%
    group_by(experiment, aggr_level) %>%
    summarise(
      n_holdout_fits = dplyr::n(),
      n_fit_failed = sum(fit_ok %in% FALSE, na.rm = TRUE),
      n_nonposdef_hessian = sum(fit_nonposdef_hessian %in% TRUE, na.rm = TRUE),
      n_singular_re = sum(fit_singular_re %in% TRUE, na.rm = TRUE),
      n_nonzero_convergence = sum(fit_nonzero_convergence %in% TRUE, na.rm = TRUE),
      n_cond_rank_deficient = sum(cond_rank_deficient %in% TRUE, na.rm = TRUE),
      n_zi_rank_deficient = sum(zi_rank_deficient %in% TRUE, na.rm = TRUE),
      n_fits_with_warning = sum(has_warning, na.rm = TRUE),
      total_warning_messages = sum(n_warning_messages, na.rm = TRUE),
      total_test_zero = sum(n_test_zero, na.rm = TRUE),
      total_test_positive = sum(n_test_positive, na.rm = TRUE),
      mean_rmse = mean(rmse, na.rm = TRUE),
      mean_mae = mean(mae, na.rm = TRUE),
      mean_bias = mean(bias, na.rm = TRUE),
      median_rmse = median(rmse, na.rm = TRUE),
      median_mae = median(mae, na.rm = TRUE),
      median_bias = median(bias, na.rm = TRUE),
      .groups = "drop"
    )

  problem_counts <- loo_all %>%
    count(aggr_level, problem_type, sort = TRUE)

  problem_fits_only <- loo_all %>%
    filter(
      problem_type != "none" |
        fit_ok %in% FALSE |
        fit_nonposdef_hessian %in% TRUE |
        fit_singular_re %in% TRUE |
        fit_nonzero_convergence %in% TRUE |
        cond_rank_deficient %in% TRUE |
        zi_rank_deficient %in% TRUE |
        n_warning_messages > 0 |
        !is.na(prediction_error)
    ) %>%
    arrange(aggr_level, heldout_topic)

  list(
    loo_all = loo_all,
    loo_summary = loo_summary,
    problem_counts = problem_counts,
    problem_fits_only = problem_fits_only
  )
}

# =========================================================
# AUSFÜHREN
# =========================================================

res_encap_mt2_topicloo_rrt <- run_topicloo_encap_mt2_rrt(
  df = encap_mt2,
  exp_name = "encap_mt2",
  verbose = TRUE
)

# =========================================================
# OUTPUTS
# =========================================================

#round_df_4(res_encap_mt2_topicloo_rrt$loo_all)
#round_df_4(res_encap_mt2_topicloo_rrt$loo_summary)
#round_df_4(res_encap_mt2_topicloo_rrt$problem_counts)
#round_df_4(res_encap_mt2_topicloo_rrt$problem_fits_only)

knitr::kable(
 round_df_4(res_encap_mt2_topicloo_rrt$loo_summary),
  format = "html",
  table.attr = 'style="width:100%; table-layout: fixed;"'
)
```


*RRT: Participant LOO +  Fit Problems*
```{r}
library(dplyr)
library(tidyr)
library(purrr)
library(tibble)
library(stringr)
library(glmmTMB)
library(lme4)

# =========================================================
# HILFSFUNKTIONEN
# =========================================================

safe_plain_df <- function(df) {
  out <- as.data.frame(df, stringsAsFactors = FALSE)
  rownames(out) <- NULL
  out
}

round_df_4 <- function(df) {
  df %>%
    mutate(across(where(is.numeric), ~ round(.x, 4)))
}

collapse_unique <- function(x) {
  x <- unique(stats::na.omit(x))
  if (length(x) == 0) return(NA_character_)
  paste(x, collapse = " | ")
}

calc_metrics <- function(obs, pred) {
  keep <- is.finite(obs) & is.finite(pred)
  obs  <- obs[keep]
  pred <- pred[keep]

  if (length(obs) == 0) {
    return(tibble(
      n_test = 0L,
      rmse   = NA_real_,
      mae    = NA_real_,
      bias   = NA_real_
    ))
  }

  err <- pred - obs

  tibble(
    n_test = length(obs),
    rmse   = sqrt(mean(err^2)),
    mae    = mean(abs(err)),
    bias   = mean(err)
  )
}

# =========================================================
# HEURISTIK FÜR SINGULAR RANDOM EFFECTS IN glmmTMB
# =========================================================

extract_re_variances_glmmtmb <- function(fit_obj) {
  vc <- tryCatch(VarCorr(fit_obj), error = function(e) NULL)
  if (is.null(vc) || is.null(vc$cond)) return(numeric(0))

  vars <- c()

  for (grp in names(vc$cond)) {
    mat <- vc$cond[[grp]]
    sd_attr <- attr(mat, "stddev")

    if (!is.null(sd_attr) && length(sd_attr) > 0) {
      vars <- c(vars, as.numeric(sd_attr)^2)
    } else if (is.matrix(mat) && nrow(mat) > 0) {
      vars <- c(vars, diag(as.matrix(mat)))
    }
  }

  vars
}

check_singular_re_glmmtmb <- function(fit_obj, tol = 1e-8) {
  re_vars <- extract_re_variances_glmmtmb(fit_obj)

  if (length(re_vars) == 0) {
    return(tibble(
      fit_singular_re = NA,
      n_random_effect_vars = 0L,
      n_near_zero_random_effect_vars = NA_integer_,
      min_random_effect_var = NA_real_
    ))
  }

  tibble(
    fit_singular_re = any(re_vars < tol),
    n_random_effect_vars = length(re_vars),
    n_near_zero_random_effect_vars = sum(re_vars < tol),
    min_random_effect_var = min(re_vars)
  )
}

# =========================================================
# DATEN
# Fit und Fehlermetriken auf allen Werten
# Erwartet:
# RRT, Referent, Category, AOI, nLetters,
# Participant, Topic, aggr_level
# =========================================================

prep_encap_mt2_participantloo_data_rrt <- function(df) {
  needed <- c(
    "RRT", "Referent", "Category", "AOI",
    "nLetters", "Participant", "Topic", "aggr_level"
  )

  missing_cols <- setdiff(needed, names(df))
  if (length(missing_cols) > 0) {
    stop("Fehlende Variablen: ", paste(missing_cols, collapse = ", "))
  }

  out <- df %>%
    filter(!is.na(RRT), RRT >= 0) %>%
    filter(!is.na(Referent), !is.na(Category), !is.na(AOI), !is.na(nLetters)) %>%
    filter(!is.na(Participant), !is.na(Topic), !is.na(aggr_level)) %>%
    mutate(
      Referent = factor(Referent),
      Category = factor(Category),
      AOI = factor(AOI),
      Participant = factor(Participant),
      Topic = factor(Topic),
      aggr_level = factor(aggr_level)
    )

  cluster_sizes <- out %>%
    distinct(aggr_level, AOI) %>%
    count(aggr_level, name = "n_aoi_in_cluster")

  list(
    data = safe_plain_df(out),
    cluster_sizes = cluster_sizes
  )
}

# =========================================================
# FORMELN PRO CLUSTER
# =========================================================

make_formula_cluster_encap_mt2_rrt <- function(response = "RRT",
                                               n_aoi_in_cluster = 1L) {
  rhs <- if (n_aoi_in_cluster > 1) {
    "Referent*AOI + Category*AOI + nLetters + (1 | Topic) + (1 | Participant)"
  } else {
    "Referent + Category + nLetters + (1 | Topic) + (1 | Participant)"
  }

  as.formula(paste(response, "~", rhs))
}

make_ziformula_cluster_encap_mt2_rrt <- function(n_aoi_in_cluster = 1L) {
  rhs <- if (n_aoi_in_cluster > 1) {
    "Referent*AOI + Category*AOI + nLetters"
  } else {
    "Referent + Category + nLetters"
  }

  as.formula(paste("~", rhs))
}

# =========================================================
# DESIGNMATRIX-CHECK
# getrennt für cond und zi
# =========================================================

inspect_one_matrix <- function(formula, data) {
  mf <- model.frame(formula, data = droplevels(data), na.action = na.omit)
  X <- model.matrix(formula, data = mf)

  qrX <- qr(X)
  rankX <- qrX$rank
  pX <- ncol(X)
  rankdef <- rankX < pX

  redundant_cols <- if (rankdef) {
    pivot <- qrX$pivot
    redundant_idx <- pivot[seq.int(rankX + 1, pX)]
    colnames(X)[redundant_idx]
  } else {
    character(0)
  }

  tibble(
    X_rank = rankX,
    X_ncol = pX,
    fit_rank_deficient = rankdef,
    redundant_cols = collapse_unique(redundant_cols)
  )
}

inspect_design_matrices_encap_mt2_rrt <- function(df,
                                                  formula_cond,
                                                  formula_zi) {
  fixed_cond <- lme4::nobars(formula_cond)

  cond_info <- inspect_one_matrix(fixed_cond, df) %>%
    rename(
      cond_X_rank = X_rank,
      cond_X_ncol = X_ncol,
      cond_rank_deficient = fit_rank_deficient,
      cond_redundant_cols = redundant_cols
    )

  zi_info <- inspect_one_matrix(formula_zi, df) %>%
    rename(
      zi_X_rank = X_rank,
      zi_X_ncol = X_ncol,
      zi_rank_deficient = fit_rank_deficient,
      zi_redundant_cols = redundant_cols
    )

  bind_cols(cond_info, zi_info) %>%
    mutate(
      n_distinct_nLetters = dplyr::n_distinct(df$nLetters),
      min_nLetters = min(df$nLetters),
      max_nLetters = max(df$nLetters),
      prop_zero = mean(df$RRT == 0),
      n_zero = sum(df$RRT == 0),
      n_positive = sum(df$RRT > 0)
    )
}

# =========================================================
# FIT
# =========================================================

fit_glmmtmb_capture_encap_mt2_rrt <- function(df,
                                              formula_cond,
                                              formula_zi) {
  warn_texts <- character()
  msg_texts  <- character()

  fit <- withCallingHandlers(
    tryCatch(
      glmmTMB::glmmTMB(
        formula = formula_cond,
        ziformula = formula_zi,
        data = droplevels(df),
        family = ziGamma(link = "log")
      ),
      error = function(e) e
    ),
    warning = function(w) {
      warn_texts <<- c(warn_texts, conditionMessage(w))
      invokeRestart("muffleWarning")
    },
    message = function(m) {
      msg_texts <<- c(msg_texts, conditionMessage(m))
      invokeRestart("muffleMessage")
    }
  )

  if (inherits(fit, "error")) {
    return(list(
      fit_tbl = tibble(
        fit_ok = FALSE,
        fit_error = conditionMessage(fit),
        fit_warning_text = collapse_unique(warn_texts),
        fit_message_text = collapse_unique(msg_texts),
        n_warning_messages = length(unique(stats::na.omit(warn_texts))),
        n_messages = length(unique(stats::na.omit(msg_texts))),
        fit_nonposdef_hessian = NA,
        fit_nonzero_convergence = NA,
        fit_singular_re = NA,
        n_random_effect_vars = NA_integer_,
        n_near_zero_random_effect_vars = NA_integer_,
        min_random_effect_var = NA_real_,
        aic = NA_real_,
        bic = NA_real_,
        logLik = NA_real_
      ),
      fit_obj = NULL
    ))
  }

  fit_nonposdef_hessian <- tryCatch(
    !isTRUE(fit$sdr$pdHess),
    error = function(e) NA
  )

  fit_nonzero_convergence <- tryCatch(
    {
      conv <- fit$fit$convergence
      if (is.null(conv)) NA else conv != 0
    },
    error = function(e) NA
  )

  singular_info <- check_singular_re_glmmtmb(fit)

  list(
    fit_tbl = tibble(
      fit_ok = TRUE,
      fit_error = NA_character_,
      fit_warning_text = collapse_unique(warn_texts),
      fit_message_text = collapse_unique(msg_texts),
      n_warning_messages = length(unique(stats::na.omit(warn_texts))),
      n_messages = length(unique(stats::na.omit(msg_texts))),
      fit_nonposdef_hessian = fit_nonposdef_hessian,
      fit_nonzero_convergence = fit_nonzero_convergence
    ) %>%
      bind_cols(singular_info) %>%
      mutate(
        aic = tryCatch(as.numeric(AIC(fit)), error = function(e) NA_real_),
        bic = tryCatch(as.numeric(BIC(fit)), error = function(e) NA_real_),
        logLik = tryCatch(as.numeric(logLik(fit)), error = function(e) NA_real_)
      ),
    fit_obj = fit
  )
}

# =========================================================
# KLASSIFIKATION PROBLEMART
# Hessian und Singularität getrennt
# =========================================================

classify_problem_encap_mt2_rrt <- function(cond_rank_deficient,
                                           zi_rank_deficient,
                                           fit_warning_text,
                                           fit_message_text,
                                           fit_error,
                                           fit_nonposdef_hessian,
                                           fit_nonzero_convergence,
                                           fit_singular_re,
                                           prediction_error) {
  txt <- paste(
    c(fit_warning_text, fit_message_text, fit_error, prediction_error),
    collapse = " | "
  )

  any_rankdef <- isTRUE(cond_rank_deficient) || isTRUE(zi_rank_deficient)

  case_when(
    !is.na(fit_error) ~ "fit_error",
    !is.na(prediction_error) ~ "prediction_error",
    isTRUE(fit_nonposdef_hessian) ~ "nonposdef_hessian",
    isTRUE(fit_singular_re) ~ "singular_random_effects",
    isTRUE(fit_nonzero_convergence) ~ "nonzero_convergence",
    any_rankdef &
      str_detect(txt, regex("rank-deficient|dropping columns|dropp.*column", ignore_case = TRUE)) ~
      "rank_deficient_design_and_fit_signal",
    any_rankdef ~ "rank_deficient_design",
    str_detect(txt, regex("rank-deficient|dropping columns|dropp.*column", ignore_case = TRUE)) ~
      "rank_deficient_warning_without_matrix_flag",
    str_detect(txt, regex("hessian|not positive definite|non-positive-definite", ignore_case = TRUE)) ~
      "hessian_signal_only",
    str_detect(txt, regex("convergence|did not converge|false convergence|iteration limit", ignore_case = TRUE)) ~
      "convergence_signal",
    (!is.na(fit_warning_text) || !is.na(fit_message_text)) ~ "warning_or_message_only",
    TRUE ~ "none"
  )
}

# =========================================================
# EIN PARTICIPANT-LOO-FIT
# Vorhersage und Fehlermetriken auf allen Testwerten
# =========================================================

fit_one_participantloo_case_encap_mt2_rrt <- function(train_df,
                                                      test_df,
                                                      formula_cond,
                                                      formula_zi,
                                                      n_aoi_in_cluster,
                                                      exp_name = "encap_mt2",
                                                      aggr_i,
                                                      participant_i) {
  train_df <- droplevels(train_df)
  test_df  <- droplevels(test_df)

  design_info <- inspect_design_matrices_encap_mt2_rrt(
    df = train_df,
    formula_cond = formula_cond,
    formula_zi = formula_zi
  )

  fit_res <- fit_glmmtmb_capture_encap_mt2_rrt(
    df = train_df,
    formula_cond = formula_cond,
    formula_zi = formula_zi
  )

  fit_info <- fit_res$fit_tbl
  fit_obj  <- fit_res$fit_obj

  pred_error <- NA_character_
  pred <- rep(NA_real_, nrow(test_df))

  if (!is.null(fit_obj)) {
    pred <- tryCatch(
      predict(
        fit_obj,
        newdata = test_df,
        type = "response",
        re.form = NULL,
        allow.new.levels = TRUE
      ),
      error = function(e) {
        pred_error <<- conditionMessage(e)
        rep(NA_real_, nrow(test_df))
      }
    )
  }

  metric_info <- calc_metrics(
    obs = test_df$RRT,
    pred = pred
  )

  tibble(
    experiment = exp_name,
    aggr_level = aggr_i,
    heldout_participant = participant_i,
    n_aoi_in_cluster = n_aoi_in_cluster,
    cluster_formula_type = ifelse(n_aoi_in_cluster > 1, "Referent_Category_AOI", "Referent_Category_only"),
    n_train = nrow(train_df),
    n_test_rows = nrow(test_df),
    n_test_zero = sum(test_df$RRT == 0),
    n_test_positive = sum(test_df$RRT > 0),
    formula_cond = paste(deparse(formula_cond), collapse = " "),
    formula_zi = paste(deparse(formula_zi), collapse = " ")
  ) %>%
    bind_cols(design_info) %>%
    bind_cols(fit_info) %>%
    bind_cols(metric_info) %>%
    mutate(
      prediction_error = pred_error,
      problem_type = classify_problem_encap_mt2_rrt(
        cond_rank_deficient = cond_rank_deficient,
        zi_rank_deficient = zi_rank_deficient,
        fit_warning_text = fit_warning_text,
        fit_message_text = fit_message_text,
        fit_error = fit_error,
        fit_nonposdef_hessian = fit_nonposdef_hessian,
        fit_nonzero_convergence = fit_nonzero_convergence,
        fit_singular_re = fit_singular_re,
        prediction_error = prediction_error
      )
    )
}

# =========================================================
# PARTICIPANT-LOO FÜR encap_mt2, RRT
# Gesamtbereich inklusive 0ern
# =========================================================

run_participantloo_encap_mt2_rrt <- function(df,
                                             exp_name = "encap_mt2",
                                             verbose = TRUE) {
  prep <- prep_encap_mt2_participantloo_data_rrt(df)
  dat <- prep$data
  cluster_sizes <- prep$cluster_sizes

  aggr_levels <- unique(as.character(dat$aggr_level))

  loo_all <- purrr::map_dfr(aggr_levels, function(aggr_i) {
    dat_cluster <- dat %>%
      filter(as.character(aggr_level) == aggr_i)

    n_aoi_cluster <- cluster_sizes$n_aoi_in_cluster[
      match(aggr_i, cluster_sizes$aggr_level)
    ]

    formula_cond <- make_formula_cluster_encap_mt2_rrt(
      response = "RRT",
      n_aoi_in_cluster = n_aoi_cluster
    )

    formula_zi <- make_ziformula_cluster_encap_mt2_rrt(
      n_aoi_in_cluster = n_aoi_cluster
    )

    participants_cluster <- unique(as.character(dat_cluster$Participant))

    purrr::map_dfr(participants_cluster, function(participant_i) {
      train_df <- dat_cluster %>%
        filter(as.character(Participant) != participant_i)

      test_df <- dat_cluster %>%
        filter(as.character(Participant) == participant_i)

      if (isTRUE(verbose)) {
        message(
          "[RRT Participant-LOO] exp=", exp_name,
          " aggr_level=", aggr_i,
          " holdout_participant=", participant_i,
          " n_train=", nrow(train_df)
        )
      }

      fit_one_participantloo_case_encap_mt2_rrt(
        train_df = train_df,
        test_df = test_df,
        formula_cond = formula_cond,
        formula_zi = formula_zi,
        n_aoi_in_cluster = n_aoi_cluster,
        exp_name = exp_name,
        aggr_i = aggr_i,
        participant_i = participant_i
      )
    })
  })

  loo_summary <- loo_all %>%
    mutate(has_warning = n_warning_messages > 0) %>%
    group_by(experiment, aggr_level) %>%
    summarise(
      n_holdout_fits = dplyr::n(),
      n_fit_failed = sum(fit_ok %in% FALSE, na.rm = TRUE),
      n_nonposdef_hessian = sum(fit_nonposdef_hessian %in% TRUE, na.rm = TRUE),
      n_singular_re = sum(fit_singular_re %in% TRUE, na.rm = TRUE),
      n_nonzero_convergence = sum(fit_nonzero_convergence %in% TRUE, na.rm = TRUE),
      n_cond_rank_deficient = sum(cond_rank_deficient %in% TRUE, na.rm = TRUE),
      n_zi_rank_deficient = sum(zi_rank_deficient %in% TRUE, na.rm = TRUE),
      n_fits_with_warning = sum(has_warning, na.rm = TRUE),
      total_warning_messages = sum(n_warning_messages, na.rm = TRUE),
      total_test_zero = sum(n_test_zero, na.rm = TRUE),
      total_test_positive = sum(n_test_positive, na.rm = TRUE),
      mean_rmse = mean(rmse, na.rm = TRUE),
      mean_mae = mean(mae, na.rm = TRUE),
      mean_bias = mean(bias, na.rm = TRUE),
      median_rmse = median(rmse, na.rm = TRUE),
      median_mae = median(mae, na.rm = TRUE),
      median_bias = median(bias, na.rm = TRUE),
      .groups = "drop"
    )

  problem_counts <- loo_all %>%
    count(aggr_level, problem_type, sort = TRUE)

  problem_fits_only <- loo_all %>%
    filter(
      problem_type != "none" |
        fit_ok %in% FALSE |
        fit_nonposdef_hessian %in% TRUE |
        fit_singular_re %in% TRUE |
        fit_nonzero_convergence %in% TRUE |
        cond_rank_deficient %in% TRUE |
        zi_rank_deficient %in% TRUE |
        n_warning_messages > 0 |
        !is.na(prediction_error)
    ) %>%
    arrange(aggr_level, heldout_participant)

  list(
    loo_all = loo_all,
    loo_summary = loo_summary,
    problem_counts = problem_counts,
    problem_fits_only = problem_fits_only
  )
}

# =========================================================
# AUSFÜHREN
# =========================================================

res_encap_mt2_participantloo_rrt <- run_participantloo_encap_mt2_rrt(
  df = encap_mt2,
  exp_name = "encap_mt2",
  verbose = TRUE
)

# =========================================================
# OUTPUTS
# =========================================================

#round_df_4(res_encap_mt2_participantloo_rrt$loo_all)
#round_df_4(res_encap_mt2_participantloo_rrt$loo_summary)
#round_df_4(res_encap_mt2_participantloo_rrt$problem_counts)
#round_df_4(res_encap_mt2_participantloo_rrt$problem_fits_only)

knitr::kable(
 round_df_4(res_encap_mt2_participantloo_rrt$loo_summary),
  format = "html",
  table.attr = 'style="width:100%; table-layout: fixed;"'
)
```



*RRT: 0er Analyse Participant LOO*
```{r}
library(dplyr)
library(tidyr)
library(purrr)
library(tibble)
library(stringr)
library(glmmTMB)
library(lme4)

# =========================================================
# HILFSFUNKTIONEN
# =========================================================

safe_plain_df <- function(df) {
  out <- as.data.frame(df, stringsAsFactors = FALSE)
  rownames(out) <- NULL
  out
}

round_df_4 <- function(df) {
  df %>%
    mutate(across(where(is.numeric), ~ round(.x, 4)))
}

collapse_unique <- function(x) {
  x <- unique(stats::na.omit(x))
  if (length(x) == 0) return(NA_character_)
  paste(x, collapse = " | ")
}

safe_div <- function(num, den) {
  ifelse(is.na(den) | den == 0, NA_real_, num / den)
}

calc_zero_metrics <- function(obs_zero, pred_prob_zero, threshold = 0.5) {
  keep <- is.finite(obs_zero) & is.finite(pred_prob_zero)
  obs_zero <- obs_zero[keep]
  pred_prob_zero <- pred_prob_zero[keep]

  if (length(obs_zero) == 0) {
    return(tibble(
      n_eval = 0L,
      threshold = threshold,
      observed_zero_rate = NA_real_,
      mean_pred_zero_prob = NA_real_,
      calibration_gap = NA_real_,
      brier_zero = NA_real_,
      accuracy = NA_real_,
      balanced_accuracy = NA_real_,
      recall_zero = NA_real_,
      recall_positive = NA_real_,
      precision_zero = NA_real_,
      n_pred_zero = NA_real_,
      n_pred_positive = NA_real_,
      TP_zero = NA_real_,
      TN_positive = NA_real_,
      FP_zero = NA_real_,
      FN_zero = NA_real_
    ))
  }

  pred_zero_class <- pred_prob_zero >= threshold

  TP <- sum(pred_zero_class & obs_zero)
  TN <- sum(!pred_zero_class & !obs_zero)
  FP <- sum(pred_zero_class & !obs_zero)
  FN <- sum(!pred_zero_class & obs_zero)

  recall_zero <- safe_div(TP, TP + FN)
  recall_positive <- safe_div(TN, TN + FP)
  precision_zero <- safe_div(TP, TP + FP)
  accuracy <- safe_div(TP + TN, length(obs_zero))

  balanced_accuracy <- if (all(is.na(c(recall_zero, recall_positive)))) {
    NA_real_
  } else {
    mean(c(recall_zero, recall_positive), na.rm = TRUE)
  }

  observed_zero_rate <- mean(obs_zero)
  mean_pred_zero_prob <- mean(pred_prob_zero)

  tibble(
    n_eval = length(obs_zero),
    threshold = threshold,
    observed_zero_rate = observed_zero_rate,
    mean_pred_zero_prob = mean_pred_zero_prob,
    calibration_gap = mean_pred_zero_prob - observed_zero_rate,
    brier_zero = mean((pred_prob_zero - as.numeric(obs_zero))^2),
    accuracy = accuracy,
    balanced_accuracy = balanced_accuracy,
    recall_zero = recall_zero,
    recall_positive = recall_positive,
    precision_zero = precision_zero,
    n_pred_zero = sum(pred_zero_class),
    n_pred_positive = sum(!pred_zero_class),
    TP_zero = TP,
    TN_positive = TN,
    FP_zero = FP,
    FN_zero = FN
  )
}

# =========================================================
# DATEN
# Erwartet:
# RRT, Referent, Category, AOI, nLetters,
# Participant, Topic, aggr_level
# =========================================================

prep_encap_mt2_rrt_zero_participantloo_data <- function(df) {
  needed <- c(
    "RRT", "Referent", "Category", "AOI",
    "nLetters", "Participant", "Topic", "aggr_level"
  )

  missing_cols <- setdiff(needed, names(df))
  if (length(missing_cols) > 0) {
    stop("Fehlende Variablen: ", paste(missing_cols, collapse = ", "))
  }

  out <- df %>%
    filter(!is.na(RRT), RRT >= 0) %>%
    filter(!is.na(Referent), !is.na(Category), !is.na(AOI), !is.na(nLetters)) %>%
    filter(!is.na(Participant), !is.na(Topic), !is.na(aggr_level)) %>%
    mutate(
      Referent = factor(Referent),
      Category = factor(Category),
      AOI = factor(AOI),
      Participant = factor(Participant),
      Topic = factor(Topic),
      aggr_level = factor(aggr_level),
      is_zero = RRT == 0
    )

  cluster_sizes <- out %>%
    distinct(aggr_level, AOI) %>%
    count(aggr_level, name = "n_aoi_in_cluster")

  list(
    data = safe_plain_df(out),
    cluster_sizes = cluster_sizes
  )
}

# =========================================================
# FORMELN PRO CLUSTER
# =========================================================

make_formula_cluster_encap_mt2_rrt <- function(response = "RRT",
                                               n_aoi_in_cluster = 1L) {
  rhs <- if (n_aoi_in_cluster > 1) {
    "Referent*AOI + Category*AOI + nLetters + (1 | Topic) + (1 | Participant)"
  } else {
    "Referent + Category + nLetters + (1 | Topic) + (1 | Participant)"
  }

  as.formula(paste(response, "~", rhs))
}

make_ziformula_cluster_encap_mt2_rrt <- function(n_aoi_in_cluster = 1L) {
  rhs <- if (n_aoi_in_cluster > 1) {
    "Referent*AOI + Category*AOI + nLetters"
  } else {
    "Referent + Category + nLetters"
  }

  as.formula(paste("~", rhs))
}

# =========================================================
# DESIGNMATRIX-CHECKS
# =========================================================

inspect_one_matrix <- function(formula, data) {
  mf <- model.frame(formula, data = droplevels(data), na.action = na.omit)
  X <- model.matrix(formula, data = mf)

  qrX <- qr(X)
  rankX <- qrX$rank
  pX <- ncol(X)
  rankdef <- rankX < pX

  redundant_cols <- if (rankdef) {
    pivot <- qrX$pivot
    redundant_idx <- pivot[seq.int(rankX + 1, pX)]
    colnames(X)[redundant_idx]
  } else {
    character(0)
  }

  tibble(
    X_rank = rankX,
    X_ncol = pX,
    fit_rank_deficient = rankdef,
    redundant_cols = collapse_unique(redundant_cols)
  )
}

inspect_design_matrices_encap_mt2_rrt <- function(df, formula_cond, formula_zi) {
  fixed_cond <- lme4::nobars(formula_cond)

  cond_info <- inspect_one_matrix(fixed_cond, df) %>%
    rename(
      cond_X_rank = X_rank,
      cond_X_ncol = X_ncol,
      cond_rank_deficient = fit_rank_deficient,
      cond_redundant_cols = redundant_cols
    )

  zi_info <- inspect_one_matrix(formula_zi, df) %>%
    rename(
      zi_X_rank = X_rank,
      zi_X_ncol = X_ncol,
      zi_rank_deficient = fit_rank_deficient,
      zi_redundant_cols = redundant_cols
    )

  bind_cols(cond_info, zi_info) %>%
    mutate(
      n_distinct_nLetters = dplyr::n_distinct(df$nLetters),
      min_nLetters = min(df$nLetters),
      max_nLetters = max(df$nLetters)
    )
}

# =========================================================
# HEURISTIK FÜR SINGULAR RANDOM EFFECTS IN glmmTMB
# =========================================================

extract_re_variances_glmmtmb <- function(fit_obj) {
  vc <- tryCatch(VarCorr(fit_obj), error = function(e) NULL)
  if (is.null(vc) || is.null(vc$cond)) return(numeric(0))

  vars <- c()

  for (grp in names(vc$cond)) {
    mat <- vc$cond[[grp]]
    sd_attr <- attr(mat, "stddev")

    if (!is.null(sd_attr) && length(sd_attr) > 0) {
      vars <- c(vars, as.numeric(sd_attr)^2)
    } else if (is.matrix(mat) && nrow(mat) > 0) {
      vars <- c(vars, diag(as.matrix(mat)))
    }
  }

  vars
}

check_singular_re_glmmtmb <- function(fit_obj, tol = 1e-8) {
  re_vars <- extract_re_variances_glmmtmb(fit_obj)

  if (length(re_vars) == 0) {
    return(tibble(
      fit_singular_re = NA,
      n_random_effect_vars = 0L,
      n_near_zero_random_effect_vars = NA_integer_,
      min_random_effect_var = NA_real_
    ))
  }

  tibble(
    fit_singular_re = any(re_vars < tol),
    n_random_effect_vars = length(re_vars),
    n_near_zero_random_effect_vars = sum(re_vars < tol),
    min_random_effect_var = min(re_vars)
  )
}

# =========================================================
# FIT
# =========================================================

fit_glmmtmb_capture_encap_mt2_rrt <- function(df, formula_cond, formula_zi) {
  warn_texts <- character()
  msg_texts  <- character()

  fit <- withCallingHandlers(
    tryCatch(
      glmmTMB::glmmTMB(
        formula = formula_cond,
        ziformula = formula_zi,
        data = droplevels(df),
        family = ziGamma(link = "log")
      ),
      error = function(e) e
    ),
    warning = function(w) {
      warn_texts <<- c(warn_texts, conditionMessage(w))
      invokeRestart("muffleWarning")
    },
    message = function(m) {
      msg_texts <<- c(msg_texts, conditionMessage(m))
      invokeRestart("muffleMessage")
    }
  )

  if (inherits(fit, "error")) {
    return(list(
      fit_tbl = tibble(
        fit_ok = FALSE,
        fit_error = conditionMessage(fit),
        fit_warning_text = collapse_unique(warn_texts),
        fit_message_text = collapse_unique(msg_texts),
        n_warning_messages = length(unique(stats::na.omit(warn_texts))),
        n_messages = length(unique(stats::na.omit(msg_texts))),
        fit_nonposdef_hessian = NA,
        fit_nonzero_convergence = NA,
        fit_singular_re = NA,
        n_random_effect_vars = NA_integer_,
        n_near_zero_random_effect_vars = NA_integer_,
        min_random_effect_var = NA_real_,
        aic = NA_real_,
        bic = NA_real_,
        logLik = NA_real_
      ),
      fit_obj = NULL
    ))
  }

  fit_nonposdef_hessian <- tryCatch(
    !isTRUE(fit$sdr$pdHess),
    error = function(e) NA
  )

  fit_nonzero_convergence <- tryCatch(
    {
      conv <- fit$fit$convergence
      if (is.null(conv)) NA else conv != 0
    },
    error = function(e) NA
  )

  singular_info <- check_singular_re_glmmtmb(fit)

  list(
    fit_tbl = tibble(
      fit_ok = TRUE,
      fit_error = NA_character_,
      fit_warning_text = collapse_unique(warn_texts),
      fit_message_text = collapse_unique(msg_texts),
      n_warning_messages = length(unique(stats::na.omit(warn_texts))),
      n_messages = length(unique(stats::na.omit(msg_texts))),
      fit_nonposdef_hessian = fit_nonposdef_hessian,
      fit_nonzero_convergence = fit_nonzero_convergence
    ) %>%
      bind_cols(singular_info) %>%
      mutate(
        aic = tryCatch(as.numeric(AIC(fit)), error = function(e) NA_real_),
        bic = tryCatch(as.numeric(BIC(fit)), error = function(e) NA_real_),
        logLik = tryCatch(as.numeric(logLik(fit)), error = function(e) NA_real_)
      ),
    fit_obj = fit
  )
}

# =========================================================
# NULLWAHRSCHEINLICHKEIT
# =========================================================

predict_prob_zero <- function(fit_obj, newdata) {
  predict(
    fit_obj,
    newdata = newdata,
    type = "zprob",
    re.form = NULL,
    allow.new.levels = TRUE
  )
}

# =========================================================
# PROBLEMTYP
# =========================================================

classify_problem_encap_mt2_rrt <- function(cond_rank_deficient,
                                           zi_rank_deficient,
                                           fit_warning_text,
                                           fit_message_text,
                                           fit_error,
                                           fit_nonposdef_hessian,
                                           fit_nonzero_convergence,
                                           fit_singular_re,
                                           prediction_error) {
  txt <- paste(
    c(fit_warning_text, fit_message_text, fit_error, prediction_error),
    collapse = " | "
  )

  any_rankdef <- isTRUE(cond_rank_deficient) || isTRUE(zi_rank_deficient)

  case_when(
    !is.na(fit_error) ~ "fit_error",
    !is.na(prediction_error) ~ "prediction_error",
    isTRUE(fit_nonposdef_hessian) ~ "nonposdef_hessian",
    isTRUE(fit_singular_re) ~ "singular_random_effects",
    isTRUE(fit_nonzero_convergence) ~ "nonzero_convergence",
    any_rankdef &
      str_detect(txt, regex("rank-deficient|dropping columns|dropp.*column", ignore_case = TRUE)) ~
      "rank_deficient_design_and_fit_signal",
    any_rankdef ~ "rank_deficient_design",
    str_detect(txt, regex("rank-deficient|dropping columns|dropp.*column", ignore_case = TRUE)) ~
      "rank_deficient_warning_without_matrix_flag",
    str_detect(txt, regex("hessian|not positive definite|non-positive-definite", ignore_case = TRUE)) ~
      "hessian_signal_only",
    str_detect(txt, regex("convergence|did not converge|false convergence|iteration limit", ignore_case = TRUE)) ~
      "convergence_signal",
    (!is.na(fit_warning_text) || !is.na(fit_message_text)) ~ "warning_or_message_only",
    TRUE ~ "none"
  )
}

# =========================================================
# EIN PARTICIPANT-LOO-FIT
# 0-ANALYSE
# =========================================================

fit_one_participantloo_case_encap_mt2_rrt_zero <- function(train_df,
                                                           test_df,
                                                           formula_cond,
                                                           formula_zi,
                                                           n_aoi_in_cluster,
                                                           exp_name = "encap_mt2",
                                                           aggr_i,
                                                           participant_i,
                                                           threshold = 0.5) {
  train_df <- droplevels(train_df)
  test_df  <- droplevels(test_df)

  design_info <- inspect_design_matrices_encap_mt2_rrt(
    df = train_df,
    formula_cond = formula_cond,
    formula_zi = formula_zi
  )

  fit_res <- fit_glmmtmb_capture_encap_mt2_rrt(
    df = train_df,
    formula_cond = formula_cond,
    formula_zi = formula_zi
  )

  fit_info <- fit_res$fit_tbl
  fit_obj  <- fit_res$fit_obj

  pred_error <- NA_character_

  zero_metrics <- if (!is.null(fit_obj)) {
    pred_prob_zero <- tryCatch(
      predict_prob_zero(fit_obj, test_df),
      error = function(e) {
        pred_error <<- conditionMessage(e)
        rep(NA_real_, nrow(test_df))
      }
    )

    calc_zero_metrics(
      obs_zero = test_df$is_zero,
      pred_prob_zero = pred_prob_zero,
      threshold = threshold
    )
  } else {
    tibble(
      n_eval = NA_real_,
      threshold = threshold,
      observed_zero_rate = NA_real_,
      mean_pred_zero_prob = NA_real_,
      calibration_gap = NA_real_,
      brier_zero = NA_real_,
      accuracy = NA_real_,
      balanced_accuracy = NA_real_,
      recall_zero = NA_real_,
      recall_positive = NA_real_,
      precision_zero = NA_real_,
      n_pred_zero = NA_real_,
      n_pred_positive = NA_real_,
      TP_zero = NA_real_,
      TN_positive = NA_real_,
      FP_zero = NA_real_,
      FN_zero = NA_real_
    )
  }

  tibble(
    experiment = exp_name,
    aggr_level = aggr_i,
    heldout_participant = participant_i,
    n_aoi_in_cluster = n_aoi_in_cluster,
    cluster_formula_type = ifelse(n_aoi_in_cluster > 1, "Referent_Category_AOI", "Referent_Category_only"),
    n_train = nrow(train_df),
    n_test_rows = nrow(test_df),
    n_test_zero = sum(test_df$is_zero),
    n_test_positive = sum(!test_df$is_zero),
    cond_formula = paste(deparse(formula_cond), collapse = " "),
    zi_formula = paste(deparse(formula_zi), collapse = " ")
  ) %>%
    bind_cols(design_info) %>%
    bind_cols(fit_info) %>%
    bind_cols(zero_metrics) %>%
    mutate(
      prediction_error = pred_error,
      problem_type = classify_problem_encap_mt2_rrt(
        cond_rank_deficient = cond_rank_deficient,
        zi_rank_deficient = zi_rank_deficient,
        fit_warning_text = fit_warning_text,
        fit_message_text = fit_message_text,
        fit_error = fit_error,
        fit_nonposdef_hessian = fit_nonposdef_hessian,
        fit_nonzero_convergence = fit_nonzero_convergence,
        fit_singular_re = fit_singular_re,
        prediction_error = prediction_error
      )
    )
}

# =========================================================
# PARTICIPANT-LOO FÜR 0-ANALYSE
# =========================================================

run_participantloo_encap_mt2_rrt_zero <- function(df,
                                                  exp_name = "encap_mt2",
                                                  threshold = 0.5,
                                                  verbose = TRUE) {
  prep <- prep_encap_mt2_rrt_zero_participantloo_data(df)
  dat <- prep$data
  cluster_sizes <- prep$cluster_sizes

  aggr_levels <- unique(as.character(dat$aggr_level))

  loo_all <- purrr::map_dfr(aggr_levels, function(aggr_i) {
    dat_cluster <- dat %>%
      filter(as.character(aggr_level) == aggr_i)

    n_aoi_cluster <- cluster_sizes$n_aoi_in_cluster[
      match(aggr_i, cluster_sizes$aggr_level)
    ]

    formula_cond <- make_formula_cluster_encap_mt2_rrt(
      response = "RRT",
      n_aoi_in_cluster = n_aoi_cluster
    )

    formula_zi <- make_ziformula_cluster_encap_mt2_rrt(
      n_aoi_in_cluster = n_aoi_cluster
    )

    participants_cluster <- unique(as.character(dat_cluster$Participant))

    purrr::map_dfr(participants_cluster, function(participant_i) {
      train_df <- dat_cluster %>%
        filter(as.character(Participant) != participant_i)

      test_df <- dat_cluster %>%
        filter(as.character(Participant) == participant_i)

      if (isTRUE(verbose)) {
        message(
          "[RRT zero Participant-LOO] exp=", exp_name,
          " aggr_level=", aggr_i,
          " holdout_participant=", participant_i,
          " n_train=", nrow(train_df)
        )
      }

      fit_one_participantloo_case_encap_mt2_rrt_zero(
        train_df = train_df,
        test_df = test_df,
        formula_cond = formula_cond,
        formula_zi = formula_zi,
        n_aoi_in_cluster = n_aoi_cluster,
        exp_name = exp_name,
        aggr_i = aggr_i,
        participant_i = participant_i,
        threshold = threshold
      )
    })
  })

  loo_summary <- loo_all %>%
    mutate(has_warning = n_warning_messages > 0) %>%
    group_by(experiment, aggr_level) %>%
    summarise(
      n_holdout_fits = dplyr::n(),
      n_fit_failed = sum(fit_ok %in% FALSE, na.rm = TRUE),
      n_nonposdef_hessian = sum(fit_nonposdef_hessian %in% TRUE, na.rm = TRUE),
      n_singular_re = sum(fit_singular_re %in% TRUE, na.rm = TRUE),
      n_nonzero_convergence = sum(fit_nonzero_convergence %in% TRUE, na.rm = TRUE),
      n_cond_rank_deficient = sum(cond_rank_deficient %in% TRUE, na.rm = TRUE),
      n_zi_rank_deficient = sum(zi_rank_deficient %in% TRUE, na.rm = TRUE),
      n_fits_with_warning = sum(has_warning, na.rm = TRUE),
      total_warning_messages = sum(n_warning_messages, na.rm = TRUE),
      total_n_eval = sum(n_eval, na.rm = TRUE),
      mean_observed_zero_rate = mean(observed_zero_rate, na.rm = TRUE),
      mean_pred_zero_prob = mean(mean_pred_zero_prob, na.rm = TRUE),
      mean_calibration_gap = mean(calibration_gap, na.rm = TRUE),
      mean_brier_zero = mean(brier_zero, na.rm = TRUE),
      mean_accuracy = mean(accuracy, na.rm = TRUE),
      mean_balanced_accuracy = mean(balanced_accuracy, na.rm = TRUE),
      mean_recall_zero = mean(recall_zero, na.rm = TRUE),
      mean_recall_positive = mean(recall_positive, na.rm = TRUE),
      mean_precision_zero = mean(precision_zero, na.rm = TRUE),
      .groups = "drop"
    )

  problem_counts <- loo_all %>%
    count(aggr_level, problem_type, sort = TRUE)

  problem_fits_only <- loo_all %>%
    filter(
      problem_type != "none" |
        fit_ok %in% FALSE |
        fit_nonposdef_hessian %in% TRUE |
        fit_singular_re %in% TRUE |
        fit_nonzero_convergence %in% TRUE |
        cond_rank_deficient %in% TRUE |
        zi_rank_deficient %in% TRUE |
        n_warning_messages > 0 |
        !is.na(prediction_error)
    ) %>%
    arrange(aggr_level, heldout_participant)

  list(
    loo_all = loo_all,
    loo_summary = loo_summary,
    problem_counts = problem_counts,
    problem_fits_only = problem_fits_only
  )
}

# =========================================================
# AUSFÜHREN
# =========================================================

res_encap_mt2_rrt_zero_participantloo <- run_participantloo_encap_mt2_rrt_zero(
  df = encap_mt2,
  exp_name = "encap_mt2",
  threshold = 0.5,
  verbose = TRUE
)

# =========================================================
# OUTPUTS
# =========================================================

#round_df_4(res_encap_mt2_rrt_zero_participantloo$loo_all)
#round_df_4(res_encap_mt2_rrt_zero_participantloo$loo_summary)
#round_df_4(res_encap_mt2_rrt_zero_participantloo$problem_counts)
#round_df_4(res_encap_mt2_rrt_zero_participantloo$problem_fits_only)

knitr::kable(
 round_df_4(res_encap_mt2_rrt_zero_participantloo$loo_summary),
  format = "html",
  table.attr = 'style="width:100%; table-layout: fixed;"'
)
```


*RRT: 0er Analyse Topic LOO*
```{r}
library(dplyr)
library(tidyr)
library(purrr)
library(tibble)
library(stringr)
library(glmmTMB)
library(lme4)

# =========================================================
# HILFSFUNKTIONEN
# =========================================================

safe_plain_df <- function(df) {
  out <- as.data.frame(df, stringsAsFactors = FALSE)
  rownames(out) <- NULL
  out
}

round_df_4 <- function(df) {
  df %>%
    mutate(across(where(is.numeric), ~ round(.x, 4)))
}

collapse_unique <- function(x) {
  x <- unique(stats::na.omit(x))
  if (length(x) == 0) return(NA_character_)
  paste(x, collapse = " | ")
}

safe_div <- function(num, den) {
  ifelse(is.na(den) | den == 0, NA_real_, num / den)
}

calc_zero_metrics <- function(obs_zero, pred_prob_zero, threshold = 0.5) {
  keep <- is.finite(obs_zero) & is.finite(pred_prob_zero)
  obs_zero <- obs_zero[keep]
  pred_prob_zero <- pred_prob_zero[keep]

  if (length(obs_zero) == 0) {
    return(tibble(
      n_eval = 0L,
      threshold = threshold,
      observed_zero_rate = NA_real_,
      mean_pred_zero_prob = NA_real_,
      calibration_gap = NA_real_,
      brier_zero = NA_real_,
      accuracy = NA_real_,
      balanced_accuracy = NA_real_,
      recall_zero = NA_real_,
      recall_positive = NA_real_,
      precision_zero = NA_real_,
      n_pred_zero = NA_real_,
      n_pred_positive = NA_real_,
      TP_zero = NA_real_,
      TN_positive = NA_real_,
      FP_zero = NA_real_,
      FN_zero = NA_real_
    ))
  }

  pred_zero_class <- pred_prob_zero >= threshold

  TP <- sum(pred_zero_class & obs_zero)
  TN <- sum(!pred_zero_class & !obs_zero)
  FP <- sum(pred_zero_class & !obs_zero)
  FN <- sum(!pred_zero_class & obs_zero)

  recall_zero <- safe_div(TP, TP + FN)
  recall_positive <- safe_div(TN, TN + FP)
  precision_zero <- safe_div(TP, TP + FP)
  accuracy <- safe_div(TP + TN, length(obs_zero))

  balanced_accuracy <- if (all(is.na(c(recall_zero, recall_positive)))) {
    NA_real_
  } else {
    mean(c(recall_zero, recall_positive), na.rm = TRUE)
  }

  observed_zero_rate <- mean(obs_zero)
  mean_pred_zero_prob <- mean(pred_prob_zero)

  tibble(
    n_eval = length(obs_zero),
    threshold = threshold,
    observed_zero_rate = observed_zero_rate,
    mean_pred_zero_prob = mean_pred_zero_prob,
    calibration_gap = mean_pred_zero_prob - observed_zero_rate,
    brier_zero = mean((pred_prob_zero - as.numeric(obs_zero))^2),
    accuracy = accuracy,
    balanced_accuracy = balanced_accuracy,
    recall_zero = recall_zero,
    recall_positive = recall_positive,
    precision_zero = precision_zero,
    n_pred_zero = sum(pred_zero_class),
    n_pred_positive = sum(!pred_zero_class),
    TP_zero = TP,
    TN_positive = TN,
    FP_zero = FP,
    FN_zero = FN
  )
}

# =========================================================
# DATEN
# Erwartet:
# RRT, Referent, Category, AOI, nLetters,
# Participant, Topic, aggr_level
# =========================================================

prep_encap_mt2_rrt_zero_topicloo_data <- function(df) {
  needed <- c(
    "RRT", "Referent", "Category", "AOI",
    "nLetters", "Participant", "Topic", "aggr_level"
  )

  missing_cols <- setdiff(needed, names(df))
  if (length(missing_cols) > 0) {
    stop("Fehlende Variablen: ", paste(missing_cols, collapse = ", "))
  }

  out <- df %>%
    filter(!is.na(RRT), RRT >= 0) %>%
    filter(!is.na(Referent), !is.na(Category), !is.na(AOI), !is.na(nLetters)) %>%
    filter(!is.na(Participant), !is.na(Topic), !is.na(aggr_level)) %>%
    mutate(
      Referent = factor(Referent),
      Category = factor(Category),
      AOI = factor(AOI),
      Participant = factor(Participant),
      Topic = factor(Topic),
      aggr_level = factor(aggr_level),
      is_zero = RRT == 0
    )

  cluster_sizes <- out %>%
    distinct(aggr_level, AOI) %>%
    count(aggr_level, name = "n_aoi_in_cluster")

  list(
    data = safe_plain_df(out),
    cluster_sizes = cluster_sizes
  )
}

# =========================================================
# FORMELN PRO CLUSTER
# =========================================================

make_formula_cluster_encap_mt2_rrt <- function(response = "RRT",
                                               n_aoi_in_cluster = 1L) {
  rhs <- if (n_aoi_in_cluster > 1) {
    "Referent*AOI + Category*AOI + nLetters + (1 | Topic) + (1 | Participant)"
  } else {
    "Referent + Category + nLetters + (1 | Topic) + (1 | Participant)"
  }

  as.formula(paste(response, "~", rhs))
}

make_ziformula_cluster_encap_mt2_rrt <- function(n_aoi_in_cluster = 1L) {
  rhs <- if (n_aoi_in_cluster > 1) {
    "Referent*AOI + Category*AOI + nLetters"
  } else {
    "Referent + Category + nLetters"
  }

  as.formula(paste("~", rhs))
}

# =========================================================
# DESIGNMATRIX-CHECKS
# =========================================================

inspect_one_matrix <- function(formula, data) {
  mf <- model.frame(formula, data = droplevels(data), na.action = na.omit)
  X <- model.matrix(formula, data = mf)

  qrX <- qr(X)
  rankX <- qrX$rank
  pX <- ncol(X)
  rankdef <- rankX < pX

  redundant_cols <- if (rankdef) {
    pivot <- qrX$pivot
    redundant_idx <- pivot[seq.int(rankX + 1, pX)]
    colnames(X)[redundant_idx]
  } else {
    character(0)
  }

  tibble(
    X_rank = rankX,
    X_ncol = pX,
    fit_rank_deficient = rankdef,
    redundant_cols = collapse_unique(redundant_cols)
  )
}

inspect_design_matrices_encap_mt2_rrt <- function(df, formula_cond, formula_zi) {
  fixed_cond <- lme4::nobars(formula_cond)

  cond_info <- inspect_one_matrix(fixed_cond, df) %>%
    rename(
      cond_X_rank = X_rank,
      cond_X_ncol = X_ncol,
      cond_rank_deficient = fit_rank_deficient,
      cond_redundant_cols = redundant_cols
    )

  zi_info <- inspect_one_matrix(formula_zi, df) %>%
    rename(
      zi_X_rank = X_rank,
      zi_X_ncol = X_ncol,
      zi_rank_deficient = fit_rank_deficient,
      zi_redundant_cols = redundant_cols
    )

  bind_cols(cond_info, zi_info) %>%
    mutate(
      n_distinct_nLetters = dplyr::n_distinct(df$nLetters),
      min_nLetters = min(df$nLetters),
      max_nLetters = max(df$nLetters)
    )
}

# =========================================================
# HEURISTIK FÜR SINGULAR RANDOM EFFECTS IN glmmTMB
# =========================================================

extract_re_variances_glmmtmb <- function(fit_obj) {
  vc <- tryCatch(VarCorr(fit_obj), error = function(e) NULL)
  if (is.null(vc) || is.null(vc$cond)) return(numeric(0))

  vars <- c()

  for (grp in names(vc$cond)) {
    mat <- vc$cond[[grp]]
    sd_attr <- attr(mat, "stddev")

    if (!is.null(sd_attr) && length(sd_attr) > 0) {
      vars <- c(vars, as.numeric(sd_attr)^2)
    } else if (is.matrix(mat) && nrow(mat) > 0) {
      vars <- c(vars, diag(as.matrix(mat)))
    }
  }

  vars
}

check_singular_re_glmmtmb <- function(fit_obj, tol = 1e-8) {
  re_vars <- extract_re_variances_glmmtmb(fit_obj)

  if (length(re_vars) == 0) {
    return(tibble(
      fit_singular_re = NA,
      n_random_effect_vars = 0L,
      n_near_zero_random_effect_vars = NA_integer_,
      min_random_effect_var = NA_real_
    ))
  }

  tibble(
    fit_singular_re = any(re_vars < tol),
    n_random_effect_vars = length(re_vars),
    n_near_zero_random_effect_vars = sum(re_vars < tol),
    min_random_effect_var = min(re_vars)
  )
}

# =========================================================
# FIT
# =========================================================

fit_glmmtmb_capture_encap_mt2_rrt <- function(df, formula_cond, formula_zi) {
  warn_texts <- character()
  msg_texts  <- character()

  fit <- withCallingHandlers(
    tryCatch(
      glmmTMB::glmmTMB(
        formula = formula_cond,
        ziformula = formula_zi,
        data = droplevels(df),
        family = ziGamma(link = "log")
      ),
      error = function(e) e
    ),
    warning = function(w) {
      warn_texts <<- c(warn_texts, conditionMessage(w))
      invokeRestart("muffleWarning")
    },
    message = function(m) {
      msg_texts <<- c(msg_texts, conditionMessage(m))
      invokeRestart("muffleMessage")
    }
  )

  if (inherits(fit, "error")) {
    return(list(
      fit_tbl = tibble(
        fit_ok = FALSE,
        fit_error = conditionMessage(fit),
        fit_warning_text = collapse_unique(warn_texts),
        fit_message_text = collapse_unique(msg_texts),
        n_warning_messages = length(unique(stats::na.omit(warn_texts))),
        n_messages = length(unique(stats::na.omit(msg_texts))),
        fit_nonposdef_hessian = NA,
        fit_nonzero_convergence = NA,
        fit_singular_re = NA,
        n_random_effect_vars = NA_integer_,
        n_near_zero_random_effect_vars = NA_integer_,
        min_random_effect_var = NA_real_,
        aic = NA_real_,
        bic = NA_real_,
        logLik = NA_real_
      ),
      fit_obj = NULL
    ))
  }

  fit_nonposdef_hessian <- tryCatch(
    !isTRUE(fit$sdr$pdHess),
    error = function(e) NA
  )

  fit_nonzero_convergence <- tryCatch(
    {
      conv <- fit$fit$convergence
      if (is.null(conv)) NA else conv != 0
    },
    error = function(e) NA
  )

  singular_info <- check_singular_re_glmmtmb(fit)

  list(
    fit_tbl = tibble(
      fit_ok = TRUE,
      fit_error = NA_character_,
      fit_warning_text = collapse_unique(warn_texts),
      fit_message_text = collapse_unique(msg_texts),
      n_warning_messages = length(unique(stats::na.omit(warn_texts))),
      n_messages = length(unique(stats::na.omit(msg_texts))),
      fit_nonposdef_hessian = fit_nonposdef_hessian,
      fit_nonzero_convergence = fit_nonzero_convergence
    ) %>%
      bind_cols(singular_info) %>%
      mutate(
        aic = tryCatch(as.numeric(AIC(fit)), error = function(e) NA_real_),
        bic = tryCatch(as.numeric(BIC(fit)), error = function(e) NA_real_),
        logLik = tryCatch(as.numeric(logLik(fit)), error = function(e) NA_real_)
      ),
    fit_obj = fit
  )
}

# =========================================================
# NULLWAHRSCHEINLICHKEIT
# =========================================================

predict_prob_zero <- function(fit_obj, newdata) {
  predict(
    fit_obj,
    newdata = newdata,
    type = "zprob",
    re.form = NULL,
    allow.new.levels = TRUE
  )
}

# =========================================================
# PROBLEMTYP
# =========================================================

classify_problem_encap_mt2_rrt <- function(cond_rank_deficient,
                                           zi_rank_deficient,
                                           fit_warning_text,
                                           fit_message_text,
                                           fit_error,
                                           fit_nonposdef_hessian,
                                           fit_nonzero_convergence,
                                           fit_singular_re,
                                           prediction_error) {
  txt <- paste(
    c(fit_warning_text, fit_message_text, fit_error, prediction_error),
    collapse = " | "
  )

  any_rankdef <- isTRUE(cond_rank_deficient) || isTRUE(zi_rank_deficient)

  case_when(
    !is.na(fit_error) ~ "fit_error",
    !is.na(prediction_error) ~ "prediction_error",
    isTRUE(fit_nonposdef_hessian) ~ "nonposdef_hessian",
    isTRUE(fit_singular_re) ~ "singular_random_effects",
    isTRUE(fit_nonzero_convergence) ~ "nonzero_convergence",
    any_rankdef &
      str_detect(txt, regex("rank-deficient|dropping columns|dropp.*column", ignore_case = TRUE)) ~
      "rank_deficient_design_and_fit_signal",
    any_rankdef ~ "rank_deficient_design",
    str_detect(txt, regex("rank-deficient|dropping columns|dropp.*column", ignore_case = TRUE)) ~
      "rank_deficient_warning_without_matrix_flag",
    str_detect(txt, regex("hessian|not positive definite|non-positive-definite", ignore_case = TRUE)) ~
      "hessian_signal_only",
    str_detect(txt, regex("convergence|did not converge|false convergence|iteration limit", ignore_case = TRUE)) ~
      "convergence_signal",
    (!is.na(fit_warning_text) || !is.na(fit_message_text)) ~ "warning_or_message_only",
    TRUE ~ "none"
  )
}

# =========================================================
# EIN TOPIC-LOO-FIT
# 0-ANALYSE
# =========================================================

fit_one_topicloo_case_encap_mt2_rrt_zero <- function(train_df,
                                                     test_df,
                                                     formula_cond,
                                                     formula_zi,
                                                     n_aoi_in_cluster,
                                                     exp_name = "encap_mt2",
                                                     aggr_i,
                                                     topic_i,
                                                     threshold = 0.5) {
  train_df <- droplevels(train_df)
  test_df  <- droplevels(test_df)

  design_info <- inspect_design_matrices_encap_mt2_rrt(
    df = train_df,
    formula_cond = formula_cond,
    formula_zi = formula_zi
  )

  fit_res <- fit_glmmtmb_capture_encap_mt2_rrt(
    df = train_df,
    formula_cond = formula_cond,
    formula_zi = formula_zi
  )

  fit_info <- fit_res$fit_tbl
  fit_obj  <- fit_res$fit_obj

  pred_error <- NA_character_

  zero_metrics <- if (!is.null(fit_obj)) {
    pred_prob_zero <- tryCatch(
      predict_prob_zero(fit_obj, test_df),
      error = function(e) {
        pred_error <<- conditionMessage(e)
        rep(NA_real_, nrow(test_df))
      }
    )

    calc_zero_metrics(
      obs_zero = test_df$is_zero,
      pred_prob_zero = pred_prob_zero,
      threshold = threshold
    )
  } else {
    tibble(
      n_eval = NA_real_,
      threshold = threshold,
      observed_zero_rate = NA_real_,
      mean_pred_zero_prob = NA_real_,
      calibration_gap = NA_real_,
      brier_zero = NA_real_,
      accuracy = NA_real_,
      balanced_accuracy = NA_real_,
      recall_zero = NA_real_,
      recall_positive = NA_real_,
      precision_zero = NA_real_,
      n_pred_zero = NA_real_,
      n_pred_positive = NA_real_,
      TP_zero = NA_real_,
      TN_positive = NA_real_,
      FP_zero = NA_real_,
      FN_zero = NA_real_
    )
  }

  tibble(
    experiment = exp_name,
    aggr_level = aggr_i,
    heldout_topic = topic_i,
    n_aoi_in_cluster = n_aoi_in_cluster,
    cluster_formula_type = ifelse(n_aoi_in_cluster > 1, "Referent_Category_AOI", "Referent_Category_only"),
    n_train = nrow(train_df),
    n_test_rows = nrow(test_df),
    n_test_zero = sum(test_df$is_zero),
    n_test_positive = sum(!test_df$is_zero),
    cond_formula = paste(deparse(formula_cond), collapse = " "),
    zi_formula = paste(deparse(formula_zi), collapse = " ")
  ) %>%
    bind_cols(design_info) %>%
    bind_cols(fit_info) %>%
    bind_cols(zero_metrics) %>%
    mutate(
      prediction_error = pred_error,
      problem_type = classify_problem_encap_mt2_rrt(
        cond_rank_deficient = cond_rank_deficient,
        zi_rank_deficient = zi_rank_deficient,
        fit_warning_text = fit_warning_text,
        fit_message_text = fit_message_text,
        fit_error = fit_error,
        fit_nonposdef_hessian = fit_nonposdef_hessian,
        fit_nonzero_convergence = fit_nonzero_convergence,
        fit_singular_re = fit_singular_re,
        prediction_error = prediction_error
      )
    )
}

# =========================================================
# TOPIC-LOO FÜR 0-ANALYSE
# =========================================================

run_topicloo_encap_mt2_rrt_zero <- function(df,
                                            exp_name = "encap_mt2",
                                            threshold = 0.5,
                                            verbose = TRUE) {
  prep <- prep_encap_mt2_rrt_zero_topicloo_data(df)
  dat <- prep$data
  cluster_sizes <- prep$cluster_sizes

  aggr_levels <- unique(as.character(dat$aggr_level))

  loo_all <- purrr::map_dfr(aggr_levels, function(aggr_i) {
    dat_cluster <- dat %>%
      filter(as.character(aggr_level) == aggr_i)

    n_aoi_cluster <- cluster_sizes$n_aoi_in_cluster[
      match(aggr_i, cluster_sizes$aggr_level)
    ]

    formula_cond <- make_formula_cluster_encap_mt2_rrt(
      response = "RRT",
      n_aoi_in_cluster = n_aoi_cluster
    )

    formula_zi <- make_ziformula_cluster_encap_mt2_rrt(
      n_aoi_in_cluster = n_aoi_cluster
    )

    topics_cluster <- unique(as.character(dat_cluster$Topic))

    purrr::map_dfr(topics_cluster, function(topic_i) {
      train_df <- dat_cluster %>%
        filter(as.character(Topic) != topic_i)

      test_df <- dat_cluster %>%
        filter(as.character(Topic) == topic_i)

      if (isTRUE(verbose)) {
        message(
          "[RRT zero Topic-LOO] exp=", exp_name,
          " aggr_level=", aggr_i,
          " holdout_topic=", topic_i,
          " n_train=", nrow(train_df)
        )
      }

      fit_one_topicloo_case_encap_mt2_rrt_zero(
        train_df = train_df,
        test_df = test_df,
        formula_cond = formula_cond,
        formula_zi = formula_zi,
        n_aoi_in_cluster = n_aoi_cluster,
        exp_name = exp_name,
        aggr_i = aggr_i,
        topic_i = topic_i,
        threshold = threshold
      )
    })
  })

  loo_summary <- loo_all %>%
    mutate(has_warning = n_warning_messages > 0) %>%
    group_by(experiment, aggr_level) %>%
    summarise(
      n_holdout_fits = dplyr::n(),
      n_fit_failed = sum(fit_ok %in% FALSE, na.rm = TRUE),
      n_nonposdef_hessian = sum(fit_nonposdef_hessian %in% TRUE, na.rm = TRUE),
      n_singular_re = sum(fit_singular_re %in% TRUE, na.rm = TRUE),
      n_nonzero_convergence = sum(fit_nonzero_convergence %in% TRUE, na.rm = TRUE),
      n_cond_rank_deficient = sum(cond_rank_deficient %in% TRUE, na.rm = TRUE),
      n_zi_rank_deficient = sum(zi_rank_deficient %in% TRUE, na.rm = TRUE),
      n_fits_with_warning = sum(has_warning, na.rm = TRUE),
      total_warning_messages = sum(n_warning_messages, na.rm = TRUE),
      total_n_eval = sum(n_eval, na.rm = TRUE),
      mean_observed_zero_rate = mean(observed_zero_rate, na.rm = TRUE),
      mean_pred_zero_prob = mean(mean_pred_zero_prob, na.rm = TRUE),
      mean_calibration_gap = mean(calibration_gap, na.rm = TRUE),
      mean_brier_zero = mean(brier_zero, na.rm = TRUE),
      mean_accuracy = mean(accuracy, na.rm = TRUE),
      mean_balanced_accuracy = mean(balanced_accuracy, na.rm = TRUE),
      mean_recall_zero = mean(recall_zero, na.rm = TRUE),
      mean_recall_positive = mean(recall_positive, na.rm = TRUE),
      mean_precision_zero = mean(precision_zero, na.rm = TRUE),
      .groups = "drop"
    )

  problem_counts <- loo_all %>%
    count(aggr_level, problem_type, sort = TRUE)

  problem_fits_only <- loo_all %>%
    filter(
      problem_type != "none" |
        fit_ok %in% FALSE |
        fit_nonposdef_hessian %in% TRUE |
        fit_singular_re %in% TRUE |
        fit_nonzero_convergence %in% TRUE |
        cond_rank_deficient %in% TRUE |
        zi_rank_deficient %in% TRUE |
        n_warning_messages > 0 |
        !is.na(prediction_error)
    ) %>%
    arrange(aggr_level, heldout_topic)

  list(
    loo_all = loo_all,
    loo_summary = loo_summary,
    problem_counts = problem_counts,
    problem_fits_only = problem_fits_only
  )
}

# =========================================================
# AUSFÜHREN
# =========================================================

res_encap_mt2_rrt_zero_topicloo <- run_topicloo_encap_mt2_rrt_zero(
  df = encap_mt2,
  exp_name = "encap_mt2",
  threshold = 0.5,
  verbose = TRUE
)

# =========================================================
# OUTPUTS
# =========================================================

#round_df_4(res_encap_mt2_rrt_zero_topicloo$loo_all)
#round_df_4(res_encap_mt2_rrt_zero_topicloo$loo_summary)
#round_df_4(res_encap_mt2_rrt_zero_topicloo$problem_counts)
#round_df_4(res_encap_mt2_rrt_zero_topicloo$problem_fits_only)

knitr::kable(
  round_df_4(res_encap_mt2_rrt_zero_topicloo$loo_summary),
  format = "html",
  table.attr = 'style="width:100%; table-layout: fixed;"'
)
```


**Residualanalyse**

*TRT*
```{r}
library(dplyr)
library(tidyr)
library(purrr)
library(tibble)
library(stringr)
library(glmmTMB)
library(lme4)
library(DHARMa)
library(ggplot2)

# =========================================================
# SETTINGS
# =========================================================

set.seed(123)

# =========================================================
# HILFSFUNKTIONEN
# =========================================================

safe_plain_df <- function(df) {
  out <- as.data.frame(df, stringsAsFactors = FALSE)
  rownames(out) <- NULL
  out
}

round_df_4 <- function(df) {
  df %>%
    mutate(across(where(is.numeric), ~ round(.x, 4)))
}

collapse_unique <- function(x) {
  x <- unique(stats::na.omit(x))
  if (length(x) == 0) return(NA_character_)
  paste(x, collapse = " | ")
}

safe_cor <- function(x, y) {
  keep <- is.finite(x) & is.finite(y)
  x <- x[keep]
  y <- y[keep]

  if (length(x) < 3) return(NA_real_)
  suppressWarnings(stats::cor(x, y))
}

safe_test_p <- function(test_obj) {
  if (is.null(test_obj)) return(NA_real_)
  if (!("p.value" %in% names(test_obj))) return(NA_real_)
  p <- test_obj$p.value
  if (is.null(p) || length(p) == 0) return(NA_real_)
  as.numeric(p[1])
}

safe_test_stat <- function(test_obj) {
  if (is.null(test_obj)) return(NA_real_)
  if (!("statistic" %in% names(test_obj))) return(NA_real_)
  s <- test_obj$statistic
  if (is.null(s) || length(s) == 0) return(NA_real_)
  as.numeric(s[1])
}

safe_kruskal_test <- function(residual, group) {
  keep <- is.finite(residual) & !is.na(group)
  residual <- residual[keep]
  group <- group[keep]

  if (length(residual) == 0) return(NULL)
  if (length(unique(group)) < 2) return(NULL)

  tryCatch(
    kruskal.test(residual ~ group),
    error = function(e) NULL
  )
}

# =========================================================
# DATEN
# Erwartet:
# TRT, Referent, Category, AOI, nLetters,
# Participant, Topic, aggr_level
# =========================================================

prep_encap_mt2_trt_residual_data <- function(df) {
  needed <- c(
    "TRT", "Referent", "Category", "AOI",
    "nLetters", "Participant", "Topic", "aggr_level"
  )

  missing_cols <- setdiff(needed, names(df))
  if (length(missing_cols) > 0) {
    stop("Fehlende Variablen: ", paste(missing_cols, collapse = ", "))
  }

  out <- df %>%
    filter(!is.na(TRT), TRT > 0) %>%
    filter(!is.na(Referent), !is.na(Category), !is.na(AOI), !is.na(nLetters)) %>%
    filter(!is.na(Participant), !is.na(Topic), !is.na(aggr_level)) %>%
    mutate(
      Referent = factor(Referent),
      Category = factor(Category),
      AOI = factor(AOI),
      Participant = factor(Participant),
      Topic = factor(Topic),
      aggr_level = factor(aggr_level)
    )

  cluster_sizes <- out %>%
    distinct(aggr_level, AOI) %>%
    count(aggr_level, name = "n_aoi_in_cluster")

  list(
    data = safe_plain_df(out),
    cluster_sizes = cluster_sizes
  )
}

# =========================================================
# FORMELN PRO CLUSTER
# =========================================================

make_formula_cluster_encap_mt2_trt <- function(response = "TRT",
                                               n_aoi_in_cluster = 1L) {
  rhs <- if (n_aoi_in_cluster > 1) {
    "Referent*AOI + Category*AOI + nLetters + (1 | Topic) + (1 | Participant)"
  } else {
    "Referent + Category + nLetters + (1 | Topic) + (1 | Participant)"
  }

  as.formula(paste(response, "~", rhs))
}

# =========================================================
# FIT MIT FEHLER- UND WARNING-CATCHING
# =========================================================

fit_glmmtmb_capture_trt_encap_mt2 <- function(df, formula) {
  warn_texts <- character()
  msg_texts  <- character()

  fit <- withCallingHandlers(
    tryCatch(
      glmmTMB::glmmTMB(
        formula = formula,
        data = droplevels(df),
        family = Gamma(link = "log")
      ),
      error = function(e) e
    ),
    warning = function(w) {
      warn_texts <<- c(warn_texts, conditionMessage(w))
      invokeRestart("muffleWarning")
    },
    message = function(m) {
      msg_texts <<- c(msg_texts, conditionMessage(m))
      invokeRestart("muffleMessage")
    }
  )

  if (inherits(fit, "error")) {
    return(list(
      fit_tbl = tibble(
        fit_ok = FALSE,
        fit_error = conditionMessage(fit),
        fit_warning_text = collapse_unique(warn_texts),
        fit_message_text = collapse_unique(msg_texts),
        n_warning_messages = length(unique(stats::na.omit(warn_texts))),
        n_messages = length(unique(stats::na.omit(msg_texts))),
        fit_nonposdef_hessian = NA,
        fit_nonzero_convergence = NA,
        aic = NA_real_,
        bic = NA_real_,
        logLik = NA_real_
      ),
      fit_obj = NULL
    ))
  }

  fit_nonposdef_hessian <- tryCatch(
    !isTRUE(fit$sdr$pdHess),
    error = function(e) NA
  )

  fit_nonzero_convergence <- tryCatch(
    {
      conv <- fit$fit$convergence
      if (is.null(conv)) NA else conv != 0
    },
    error = function(e) NA
  )

  list(
    fit_tbl = tibble(
      fit_ok = TRUE,
      fit_error = NA_character_,
      fit_warning_text = collapse_unique(warn_texts),
      fit_message_text = collapse_unique(msg_texts),
      n_warning_messages = length(unique(stats::na.omit(warn_texts))),
      n_messages = length(unique(stats::na.omit(msg_texts))),
      fit_nonposdef_hessian = fit_nonposdef_hessian,
      fit_nonzero_convergence = fit_nonzero_convergence,
      aic = tryCatch(as.numeric(AIC(fit)), error = function(e) NA_real_),
      bic = tryCatch(as.numeric(BIC(fit)), error = function(e) NA_real_),
      logLik = tryCatch(as.numeric(logLik(fit)), error = function(e) NA_real_)
    ),
    fit_obj = fit
  )
}

# =========================================================
# DHARMa-ANALYSE
# =========================================================

analyse_dharma_trt_encap_mt2 <- function(fit_obj, df, group_var, nsim = 2000) {
  simres <- DHARMa::simulateResiduals(
    fittedModel = fit_obj,
    n = nsim,
    plot = FALSE
  )

  dharma_res <- simres$scaledResiduals
  fitted_vals <- tryCatch(
    predict(fit_obj, type = "response", re.form = NULL),
    error = function(e) rep(NA_real_, nrow(df))
  )

  uniform_test <- tryCatch(
    DHARMa::testUniformity(simres),
    error = function(e) NULL
  )

  dispersion_test <- tryCatch(
    DHARMa::testDispersion(simres),
    error = function(e) NULL
  )

  outlier_test <- tryCatch(
    DHARMa::testOutliers(simres),
    error = function(e) NULL
  )

  categorical_test_dharma <- tryCatch(
    DHARMa::testCategorical(simres, catPred = group_var),
    error = function(e) NULL
  )

  categorical_test_kruskal <- safe_kruskal_test(
    residual = dharma_res,
    group = group_var
  )

  quantiles_warn <- character()
  quantiles_test <- withCallingHandlers(
    tryCatch(
      DHARMa::testQuantiles(simres, predictor = df$nLetters),
      error = function(e) e
    ),
    warning = function(w) {
      quantiles_warn <<- c(quantiles_warn, conditionMessage(w))
      invokeRestart("muffleWarning")
    }
  )

  quantiles_error <- if (inherits(quantiles_test, "error")) {
    conditionMessage(quantiles_test)
  } else {
    NA_character_
  }

  if (inherits(quantiles_test, "error")) {
    quantiles_test <- NULL
  }

  categorical_warn <- character()
  categorical_test_dharma2 <- withCallingHandlers(
    tryCatch(
      DHARMa::testCategorical(simres, catPred = group_var),
      error = function(e) e
    ),
    warning = function(w) {
      categorical_warn <<- c(categorical_warn, conditionMessage(w))
      invokeRestart("muffleWarning")
    }
  )

  categorical_error <- if (inherits(categorical_test_dharma2, "error")) {
    conditionMessage(categorical_test_dharma2)
  } else {
    NA_character_
  }

  if (!inherits(categorical_test_dharma2, "error")) {
    categorical_test_dharma <- categorical_test_dharma2
  }

  dharma_categorical_stat <- safe_test_stat(categorical_test_dharma)
  dharma_categorical_p <- safe_test_p(categorical_test_dharma)

  if (is.na(dharma_categorical_p) && !is.null(categorical_test_kruskal)) {
    dharma_categorical_stat <- safe_test_stat(categorical_test_kruskal)
    dharma_categorical_p <- safe_test_p(categorical_test_kruskal)
  }

  residual_summary <- tibble(
    n_obs_residuals = nrow(df),
    residual_mean = mean(dharma_res, na.rm = TRUE),
    residual_median = median(dharma_res, na.rm = TRUE),
    residual_sd = stats::sd(dharma_res, na.rm = TRUE),
    residual_min = min(dharma_res, na.rm = TRUE),
    residual_max = max(dharma_res, na.rm = TRUE),
    prop_resid_lt_0_05 = mean(dharma_res < 0.05, na.rm = TRUE),
    prop_resid_gt_0_95 = mean(dharma_res > 0.95, na.rm = TRUE),
    cor_resid_fitted = safe_cor(dharma_res, fitted_vals),
    cor_resid_nLetters = safe_cor(dharma_res, df$nLetters),
    dharma_uniformity_stat = safe_test_stat(uniform_test),
    dharma_uniformity_p = safe_test_p(uniform_test),
    dharma_dispersion_stat = safe_test_stat(dispersion_test),
    dharma_dispersion_p = safe_test_p(dispersion_test),
    dharma_outlier_stat = safe_test_stat(outlier_test),
    dharma_outlier_p = safe_test_p(outlier_test),
    dharma_categorical_stat = dharma_categorical_stat,
    dharma_categorical_p = dharma_categorical_p,
    dharma_quantiles_stat = safe_test_stat(quantiles_test),
    dharma_quantiles_p = safe_test_p(quantiles_test),
    dharma_categorical_warning = collapse_unique(categorical_warn),
    dharma_categorical_error = categorical_error,
    dharma_quantiles_warning = collapse_unique(quantiles_warn),
    dharma_quantiles_error = quantiles_error
  )

  plot_data <- tibble(
    fitted = fitted_vals,
    residual = dharma_res,
    nLetters = df$nLetters,
    observed = df$TRT
  )

  list(
    simres = simres,
    residual_summary = residual_summary,
    plot_data = plot_data,
    tests = list(
      uniform_test = uniform_test,
      dispersion_test = dispersion_test,
      outlier_test = outlier_test,
      categorical_test_dharma = categorical_test_dharma,
      categorical_test_kruskal = categorical_test_kruskal,
      quantiles_test = quantiles_test
    )
  )
}

# =========================================================
# PROBLEMTYP
# =========================================================

classify_problem_trt_residual <- function(fit_ok,
                                          fit_error,
                                          fit_nonposdef_hessian,
                                          fit_nonzero_convergence,
                                          fit_warning_text,
                                          dharma_uniformity_p,
                                          dharma_dispersion_p,
                                          dharma_outlier_p,
                                          dharma_categorical_p,
                                          dharma_quantiles_p,
                                          dharma_categorical_warning,
                                          dharma_quantiles_warning,
                                          dharma_categorical_error,
                                          dharma_quantiles_error) {
  txt <- paste(
    c(
      fit_error,
      fit_warning_text,
      dharma_categorical_warning,
      dharma_quantiles_warning,
      dharma_categorical_error,
      dharma_quantiles_error
    ),
    collapse = " | "
  )

  case_when(
    !isTRUE(fit_ok) ~ "fit_error",
    isTRUE(fit_nonposdef_hessian) ~ "nonposdef_hessian",
    isTRUE(fit_nonzero_convergence) ~ "nonzero_convergence",
    !is.na(dharma_uniformity_p) && dharma_uniformity_p < 0.05 &&
      !is.na(dharma_dispersion_p) && dharma_dispersion_p < 0.05 ~
      "uniformity_and_dispersion_signal",
    !is.na(dharma_uniformity_p) && dharma_uniformity_p < 0.05 &&
      !is.na(dharma_categorical_p) && dharma_categorical_p < 0.05 ~
      "uniformity_and_categorical_signal",
    !is.na(dharma_uniformity_p) && dharma_uniformity_p < 0.05 ~
      "uniformity_signal",
    !is.na(dharma_dispersion_p) && dharma_dispersion_p < 0.05 ~
      "dispersion_signal",
    !is.na(dharma_outlier_p) && dharma_outlier_p < 0.05 ~
      "outlier_signal",
    !is.na(dharma_categorical_p) && dharma_categorical_p < 0.05 ~
      "categorical_signal",
    !is.na(dharma_quantiles_p) && dharma_quantiles_p < 0.05 ~
      "quantiles_signal",
    str_detect(txt, regex("outer Newton did not converge fully|step failure", ignore_case = TRUE)) ~
      "diagnostic_test_warning",
    str_detect(txt, regex("rank-deficient|dropping columns|dropp.*column", ignore_case = TRUE)) ~
      "rank_deficiency_signal",
    !is.na(fit_warning_text) ~ "warning_only",
    TRUE ~ "none"
  )
}

# =========================================================
# PLOTFUNKTIONEN
# =========================================================

plot_trt_residuals_vs_fitted <- function(plot_data, aggr_label, idx) {
  ggplot(plot_data, aes(x = fitted, y = residual)) +
    geom_point(alpha = 0.5) +
    labs(
      title = paste0("[", idx, "/4] TRT Residuals vs Fitted | ", aggr_label),
      x = "Fitted values",
      y = "DHARMa residuals"
    )
}

plot_trt_residuals_vs_nLetters <- function(plot_data, aggr_label, idx) {
  ggplot(plot_data, aes(x = nLetters, y = residual)) +
    geom_point(alpha = 0.5) +
    labs(
      title = paste0("[", idx, "/4] TRT Residuals vs nLetters | ", aggr_label),
      x = "nLetters",
      y = "DHARMa residuals"
    )
}

plot_trt_observed_vs_fitted <- function(plot_data, aggr_label, idx) {
  ggplot(plot_data, aes(x = fitted, y = observed)) +
    geom_point(alpha = 0.5) +
    labs(
      title = paste0("[", idx, "/4] TRT Observed vs Fitted | ", aggr_label),
      x = "Fitted values",
      y = "Observed TRT"
    )
}

# =========================================================
# EIN FULL-DATA-FIT PRO aggr_level
# =========================================================

fit_one_full_case_encap_mt2_trt_residual <- function(df_aggr,
                                                     formula,
                                                     group_var,
                                                     exp_name = "encap_mt2",
                                                     aggr_i,
                                                     n_aoi_in_cluster,
                                                     nsim = 2000) {
  df_aggr <- droplevels(df_aggr)

  fit_res <- fit_glmmtmb_capture_trt_encap_mt2(
    df = df_aggr,
    formula = formula
  )

  fit_info <- fit_res$fit_tbl
  fit_obj  <- fit_res$fit_obj

  if (is.null(fit_obj)) {
    summary_tbl <- tibble(
      experiment = exp_name,
      aggr_level = aggr_i,
      n_aoi_in_cluster = n_aoi_in_cluster,
      cluster_formula_type = ifelse(n_aoi_in_cluster > 1, "Referent_Category_AOI", "Referent_Category_only"),
      n_obs = nrow(df_aggr),
      n_topics = dplyr::n_distinct(df_aggr$Topic),
      n_participants = dplyr::n_distinct(df_aggr$Participant),
      n_Referent = dplyr::n_distinct(df_aggr$Referent),
      n_Category = dplyr::n_distinct(df_aggr$Category),
      n_AOI = dplyr::n_distinct(df_aggr$AOI),
      formula = paste(deparse(formula), collapse = " ")
    ) %>%
      bind_cols(fit_info) %>%
      mutate(
        n_obs_residuals = NA_real_,
        residual_mean = NA_real_,
        residual_median = NA_real_,
        residual_sd = NA_real_,
        residual_min = NA_real_,
        residual_max = NA_real_,
        prop_resid_lt_0_05 = NA_real_,
        prop_resid_gt_0_95 = NA_real_,
        cor_resid_fitted = NA_real_,
        cor_resid_nLetters = NA_real_,
        dharma_uniformity_stat = NA_real_,
        dharma_uniformity_p = NA_real_,
        dharma_dispersion_stat = NA_real_,
        dharma_dispersion_p = NA_real_,
        dharma_outlier_stat = NA_real_,
        dharma_outlier_p = NA_real_,
        dharma_categorical_stat = NA_real_,
        dharma_categorical_p = NA_real_,
        dharma_quantiles_stat = NA_real_,
        dharma_quantiles_p = NA_real_,
        dharma_categorical_warning = NA_character_,
        dharma_categorical_error = NA_character_,
        dharma_quantiles_warning = NA_character_,
        dharma_quantiles_error = NA_character_,
        problem_type = "fit_error"
      )

    return(list(
      summary_tbl = summary_tbl,
      simres = NULL,
      plot_data = NULL,
      tests = NULL
    ))
  }

  dharma_res <- analyse_dharma_trt_encap_mt2(
    fit_obj = fit_obj,
    df = df_aggr,
    group_var = group_var,
    nsim = nsim
  )

  summary_tbl <- tibble(
    experiment = exp_name,
    aggr_level = aggr_i,
    n_aoi_in_cluster = n_aoi_in_cluster,
    cluster_formula_type = ifelse(n_aoi_in_cluster > 1, "Referent_Category_AOI", "Referent_Category_only"),
    n_obs = nrow(df_aggr),
    n_topics = dplyr::n_distinct(df_aggr$Topic),
    n_participants = dplyr::n_distinct(df_aggr$Participant),
    n_Referent = dplyr::n_distinct(df_aggr$Referent),
    n_Category = dplyr::n_distinct(df_aggr$Category),
    n_AOI = dplyr::n_distinct(df_aggr$AOI),
    formula = paste(deparse(formula), collapse = " ")
  ) %>%
    bind_cols(fit_info) %>%
    bind_cols(dharma_res$residual_summary) %>%
    mutate(
      problem_type = classify_problem_trt_residual(
        fit_ok = fit_ok,
        fit_error = fit_error,
        fit_nonposdef_hessian = fit_nonposdef_hessian,
        fit_nonzero_convergence = fit_nonzero_convergence,
        fit_warning_text = fit_warning_text,
        dharma_uniformity_p = dharma_uniformity_p,
        dharma_dispersion_p = dharma_dispersion_p,
        dharma_outlier_p = dharma_outlier_p,
        dharma_categorical_p = dharma_categorical_p,
        dharma_quantiles_p = dharma_quantiles_p,
        dharma_categorical_warning = dharma_categorical_warning,
        dharma_quantiles_warning = dharma_quantiles_warning,
        dharma_categorical_error = dharma_categorical_error,
        dharma_quantiles_error = dharma_quantiles_error
      )
    )

  list(
    summary_tbl = summary_tbl,
    simres = dharma_res$simres,
    plot_data = dharma_res$plot_data,
    tests = dharma_res$tests
  )
}

# =========================================================
# AUTOMATISCHE PLOTAUSGABE
# =========================================================

print_all_trt_residual_plots <- function(res_obj) {
  aggr_levels <- names(res_obj$plot_data_list)

  for (i in seq_along(aggr_levels)) {
    aggr_i <- aggr_levels[i]
    plot_data <- res_obj$plot_data_list[[aggr_i]]
    simres <- res_obj$simres_list[[aggr_i]]

    if (is.null(plot_data) || is.null(simres)) next

    print(plot_trt_residuals_vs_fitted(plot_data, aggr_i, 1))
    print(plot_trt_residuals_vs_nLetters(plot_data, aggr_i, 2))
    print(plot_trt_observed_vs_fitted(plot_data, aggr_i, 3))

    graphics::plot(simres)
    graphics::mtext(
      text = paste0("[4/4] Standard DHARMa Plot | ", aggr_i),
      side = 3,
      line = 1,
      cex = 1.1,
      font = 2
    )
  }
}

# =========================================================
# HAUPTFUNKTION
# =========================================================

run_trt_residual_analysis_encap_mt2 <- function(df,
                                                exp_name = "encap_mt2",
                                                nsim = 2000,
                                                print_plots = TRUE) {
  prep <- prep_encap_mt2_trt_residual_data(df)
  dat <- prep$data
  cluster_sizes <- prep$cluster_sizes

  aggr_levels <- unique(as.character(dat$aggr_level))

  res_list <- purrr::map(aggr_levels, function(aggr_i) {
    df_aggr <- dat %>%
      filter(as.character(aggr_level) == aggr_i)

    n_aoi_cluster <- cluster_sizes$n_aoi_in_cluster[
      match(aggr_i, cluster_sizes$aggr_level)
    ]

    formula_i <- make_formula_cluster_encap_mt2_trt(
      response = "TRT",
      n_aoi_in_cluster = n_aoi_cluster
    )

    group_var <- if (n_aoi_cluster > 1) {
      interaction(
        df_aggr$Referent,
        df_aggr$Category,
        df_aggr$AOI,
        drop = TRUE
      )
    } else {
      interaction(
        df_aggr$Referent,
        df_aggr$Category,
        drop = TRUE
      )
    }

    fit_one_full_case_encap_mt2_trt_residual(
      df_aggr = df_aggr,
      formula = formula_i,
      group_var = group_var,
      exp_name = exp_name,
      aggr_i = aggr_i,
      n_aoi_in_cluster = n_aoi_cluster,
      nsim = nsim
    )
  })

  names(res_list) <- aggr_levels

  residual_summary <- purrr::map_dfr(res_list, "summary_tbl")

  problem_counts <- residual_summary %>%
    count(problem_type, sort = TRUE)

  problem_fits_only <- residual_summary %>%
    filter(problem_type != "none") %>%
    arrange(aggr_level)

  plot_data_list <- purrr::map(res_list, "plot_data")
  simres_list <- purrr::map(res_list, "simres")
  tests_list <- purrr::map(res_list, "tests")

  out <- list(
    residual_summary = residual_summary,
    problem_counts = problem_counts,
    problem_fits_only = problem_fits_only,
    plot_data_list = plot_data_list,
    simres_list = simres_list,
    tests_list = tests_list,
    raw_results = res_list
  )

  if (isTRUE(print_plots)) {
    print_all_trt_residual_plots(out)
  }

  out
}

# =========================================================
# AUSFÜHREN
# =========================================================

res_trt_residuals_encap_mt2 <- run_trt_residual_analysis_encap_mt2(
  df = encap_mt2,
  exp_name = "encap_mt2",
  nsim = 2000,
  print_plots = TRUE
)

# =========================================================
# TABELLEN
# =========================================================

#round_df_4(res_trt_residuals_encap_mt2$residual_summary)
#round_df_4(res_trt_residuals_encap_mt2$problem_counts)
#round_df_4(res_trt_residuals_encap_mt2$problem_fits_only)


knitr::kable(
  round_df_4(res_trt_residuals_encap_mt2$problem_counts),
  format = "html",
  table.attr = 'style="width:100%; table-layout: fixed;"'
)

tab <-    round_df_4(res_trt_residuals_encap_mt2$residual_summary) %>% select(- formula)


print_split_table <- function(tab, id_cols = 1:3, chunk_size = 6, font_size = 8) {
  id_names <- names(tab)[id_cols]
  other_names <- names(tab)[-id_cols]

  split_index <- ceiling(seq_along(other_names) / chunk_size)
  groups <- split(other_names, split_index)

  for (i in seq_along(groups)) {
    cols_i <- c(id_names, groups[[i]])
    tab_i <- tab[, cols_i, drop = FALSE]

    print(
      knitr::kable(
        tab_i,
        format = "html",
        escape = FALSE
      ) %>%
        kable_styling(full_width = TRUE, font_size = font_size)
    )

    cat("<br><br>")
  }
}
print_split_table(tab)
```


*FRT*
```{r}
library(dplyr)
library(tidyr)
library(purrr)
library(tibble)
library(stringr)
library(glmmTMB)
library(lme4)
library(DHARMa)
library(ggplot2)

# =========================================================
# SETTINGS
# =========================================================

set.seed(123)

# =========================================================
# HILFSFUNKTIONEN
# =========================================================

safe_plain_df <- function(df) {
  out <- as.data.frame(df, stringsAsFactors = FALSE)
  rownames(out) <- NULL
  out
}

round_df_4 <- function(df) {
  df %>%
    mutate(across(where(is.numeric), ~ round(.x, 4)))
}

collapse_unique <- function(x) {
  x <- unique(stats::na.omit(x))
  if (length(x) == 0) return(NA_character_)
  paste(x, collapse = " | ")
}

safe_cor <- function(x, y) {
  keep <- is.finite(x) & is.finite(y)
  x <- x[keep]
  y <- y[keep]

  if (length(x) < 3) return(NA_real_)
  suppressWarnings(stats::cor(x, y))
}

safe_test_p <- function(test_obj) {
  if (is.null(test_obj)) return(NA_real_)
  if (!("p.value" %in% names(test_obj))) return(NA_real_)
  p <- test_obj$p.value
  if (is.null(p) || length(p) == 0) return(NA_real_)
  as.numeric(p[1])
}

safe_test_stat <- function(test_obj) {
  if (is.null(test_obj)) return(NA_real_)
  if (!("statistic" %in% names(test_obj))) return(NA_real_)
  s <- test_obj$statistic
  if (is.null(s) || length(s) == 0) return(NA_real_)
  as.numeric(s[1])
}

safe_kruskal_test <- function(residual, group) {
  keep <- is.finite(residual) & !is.na(group)
  residual <- residual[keep]
  group <- group[keep]

  if (length(residual) == 0) return(NULL)
  if (length(unique(group)) < 2) return(NULL)

  tryCatch(
    kruskal.test(residual ~ group),
    error = function(e) NULL
  )
}

# =========================================================
# DATEN
# Erwartet:
# FRT, Referent, Category, AOI, nLetters,
# Participant, Topic, aggr_level
# =========================================================

prep_encap_mt2_frt_residual_data <- function(df) {
  needed <- c(
    "FRT", "Referent", "Category", "AOI",
    "nLetters", "Participant", "Topic", "aggr_level"
  )

  missing_cols <- setdiff(needed, names(df))
  if (length(missing_cols) > 0) {
    stop("Fehlende Variablen: ", paste(missing_cols, collapse = ", "))
  }

  out <- df %>%
    filter(!is.na(FRT), FRT > 0) %>%
    filter(!is.na(Referent), !is.na(Category), !is.na(AOI), !is.na(nLetters)) %>%
    filter(!is.na(Participant), !is.na(Topic), !is.na(aggr_level)) %>%
    mutate(
      Referent = factor(Referent),
      Category = factor(Category),
      AOI = factor(AOI),
      Participant = factor(Participant),
      Topic = factor(Topic),
      aggr_level = factor(aggr_level)
    )

  cluster_sizes <- out %>%
    distinct(aggr_level, AOI) %>%
    count(aggr_level, name = "n_aoi_in_cluster")

  list(
    data = safe_plain_df(out),
    cluster_sizes = cluster_sizes
  )
}

# =========================================================
# FORMELN PRO CLUSTER
# =========================================================

make_formula_cluster_encap_mt2_frt <- function(response = "FRT",
                                               n_aoi_in_cluster = 1L) {
  rhs <- if (n_aoi_in_cluster > 1) {
    "Referent*AOI + Category*AOI + nLetters + (1 | Topic) + (1 | Participant)"
  } else {
    "Referent + Category + nLetters + (1 | Topic) + (1 | Participant)"
  }

  as.formula(paste(response, "~", rhs))
}

# =========================================================
# FIT MIT FEHLER- UND WARNING-CATCHING
# =========================================================

fit_glmmtmb_capture_frt_encap_mt2 <- function(df, formula) {
  warn_texts <- character()
  msg_texts  <- character()

  fit <- withCallingHandlers(
    tryCatch(
      glmmTMB::glmmTMB(
        formula = formula,
        data = droplevels(df),
        family = Gamma(link = "log")
      ),
      error = function(e) e
    ),
    warning = function(w) {
      warn_texts <<- c(warn_texts, conditionMessage(w))
      invokeRestart("muffleWarning")
    },
    message = function(m) {
      msg_texts <<- c(msg_texts, conditionMessage(m))
      invokeRestart("muffleMessage")
    }
  )

  if (inherits(fit, "error")) {
    return(list(
      fit_tbl = tibble(
        fit_ok = FALSE,
        fit_error = conditionMessage(fit),
        fit_warning_text = collapse_unique(warn_texts),
        fit_message_text = collapse_unique(msg_texts),
        n_warning_messages = length(unique(stats::na.omit(warn_texts))),
        n_messages = length(unique(stats::na.omit(msg_texts))),
        fit_nonposdef_hessian = NA,
        fit_nonzero_convergence = NA,
        aic = NA_real_,
        bic = NA_real_,
        logLik = NA_real_
      ),
      fit_obj = NULL
    ))
  }

  fit_nonposdef_hessian <- tryCatch(
    !isTRUE(fit$sdr$pdHess),
    error = function(e) NA
  )

  fit_nonzero_convergence <- tryCatch(
    {
      conv <- fit$fit$convergence
      if (is.null(conv)) NA else conv != 0
    },
    error = function(e) NA
  )

  list(
    fit_tbl = tibble(
      fit_ok = TRUE,
      fit_error = NA_character_,
      fit_warning_text = collapse_unique(warn_texts),
      fit_message_text = collapse_unique(msg_texts),
      n_warning_messages = length(unique(stats::na.omit(warn_texts))),
      n_messages = length(unique(stats::na.omit(msg_texts))),
      fit_nonposdef_hessian = fit_nonposdef_hessian,
      fit_nonzero_convergence = fit_nonzero_convergence,
      aic = tryCatch(as.numeric(AIC(fit)), error = function(e) NA_real_),
      bic = tryCatch(as.numeric(BIC(fit)), error = function(e) NA_real_),
      logLik = tryCatch(as.numeric(logLik(fit)), error = function(e) NA_real_)
    ),
    fit_obj = fit
  )
}

# =========================================================
# DHARMa-ANALYSE
# =========================================================

analyse_dharma_frt_encap_mt2 <- function(fit_obj, df, group_var, nsim = 2000) {
  simres <- DHARMa::simulateResiduals(
    fittedModel = fit_obj,
    n = nsim,
    plot = FALSE
  )

  dharma_res <- simres$scaledResiduals
  fitted_vals <- tryCatch(
    predict(fit_obj, type = "response", re.form = NULL),
    error = function(e) rep(NA_real_, nrow(df))
  )

  uniform_test <- tryCatch(
    DHARMa::testUniformity(simres),
    error = function(e) NULL
  )

  dispersion_test <- tryCatch(
    DHARMa::testDispersion(simres),
    error = function(e) NULL
  )

  outlier_test <- tryCatch(
    DHARMa::testOutliers(simres),
    error = function(e) NULL
  )

  categorical_test_dharma <- tryCatch(
    DHARMa::testCategorical(simres, catPred = group_var),
    error = function(e) NULL
  )

  categorical_test_kruskal <- safe_kruskal_test(
    residual = dharma_res,
    group = group_var
  )

  quantiles_warn <- character()
  quantiles_test <- withCallingHandlers(
    tryCatch(
      DHARMa::testQuantiles(simres, predictor = df$nLetters),
      error = function(e) e
    ),
    warning = function(w) {
      quantiles_warn <<- c(quantiles_warn, conditionMessage(w))
      invokeRestart("muffleWarning")
    }
  )

  quantiles_error <- if (inherits(quantiles_test, "error")) {
    conditionMessage(quantiles_test)
  } else {
    NA_character_
  }

  if (inherits(quantiles_test, "error")) {
    quantiles_test <- NULL
  }

  categorical_warn <- character()
  categorical_test_dharma2 <- withCallingHandlers(
    tryCatch(
      DHARMa::testCategorical(simres, catPred = group_var),
      error = function(e) e
    ),
    warning = function(w) {
      categorical_warn <<- c(categorical_warn, conditionMessage(w))
      invokeRestart("muffleWarning")
    }
  )

  categorical_error <- if (inherits(categorical_test_dharma2, "error")) {
    conditionMessage(categorical_test_dharma2)
  } else {
    NA_character_
  }

  if (!inherits(categorical_test_dharma2, "error")) {
    categorical_test_dharma <- categorical_test_dharma2
  }

  dharma_categorical_stat <- safe_test_stat(categorical_test_dharma)
  dharma_categorical_p <- safe_test_p(categorical_test_dharma)

  if (is.na(dharma_categorical_p) && !is.null(categorical_test_kruskal)) {
    dharma_categorical_stat <- safe_test_stat(categorical_test_kruskal)
    dharma_categorical_p <- safe_test_p(categorical_test_kruskal)
  }

  residual_summary <- tibble(
    n_obs_residuals = nrow(df),
    residual_mean = mean(dharma_res, na.rm = TRUE),
    residual_median = median(dharma_res, na.rm = TRUE),
    residual_sd = stats::sd(dharma_res, na.rm = TRUE),
    residual_min = min(dharma_res, na.rm = TRUE),
    residual_max = max(dharma_res, na.rm = TRUE),
    prop_resid_lt_0_05 = mean(dharma_res < 0.05, na.rm = TRUE),
    prop_resid_gt_0_95 = mean(dharma_res > 0.95, na.rm = TRUE),
    cor_resid_fitted = safe_cor(dharma_res, fitted_vals),
    cor_resid_nLetters = safe_cor(dharma_res, df$nLetters),
    dharma_uniformity_stat = safe_test_stat(uniform_test),
    dharma_uniformity_p = safe_test_p(uniform_test),
    dharma_dispersion_stat = safe_test_stat(dispersion_test),
    dharma_dispersion_p = safe_test_p(dispersion_test),
    dharma_outlier_stat = safe_test_stat(outlier_test),
    dharma_outlier_p = safe_test_p(outlier_test),
    dharma_categorical_stat = dharma_categorical_stat,
    dharma_categorical_p = dharma_categorical_p,
    dharma_quantiles_stat = safe_test_stat(quantiles_test),
    dharma_quantiles_p = safe_test_p(quantiles_test),
    dharma_categorical_warning = collapse_unique(categorical_warn),
    dharma_categorical_error = categorical_error,
    dharma_quantiles_warning = collapse_unique(quantiles_warn),
    dharma_quantiles_error = quantiles_error
  )

  plot_data <- tibble(
    fitted = fitted_vals,
    residual = dharma_res,
    nLetters = df$nLetters,
    observed = df$FRT
  )

  list(
    simres = simres,
    residual_summary = residual_summary,
    plot_data = plot_data,
    tests = list(
      uniform_test = uniform_test,
      dispersion_test = dispersion_test,
      outlier_test = outlier_test,
      categorical_test_dharma = categorical_test_dharma,
      categorical_test_kruskal = categorical_test_kruskal,
      quantiles_test = quantiles_test
    )
  )
}

# =========================================================
# PROBLEMTYP
# =========================================================

classify_problem_frt_residual <- function(fit_ok,
                                          fit_error,
                                          fit_nonposdef_hessian,
                                          fit_nonzero_convergence,
                                          fit_warning_text,
                                          dharma_uniformity_p,
                                          dharma_dispersion_p,
                                          dharma_outlier_p,
                                          dharma_categorical_p,
                                          dharma_quantiles_p,
                                          dharma_categorical_warning,
                                          dharma_quantiles_warning,
                                          dharma_categorical_error,
                                          dharma_quantiles_error) {
  txt <- paste(
    c(
      fit_error,
      fit_warning_text,
      dharma_categorical_warning,
      dharma_quantiles_warning,
      dharma_categorical_error,
      dharma_quantiles_error
    ),
    collapse = " | "
  )

  case_when(
    !isTRUE(fit_ok) ~ "fit_error",
    isTRUE(fit_nonposdef_hessian) ~ "nonposdef_hessian",
    isTRUE(fit_nonzero_convergence) ~ "nonzero_convergence",
    !is.na(dharma_uniformity_p) && dharma_uniformity_p < 0.05 &&
      !is.na(dharma_dispersion_p) && dharma_dispersion_p < 0.05 ~
      "uniformity_and_dispersion_signal",
    !is.na(dharma_uniformity_p) && dharma_uniformity_p < 0.05 &&
      !is.na(dharma_categorical_p) && dharma_categorical_p < 0.05 ~
      "uniformity_and_categorical_signal",
    !is.na(dharma_uniformity_p) && dharma_uniformity_p < 0.05 ~
      "uniformity_signal",
    !is.na(dharma_dispersion_p) && dharma_dispersion_p < 0.05 ~
      "dispersion_signal",
    !is.na(dharma_outlier_p) && dharma_outlier_p < 0.05 ~
      "outlier_signal",
    !is.na(dharma_categorical_p) && dharma_categorical_p < 0.05 ~
      "categorical_signal",
    !is.na(dharma_quantiles_p) && dharma_quantiles_p < 0.05 ~
      "quantiles_signal",
    str_detect(txt, regex("outer Newton did not converge fully|step failure", ignore_case = TRUE)) ~
      "diagnostic_test_warning",
    str_detect(txt, regex("rank-deficient|dropping columns|dropp.*column", ignore_case = TRUE)) ~
      "rank_deficiency_signal",
    !is.na(fit_warning_text) ~ "warning_only",
    TRUE ~ "none"
  )
}

# =========================================================
# PLOTFUNKTIONEN
# =========================================================

plot_frt_residuals_vs_fitted <- function(plot_data, aggr_label, idx) {
  ggplot(plot_data, aes(x = fitted, y = residual)) +
    geom_point(alpha = 0.5) +
    labs(
      title = paste0("[", idx, "/4] FRT Residuals vs Fitted | ", aggr_label),
      x = "Fitted values",
      y = "DHARMa residuals"
    )
}

plot_frt_residuals_vs_nLetters <- function(plot_data, aggr_label, idx) {
  ggplot(plot_data, aes(x = nLetters, y = residual)) +
    geom_point(alpha = 0.5) +
    labs(
      title = paste0("[", idx, "/4] FRT Residuals vs nLetters | ", aggr_label),
      x = "nLetters",
      y = "DHARMa residuals"
    )
}

plot_frt_observed_vs_fitted <- function(plot_data, aggr_label, idx) {
  ggplot(plot_data, aes(x = fitted, y = observed)) +
    geom_point(alpha = 0.5) +
    labs(
      title = paste0("[", idx, "/4] FRT Observed vs Fitted | ", aggr_label),
      x = "Fitted values",
      y = "Observed FRT"
    )
}

# =========================================================
# EIN FULL-DATA-FIT PRO aggr_level
# =========================================================

fit_one_full_case_encap_mt2_frt_residual <- function(df_aggr,
                                                     formula,
                                                     group_var,
                                                     exp_name = "encap_mt2",
                                                     aggr_i,
                                                     n_aoi_in_cluster,
                                                     nsim = 2000) {
  df_aggr <- droplevels(df_aggr)

  fit_res <- fit_glmmtmb_capture_frt_encap_mt2(
    df = df_aggr,
    formula = formula
  )

  fit_info <- fit_res$fit_tbl
  fit_obj  <- fit_res$fit_obj

  if (is.null(fit_obj)) {
    summary_tbl <- tibble(
      experiment = exp_name,
      aggr_level = aggr_i,
      n_aoi_in_cluster = n_aoi_in_cluster,
      cluster_formula_type = ifelse(n_aoi_in_cluster > 1, "Referent_Category_AOI", "Referent_Category_only"),
      n_obs = nrow(df_aggr),
      n_topics = dplyr::n_distinct(df_aggr$Topic),
      n_participants = dplyr::n_distinct(df_aggr$Participant),
      n_Referent = dplyr::n_distinct(df_aggr$Referent),
      n_Category = dplyr::n_distinct(df_aggr$Category),
      n_AOI = dplyr::n_distinct(df_aggr$AOI),
      formula = paste(deparse(formula), collapse = " ")
    ) %>%
      bind_cols(fit_info) %>%
      mutate(
        n_obs_residuals = NA_real_,
        residual_mean = NA_real_,
        residual_median = NA_real_,
        residual_sd = NA_real_,
        residual_min = NA_real_,
        residual_max = NA_real_,
        prop_resid_lt_0_05 = NA_real_,
        prop_resid_gt_0_95 = NA_real_,
        cor_resid_fitted = NA_real_,
        cor_resid_nLetters = NA_real_,
        dharma_uniformity_stat = NA_real_,
        dharma_uniformity_p = NA_real_,
        dharma_dispersion_stat = NA_real_,
        dharma_dispersion_p = NA_real_,
        dharma_outlier_stat = NA_real_,
        dharma_outlier_p = NA_real_,
        dharma_categorical_stat = NA_real_,
        dharma_categorical_p = NA_real_,
        dharma_quantiles_stat = NA_real_,
        dharma_quantiles_p = NA_real_,
        dharma_categorical_warning = NA_character_,
        dharma_categorical_error = NA_character_,
        dharma_quantiles_warning = NA_character_,
        dharma_quantiles_error = NA_character_,
        problem_type = "fit_error"
      )

    return(list(
      summary_tbl = summary_tbl,
      simres = NULL,
      plot_data = NULL,
      tests = NULL
    ))
  }

  dharma_res <- analyse_dharma_frt_encap_mt2(
    fit_obj = fit_obj,
    df = df_aggr,
    group_var = group_var,
    nsim = nsim
  )

  summary_tbl <- tibble(
    experiment = exp_name,
    aggr_level = aggr_i,
    n_aoi_in_cluster = n_aoi_in_cluster,
    cluster_formula_type = ifelse(n_aoi_in_cluster > 1, "Referent_Category_AOI", "Referent_Category_only"),
    n_obs = nrow(df_aggr),
    n_topics = dplyr::n_distinct(df_aggr$Topic),
    n_participants = dplyr::n_distinct(df_aggr$Participant),
    n_Referent = dplyr::n_distinct(df_aggr$Referent),
    n_Category = dplyr::n_distinct(df_aggr$Category),
    n_AOI = dplyr::n_distinct(df_aggr$AOI),
    formula = paste(deparse(formula), collapse = " ")
  ) %>%
    bind_cols(fit_info) %>%
    bind_cols(dharma_res$residual_summary) %>%
    mutate(
      problem_type = classify_problem_frt_residual(
        fit_ok = fit_ok,
        fit_error = fit_error,
        fit_nonposdef_hessian = fit_nonposdef_hessian,
        fit_nonzero_convergence = fit_nonzero_convergence,
        fit_warning_text = fit_warning_text,
        dharma_uniformity_p = dharma_uniformity_p,
        dharma_dispersion_p = dharma_dispersion_p,
        dharma_outlier_p = dharma_outlier_p,
        dharma_categorical_p = dharma_categorical_p,
        dharma_quantiles_p = dharma_quantiles_p,
        dharma_categorical_warning = dharma_categorical_warning,
        dharma_quantiles_warning = dharma_quantiles_warning,
        dharma_categorical_error = dharma_categorical_error,
        dharma_quantiles_error = dharma_quantiles_error
      )
    )

  list(
    summary_tbl = summary_tbl,
    simres = dharma_res$simres,
    plot_data = dharma_res$plot_data,
    tests = dharma_res$tests
  )
}

# =========================================================
# AUTOMATISCHE PLOTAUSGABE
# =========================================================

print_all_frt_residual_plots <- function(res_obj) {
  aggr_levels <- names(res_obj$plot_data_list)

  for (i in seq_along(aggr_levels)) {
    aggr_i <- aggr_levels[i]
    plot_data <- res_obj$plot_data_list[[aggr_i]]
    simres <- res_obj$simres_list[[aggr_i]]

    if (is.null(plot_data) || is.null(simres)) next

    print(plot_frt_residuals_vs_fitted(plot_data, aggr_i, 1))
    print(plot_frt_residuals_vs_nLetters(plot_data, aggr_i, 2))
    print(plot_frt_observed_vs_fitted(plot_data, aggr_i, 3))

    graphics::plot(simres)
    graphics::mtext(
      text = paste0("[4/4] Standard DHARMa Plot | ", aggr_i),
      side = 3,
      line = 1,
      cex = 1.1,
      font = 2
    )
  }
}

# =========================================================
# HAUPTFUNKTION
# =========================================================

run_frt_residual_analysis_encap_mt2 <- function(df,
                                                exp_name = "encap_mt2",
                                                nsim = 2000,
                                                print_plots = TRUE) {
  prep <- prep_encap_mt2_frt_residual_data(df)
  dat <- prep$data
  cluster_sizes <- prep$cluster_sizes

  aggr_levels <- unique(as.character(dat$aggr_level))

  res_list <- purrr::map(aggr_levels, function(aggr_i) {
    df_aggr <- dat %>%
      filter(as.character(aggr_level) == aggr_i)

    n_aoi_cluster <- cluster_sizes$n_aoi_in_cluster[
      match(aggr_i, cluster_sizes$aggr_level)
    ]

    formula_i <- make_formula_cluster_encap_mt2_frt(
      response = "FRT",
      n_aoi_in_cluster = n_aoi_cluster
    )

    group_var <- if (n_aoi_cluster > 1) {
      interaction(
        df_aggr$Referent,
        df_aggr$Category,
        df_aggr$AOI,
        drop = TRUE
      )
    } else {
      interaction(
        df_aggr$Referent,
        df_aggr$Category,
        drop = TRUE
      )
    }

    fit_one_full_case_encap_mt2_frt_residual(
      df_aggr = df_aggr,
      formula = formula_i,
      group_var = group_var,
      exp_name = exp_name,
      aggr_i = aggr_i,
      n_aoi_in_cluster = n_aoi_cluster,
      nsim = nsim
    )
  })

  names(res_list) <- aggr_levels

  residual_summary <- purrr::map_dfr(res_list, "summary_tbl")

  problem_counts <- residual_summary %>%
    count(problem_type, sort = TRUE)

  problem_fits_only <- residual_summary %>%
    filter(problem_type != "none") %>%
    arrange(aggr_level)

  plot_data_list <- purrr::map(res_list, "plot_data")
  simres_list <- purrr::map(res_list, "simres")
  tests_list <- purrr::map(res_list, "tests")

  out <- list(
    residual_summary = residual_summary,
    problem_counts = problem_counts,
    problem_fits_only = problem_fits_only,
    plot_data_list = plot_data_list,
    simres_list = simres_list,
    tests_list = tests_list,
    raw_results = res_list
  )

  if (isTRUE(print_plots)) {
    print_all_frt_residual_plots(out)
  }

  out
}

# =========================================================
# AUSFÜHREN
# =========================================================

res_frt_residuals_encap_mt2 <- run_frt_residual_analysis_encap_mt2(
  df = encap_mt2,
  exp_name = "encap_mt2",
  nsim = 2000,
  print_plots = TRUE
)

# =========================================================
# TABELLEN
# =========================================================

#round_df_4(res_frt_residuals_encap_mt2$residual_summary)
#round_df_4(res_frt_residuals_encap_mt2$problem_counts)
#round_df_4(res_frt_residuals_encap_mt2$problem_fits_only)


knitr::kable(
  round_df_4(res_frt_residuals_encap_mt2$problem_counts),
  format = "html",
  table.attr = 'style="width:100%; table-layout: fixed;"'
)


tab <-  round_df_4(res_frt_residuals_encap_mt2$residual_summary) %>% select(- formula)


print_split_table <- function(tab, id_cols = 1:3, chunk_size = 6, font_size = 8) {
  id_names <- names(tab)[id_cols]
  other_names <- names(tab)[-id_cols]

  split_index <- ceiling(seq_along(other_names) / chunk_size)
  groups <- split(other_names, split_index)

  for (i in seq_along(groups)) {
    cols_i <- c(id_names, groups[[i]])
    tab_i <- tab[, cols_i, drop = FALSE]

    print(
      knitr::kable(
        tab_i,
        format = "html",
        escape = FALSE
      ) %>%
        kable_styling(full_width = TRUE, font_size = font_size)
    )

    cat("<br><br>")
  }
}
print_split_table(tab)
```



`

